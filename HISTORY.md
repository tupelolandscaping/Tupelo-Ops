# HISTORY.md — Master Append-Only Audit Log

Single chronological record of what changed, when, and why, for Tupelo-Ops. Consolidates decisions, milestones, revisions, data changes, and tooling changes into one log — there is no separate `decisions/` file. Filtering to `Type: DECISION` yields the decision track alone. Per-file changelog tables embedded in individual documents are secondary convenience only; this log is canonical. Entries are append-only — never edit or delete a past entry; if a decision is later reversed, add a new entry that supersedes it and cite the superseded ID.

Dated entries are kept contiguous and appended in write order at the top. This is "chronological" in the sense that later entries are written after earlier ones — it is **not** a guarantee that file position tracks strict event-date order. An entry's `date` field records when the underlying event or work occurred, and can occasionally trail the date of the entry immediately before it, if that prior entry's write-up was itself delayed relative to when its event happened (e.g., H-038's entry, dated 2026-07-09, sits ahead of H-039's, dated 2026-07-06, because H-039's event was written up after H-038's). Rely on append/entry-ID order, not on scanning dates top-to-bottom, when reasoning about sequence. Undated entries follow, grouped by type; the foundational architecture/business decisions carried over from the pre-migration synthesis without an individually recorded date (D1–D10 and the cross-reference-check decision) are grouped in their own section at the end of the file, per `CLAUDE.md`'s history-logging convention.

---

### Dated entries

**H-001 · 2026-06-17 · MILESTONE**
The four core strategic artifacts — One-Year Strategic Plan, Operational Toolkit, Execution Timeline, and the (prior) Financial Model workbook — were reconciled to one another, closing the original nine-domain strategic analysis into a consistent set.
*Basis:* `CONTEXT.md` Section 2.

**H-002 · 2026-06 (late June 2026, exact date not recorded) · MILESTONE**
The weekday crew-lead hire (Konji) moved from "conversation underway" to fully finalized terms, ahead of the Execution Timeline's ~July 7, 2026 tripwire.
*Basis:* `CONTEXT.md` Section 2; terms detailed in D4.

**H-003 · 2026-07-01 · DATA**
Starting-cash anchor corrected from a $1,500 planning guess to $1,400, a confirmed operating-account bank reading as of 2026-07-01. Tagged interim ESTIMATE, not a firm anchor — superseded once Relay bank-statement consolidation (Follow-Up #9) establishes the full cash picture.
*Basis:* `CONTEXT.md` Section 3, Section 6 item D.

**H-004 · 2026-07-01 · DATA**
The stale Jan–Jun revenue validation figure (`Assumptions!B43 = $9,562.71`, captured 2026-06-17) was superseded by current exports through 2026-07-01 showing $12,726.01 billed / $10,973.57 collected, reconciled across the P&L, revenue-by-customer, and sales-tax reports. Carried gross-of-surcharge pending Follow-Up #10.
*Basis:* `CONTEXT.md` Section 3, Section 6 item F.

**H-005 · 2026-07-04 · DATA**
Fixed-overhead contract figures (insurance, workers' comp, commercial auto, payroll provider, CRM subscription) were sourced and recorded — owner-supplied. Added to `reference/` so overhead figures trace and can be reconciled per D8. The equipment-maintenance line remains BLOCKED pending a stated allocation base and method.
*Basis:* Owner-supplied directly in the 2026-07-04 working session, not derived from `CONTEXT.md` — `CONTEXT.md` predates this sourcing and only records the task as pending (Section 6 item I).

**H-006 · 2026-07-05 · MILESTONE**
Baseline import committed (`2fc197b`, "baseline import — unedited source files in repo homes"): 100 files renamed/relocated into their repo homes with zero content changes — 85 Relay bank-statement CSVs plus 15 other files (6 CRM CSV exports, the P&L PDF, the source `.xlsx` workbook, `fixed-overhead.md`, and the 4 strategy/check-in Markdown docs, plus `CONTEXT.md` and `RUNBOOK.md` at root). The `incoming/` staging folder was removed once empty.
*Basis:* `CONTEXT.md` Section 10 step 2; commit `2fc197b`.

**H-021 · 2026-07-05 · REVISION**
Reconciled `strategy/strategic-plan.md` to current ground truth: Konji's crew-lead compensation replaced the season-completion-bonus placeholder with his finalized terms ($25/hr base, 6% revenue share Sep '26–May '27, canvassing and winter project rates, phase-switch to a generic $25/hr line after May 2027), and the "weekday crew lead secured" checkbox was checked; the Xavier equipment-plan figure and Q1 milestone were corrected to ~$1,800, unpaid, demand-triggered in late winter (from the underspecified "~March" phrasing and the $1.5K equipment-plan placeholder); starting cash was corrected from the $1,500 planning guess to $1,400, tagged interim ESTIMATE pending Relay consolidation; the peak-revenue anchor was corrected from the normalized $4,500 to the actual May 2026 figure of $4,908, with the plan objective restated as ~$10,000 (from ~$9,000) and the quarterly target tables flagged as still derived from the superseded $4,500-anchor model, pending regeneration in the Phase 7 rebuild; and fixed monthly costs were updated from the ~$695 rough estimate to the sourced $696.58 itemized total (excluding equipment-maintenance, still BLOCKED).
*Basis:* `CONTEXT.md` Section 6 items C (crew-lead comp + checklist), D (Xavier terms + starting-cash anchor), and I (sourced overhead); the peak-anchor/objective correction follows the same actual-May-2026-figure logic as the Section 3 objective statement. *Closes:* Section 6 item C in full; the strategic-plan-facing portions of items D and I (the Assumptions Log rows); the plan-side half of the peak-anchor correction (the model-side regeneration remains open, pending Phase 7).

**H-022 · 2026-07-05 · REVISION**
Added an auto-insurance / Konji-insured-driver decision point to `strategy/execution-timeline.md`'s Oct 1–15 section, framing the before-vs-wait timing decision against the current 6-month term end (Oct 9, 2026).
*Basis:* `CONTEXT.md` Section 6 item J. *Closes:* the timeline-facing portion of item J (adding the dated decision point); the premium-increase figure itself remains BLOCKED pending an insurer quote.

**H-023 · 2026-07-05 · REVISION**
Reconciled the "Initiate now" checklist in `strategy/operational-toolkit.md`: the single stale item ("open the crew-lead conversation with Konji... begin sourcing a backup") was split into two, each carrying its true current status — the Konji conversation marked resolved (finalized terms, late June 2026), and backup weekday-lead sourcing marked active — consistent with the corrections already made to the execution timeline and strategic plan.
*Basis:* `CONTEXT.md` Section 6 item B (the same Konji-status reconciliation applied there).

**H-024 · 2026-07-05 · DATA**
The Jan–Jun 2026 revenue anchor was superseded: the stale model figure `Assumptions!B43` is replaced by the current exports, to be used when the model is rebuilt. The billed anchor and Konji's 6% base are carried gross-of-surcharge pending the tax session (#10).
*Basis:* `CONTEXT.md` Section 6 item F (the full instruction, figures, and surcharge caveat live there — this entry records the decision, not a duplicate of the numbers). *Status:* build-time instruction, open until the Phase 7 rebuild applies it and #10 resolves the surcharge composition.

**H-025 · 2026-07-05 · DATA**
The $5,000 "New Equipment" CRM entry (April 13) is recorded as a retroactive log of an already-owned asset (the existing F-150), not a new capital outflow — it must not be modeled as a future cash event in the rebuild.
*Basis:* `CONTEXT.md` Section 6 item H. *Status:* build-time instruction for Phase 7.

**H-026 · 2026-07-05 · DATA**
Relay account map confirmed (owner ground truth): 8735 Income, 3549 OpEx, 3550 Reserve, 8737 Taxes, 8736 Owner's Comp. The three non-operating accounts (3550, 8737, and 8736) verified $0 across all 17 months of statements; only 3549 (200 txns) and 8735 (49 txns) are active. Historical cash = Income + OpEx.
*Basis:* Owner confirmation + direct file check of `reference/` Relay CSVs.

**H-027 · 2026-07-05 · TOOLING**
Relay restructuring executed: Income→OpEx instant sweep active (Income max-balance $0 → 100% to OpEx); Owner's Comp (8736) deleted; Profit account renamed → Reserve (3550).
*Basis:* Owner actions in the Relay app, this session.

**H-028 · 2026-07-05 · DECISION**
Automated OpEx→Reserve ceiling deferred. Relay forbids an account being both source and destination in instant rules; near-zero value now (cash far below any ceiling); correct percentage-allocation design blocked on the model + Follow-Up #10. Ceiling value set at ~$8,000 for eventual use (basis: peak-season OpEx outflow $4,656.77 (May 2026 actual peak, from the header-correct 3549 parse) + $4,000 operating floor; revisit as revenue grows). Reserve funded manually in the interim. Post-model endpoint: a Profit First rule on Income (verified %s to OpEx/Reserve/Taxes), which preserves Income as a clean gross-revenue measurement point.
*Basis:* Design discussion, this session. *Status:* open, pending model + #10.

**H-029 · 2026-07-05 · DECISION**
Collapsing Income+OpEx into one account considered and deferred until the model exists; both accounts retained (Income's separation preserves a clean gross-revenue signal). Taxes (8737) allocation % remains BLOCKED pending #10.
*Basis:* Design discussion, this session.

**H-030 · 2026-07-06 · DATA**
Revenue ledger extracted from 68 source invoices and validated: line-item gross ties to invoice gross exactly ($27,891.65); Jan–Jul 2026 window ties to the Homeworks billed anchor to the penny ($12,726.01). Two parse defects found and corrected en route: a payment-stub-only page misread as a 69th invoice, and long descriptions wrapping the amount onto the wrong line (which had hidden $5,070.93).
*Basis:* Direct parse + three independent reconciliation checks against Homeworks exports.

**H-031 · 2026-07-06 · DATA**
Surcharge treatment resolved empirically. The ~6% is charged on top of each line's net price, but Homeworks' reported "billed revenue" is the gross figure — so the surcharge is embedded within the $12,726.01, not additional to it. Net service revenue Jan–Jul 2026 = $12,024.58; surcharge = $701.43. Note: this corrects an earlier statement in-session that the surcharge sat "on top of" the $12,726 anchor; the owner's understanding was correct and the earlier claim was wrong.
*Basis:* Per-invoice arithmetic across 68 invoices, cross-checked against the salestax export.

**H-032 · 2026-07-06 · DECISION**
Past revenue is tracked gross. Konji's 6% revenue share is computed on net service revenue (excluding the surcharge). The surcharge's structural treatment (retained revenue vs. pass-through liability) remains an open future decision (Follow-Up #10); if it changes, revisit this.
*Basis:* Owner decision, this session.

**H-033 · 2026-07-06 · MILESTONE**
Gate B (revenue mapping / flat export) substantially resolved; no longer blocks the model build. Residual: 40.7% bundled-line revenue is not service-resolvable historically (see #6, #13).
*Basis:* `reference/revenue-line-items.csv` and `reference/revenue-invoices.csv`, validated per H-030.

**H-034 · 2026-07-06 · REVISION**
Cross-reference reconciliation: Sections 6 (item E), 9 (revenue bullet), and 10 (step 5) of `CONTEXT.md` updated to reflect Gate B's resolution, so the document no longer describes Follow-Up #6 as a live blocker.
*Basis:* The CLAUDE.md cross-reference check, applied to the H-030–H-033 edits; the join drift was detected before commit. Note: this is the first autonomous catch by that rule since its adoption.

**H-035 · 2026-07-06 · TOOLING**
Revenue-ledger update standardized: `model/parse_invoices.py` with a fail-closed reconciliation gate (sum(line_gross) == sum(invoice gross); per-invoice tie-out; gross == net_subtotal + surcharge; no duplicate invoice numbers; optional `--expect-window` check against the Homeworks billed anchor); `reference/REVENUE-UPDATE.md` procedure; full-re-export-not-delta rationale (Homeworks records are mutable, so deltas would miss edits to prior invoices). The two revenue CSVs relocated from `reference/` to `model/data/` — they are script-regenerated derivatives, not immutable sources; `reference/` is now purely immutable raw sources. This supersedes the file paths cited in H-030 and H-033, which remain unedited and true as of their date.
*Basis:* Design discussion; script validated by byte-identical reproduction of the H-030 ledger's invoice-level file (`revenue-invoices.csv`).

**H-036 · 2026-07-06 · DATA**
Service classification corrected to use the invoice's literal service header. The prior ledger let free-text description lines override the header — an implementation artifact of the original parse, not a deliberate rule — which inconsistently reclassified some bundled lines into specific services (e.g., "General Maintenance" → "Mowing" when the description happened to start with a recognizable keyword, but not when it didn't) and understated the bundled-line share. Dollar amounts, dates, customers, and all invoice-level figures are unchanged; H-030's validation stands. 9 line items across 6 invoices changed classification (8 reclassified to "General Maintenance"/`is_bundle=TRUE`; 1 restored to its full literal header "Edging/Weed-whacking"). New bundled-line share: **56.6%** ($15,795.85 of $27,891.65), up from the prior 40.7% ($11,339.40). Supersedes the 40.7% figure cited in H-033 and in `CONTEXT.md` Follow-Up #6 / Section 9.
*Basis:* Owner review of the original parse logic; `model/parse_invoices.py` never scans description text for service names, only the invoice's literal service-header field.

**H-037 · 2026-07-06 · DATA**
Legacy service names mapped to the current catalog via `model/data/service-name-map.csv` (13 renames, owner-confirmed). Extraction verified the 27 distinct invoice headers are complete — the 48 headerless lines are description fragments, not service names. `service_raw` preserved alongside canonical `service`; a fail-closed gate now rejects unrecognized headers. No dollar amounts, dates, or customers changed. The three bundle labels (General Maintenance, Lawn Care, Lawn Maintenance) are not renames and remain unmapped; bundled share stays 56.6%.
*Basis:* Header extraction cross-checked against the 22-item Homeworks catalog; owner confirmed each mapping.

**H-038 · 2026-07-09 · DATA**
The current 22-item Homeworks catalog and its two Package definitions captured as dated reference snapshots (`reference/service-catalog-2026-07-09.csv`, verbatim from the Homeworks export including its trailing `null` row; `reference/service-packages-2026-07-09.csv`, transcribed from an owner screenshot). A currency mechanism was established so future catalog changes never require a code edit: an explicit "active snapshot" pointer line in `reference/README.md` (not glob-latest, since unlike invoices a newer catalog snapshot is not guaranteed to be a superset of an older one — an item could be renamed or discontinued), with the refresh procedure documented in `reference/CATALOG-UPDATE.md`. `model/parse_invoices.py` now reads valid catalog names from the pointed-to snapshot instead of the hardcoded `ALREADY_CANONICAL_SERVICES` set introduced in H-037. An item-vs-service axis was added via `model/data/catalog-type-map.csv` (`Bagged Mulch`, `Landscaping Blocks (stone)`, `New Plants` tagged `item`; the remaining 19 catalog rows tagged `service`) surfaced as a new `kind` column on every non-bundle line item in `model/data/revenue-line-items.csv`; bundle rows (`is_bundle=TRUE`) are left with `kind` blank, since a bundled visit mixes labor and possibly materials and can't be classified as one until Follow-Up #13 decomposes it. A new fail-closed gate rejects any mapped service name absent from `catalog-type-map.csv`, catching future catalog additions that aren't yet classified. Zero-revenue catalog items are now traceable to source rather than asserted: 7 of 22 have no billed line in the historical data — `Bagged Mulch`, `Landscaping Blocks (stone)`, `New Plants` (all 3 `item`-kind rows) and `Blowing`, `Material Pick-up`, `Seeding`, `Sod Placement` (4 of 19 `service`-kind rows). Homeworks' Packages feature (`General Maintenance`, `Lawn Care` package definitions) has never been used to bill an invoice — a distinct mechanism from the informal same-named bundle labels already in the ledger; the two must not be conflated (see `reference/CATALOG-UPDATE.md`). No dollar amounts, dates, or customers changed; `revenue-invoices.csv` remains byte-identical and `revenue-line-items.csv` changed only by the addition of the `kind` column.
*Basis:* Owner-supplied Homeworks "Items and Services" CSV export and "Packages" tab screenshot, both dated 2026-07-09; cross-checked programmatically against `model/data/service-name-map.csv` and `model/data/revenue-line-items.csv`.

**H-039 · 2026-07-06 · MILESTONE**
Gate A (bank consolidation) closed — the last pre-build gate. Four active-account Relay statements (3549, 8735, 3550, 8737) captured as dated reference sources (`reference/Relay (Partial) 2026-07-01 to 2026-07-06 #<NNNN>.csv`; the disambiguating end-date was added to the existing `Relay (Partial) YYYY-MM-DD #NNNN.csv` convention because the window-start label alone collides across multiple partial pulls within the same month — the pre-existing `2026-07-01` files are a distinct, earlier 7/3 pull and were not overwritten). Consolidated cash = **$1,225.33** as of 2026-07-06 (the latest transaction date found across the four files, not the `2026-07-01` filename label, which marks the window start) — independently computed from the statement files (`reference/cash-consolidated-2026-07-06.csv`), not asserted from the owner's figure alone: 3549 (OpEx) $1,225.33, 8735 (Income) $0.00 (instant-swept to OpEx per H-027), 3550 (Reserve) $0.00, 8737 (Taxes) $0.00. Reserve and Taxes confirmed still zero-activity — no statement for either account, across all 17+ prior months plus this window, has ever contained a transaction — consistent with H-026's "$0 across all 17 months" finding; their $0.00 status here is continuity, not a newly observed balance. This supersedes the $1,400 single-account interim estimate (H-003) as the cash anchor; full history ($1,500 planning guess → $1,400 interim → $1,225.33 verified) preserved in `strategy/strategic-plan.md` Section 9's Revision Note. Phase 7 (the model build) is now unblocked — both pre-build gates (B per H-033, A per this entry) are cleared.
*Basis:* Four owner-supplied Relay CSV exports dated 2026-07-09 (covering activity through 2026-07-06), independently reconciled: per-account ending balances and their sum computed directly from the files, matching the owner-stated $1,225.33 total exactly. One immaterial discrepancy noted: the 3549 file contains 11 transaction rows, not the 12 initially described — the dollar figure is unaffected.

**H-040 · 2026-07-09 · REVISION**
`RUNBOOK.md` checkboxes synced against actual repo state for the first time since authoring: Phases 0–6 reviewed item by item and either checked with an inline citation (a `HISTORY.md` entry ID, an existing file, or a commit hash) or left unchecked with a note explaining why — either "unverifiable from repo contents" for live account/environment facts with no file trace (GitHub sign-in, VS Code Desktop, Claude Code auth, tool-version checks, extensions), or "not checked on inference alone" where the only support was reasoning backward from later work rather than a citable artifact. Several boxes were checked with a parenthetical noting the actual resolution differs from the box's original wording (Gate B's "~16-item catalog" superseded by H-038's sourced 22-item catalog; Phase 5 item D's "$1,400 interim ESTIMATE" superseded by H-039's verified $1,225.33), matching the pattern already used for the Gate A checkbox. Phase 7 confirmed entirely unstarted (`model/build_model.py` does not exist) and left unchecked throughout; Phase 8's follow-up-queue box checked (Section 7 demonstrably live and actively status-tracked) while its check-in-cadence box was left unchecked (no dated check-in entries exist yet). Separately, `reference/REVENUE-UPDATE.md`'s KPI section was updated to reflect H-038: the new `kind` (item/service) column, the fail-closed gate against `model/data/catalog-type-map.csv`, and the fact that valid catalog names are now read from the active snapshot pointed to in `reference/README.md` rather than a hardcoded list — making a catalog-currency check a prerequisite step in the update procedure, not just the invoice re-export. No dollar amounts, dates, or customers changed — documentation-only pass.
*Basis:* Direct inspection of the current repo tree, `git log`, and `HISTORY.md` entries H-001 through H-039; citations given inline in `RUNBOOK.md` rather than restated here.

**H-041 · 2026-07-09 · REVISION**
Fixed `model/PHASE7-PLAN.md`'s internal cross-reference numbering — body text throughout referenced "Section 5 decision N," a leftover from an earlier draft structure, while the document's own heading is "Section 4"; all 10 instances corrected. Added the owner's (Cyrus's) $3,800 truck-debt repayment as its own tracked line, parallel to Xavier's ~$1,800: extended `CONTEXT.md` Section 6 item D's prose (previously Xavier-only) and added a new row to `strategy/strategic-plan.md` Section 9's Financial Assumptions Log (which, on inspection, did not yet carry a Xavier line either — the truck-debt row was added on its own terms, citing the existing Cash Buffer Policy and Equipment Investment Plan language rather than an existing Xavier row). Both obligations are patient, interest-free, demand-triggered, and treated as equity for cash-buffer purposes on the same basis; the owner's repayment is sequenced strictly after Xavier's, both gated behind the $6,000 buffer being intact — `model/PHASE7-PLAN.md` Section 3(c) and decision 5 updated to state that the projection layer must encode this sequencing even though neither obligation gets a ledger row yet. One premise in the requested change did not hold: `strategy/strategic-plan.md`'s Equipment Investment Plan table (Phase 3 row) was checked directly and already read "~$1.8K / $3.8K" with correct Xavier-then-truck-debt sequencing — no stale "$1.5K" figure was found there or anywhere else in the repo describing this obligation (the only "$1,500" in the repo is the unrelated starting-cash-anchor history); no edit was made to that row, and no Revision Note entry was added for a correction that wasn't needed. Documented the 2026-07-07 Stripe routing cutover (card deposits to OpEx before that date, to Income from that date on) as a required dated parameter for the payment-matching design, replacing the single-era assumption in the original Finding 3. Added a proposed (not yet built) `type=overhead, subcategory=payment-processing-fee` ledger category: Stripe's fee (3.9% + $0.30/charge, owner-confirmed) is distinct from both the 6% surcharge (Follow-Up #10) and the billed-vs-collected gap (Follow-Up #15) and must not be conflated with either. The Homeworks-direct-card-payment fee schedule has not been supplied and was not estimated — added as new Follow-Up #17 in `CONTEXT.md`, with the fee-netting design's Homeworks-direct portion marked partially BLOCKED on it (the Stripe-fee portion is unblocked and can proceed once payment-event matching begins). Created the five skeleton ledger files per Sequencing Step 1 — `model/data/ledger-revenue.csv`, `ledger-labor.csv`, `ledger-overhead.csv`, `ledger-materials.csv`, `ledger-capital.csv` — each with the header row only (`date,type,event,category,subcategory,customer,quantity,unit_rate,amount,status,source`) and no data rows. No dollar amounts populated in any ledger row — schema only; `build_model.py` not yet written.
*Basis:* `model/PHASE7-PLAN.md` (this pass); `CONTEXT.md` Section 6 item D and Section 7; `strategy/strategic-plan.md` Section 9 and Section 6 Equipment Investment Plan; `strategy/execution-timeline.md` Cash Buffer Policy; owner-confirmed Stripe fee formula and routing-cutover date, 2026-07-09.

**H-042 · 2026-07-09 · REVISION**
Corrected seven findings from a repo-wide cross-document reconciliation audit (Part 8), each a restated/derived-text error, not a source-data change. `strategy/strategic-plan.md`'s Executive Summary carried a stale "~$800 currently... against ~$5,300 in deferred, interest-free obligations" sentence, untouched by any prior revision pass; updated to the verified $1,225.33 cash figure (as of 2026-07-06, H-039) and ~$5,600 in obligations (Xavier's ~$1,800 per H-021 plus the owner's $3,800 truck-debt repayment per H-041 — the stale $5,300 total traced to Xavier's superseded ~$1,500 estimate). Treated as a plain correction, not a new Revision Note row: this sentence was never itself a tracked Assumptions Log/Revision Note line, unlike the starting-cash anchor it sits near, so no moving-baseline table applies to it. `RUNBOOK.md` Phase 5 item F's "gross-of-surcharge" description of Konji's 6% base was superseded the very next day by H-031/H-032 (net-of-surcharge, $12,024.58 basis) but the box's main text was never corrected to say so, only cited in a parenthetical that didn't state the reversal — added an explicit inline correction citing H-031/H-032 directly, given the payroll-calculation stakes. `model/PHASE7-PLAN.md`'s Section 2 inventory table miscounted the Relay bank-statement files as "44"; recounted fresh (`ls reference/Relay*.csv | wc -l`) at 89 and corrected. `RUNBOOK.md` Phase 7's first checkbox note claimed "no unified atomic ledger with this schema exists yet," stale since H-041 created the five schema-only skeleton files; corrected to describe their existence while leaving the checkbox itself unchecked (the ledger remains unpopulated). `strategy/execution-timeline.md`'s Demand-Triggered Watch List gained a row for the owner's $3,800 truck-debt repayment, parallel to the existing Xavier row, per H-041's sequencing (Xavier first, both behind the $6,000 buffer); the Tripwire Register was left unchanged after checking it directly — it has no existing Xavier row to parallel, so no truck-debt row was fabricated there. `strategy/strategic-plan.md` Section 9's Financial Assumptions Log gained a Xavier row (previously absent, as H-041 itself had flagged) alongside the truck-debt row it sits next to, so the "parallel to Xavier" framing is now a real adjacent pair rather than an assertion with no Xavier row to be parallel to. `HISTORY.md`'s own opening description ("kept contiguous and chronological") was clarified, not reordered: added an explicit note that file position reflects append/write order, not strict event-date order, since an entry's `date` field can trail the entry before it when that prior entry's write-up was delayed (as with H-038 sitting ahead of the earlier-dated H-039) — H-038/H-039 were deliberately left in their existing append order. No dollar amounts changed in any ledger or source file; every correction here is to restated or derived text.
*Basis:* Part 8 audit findings (this session, 2026-07-09); `HISTORY.md` H-021, H-031, H-032, H-039, H-041 for the underlying figures corrected against; direct recount of `reference/Relay*.csv` and direct inspection of `strategy/execution-timeline.md`'s Tripwire Register before declining to edit it.

**H-043 · 2026-07-09 · REVISION**
Corrected Follow-Up #16 in `CONTEXT.md`: it stated "two 'Bagging' lines were charged 0%," but a precise recount of `model/data/revenue-line-items.csv` (undertaken to reconcile three disagreeing counts surfaced during Sequencing Step 2's mapping design — "4" for all zero-surcharge rows, "3" for non-bundle zero-surcharge rows, "2" in the existing Follow-Up #16 text) found the true count is **3 Bagging line items across 2 invoices** charged 0% (invoice 8's single line; invoice 38's two lines), against 3 other Bagging lines (invoices 55, 58, 59) correctly charged the normal 6% — Follow-Up #16's "two" undercounted by one line item, ambiguous between a line-count and invoice-count reading. Also documented a second, previously unrecorded instance of the same pattern found during the same recount: invoice 75's "Lawn Maintenance" bundle line was also charged 0% surcharge, though bundle lines otherwise carry the normal rate — a bundle-label instance of the identical inconsistency Follow-Up #16 already describes for a specific catalog service. Dollar totals remain unaffected by this correction, exactly as #16 already stated: the surcharge is stored per-line as actually charged, not recomputed, so correcting the count changes only the documentation, not any ledger or source figure. No historical invoice records were altered.
*Basis:* Direct recount of `model/data/revenue-line-items.csv`, filtering on `line_surcharge == 0` (4 rows total) and cross-referencing `service_raw` values, this session, 2026-07-09.

**H-044 · 2026-07-09 · DATA**
Built and ran the payment-event matcher (`model/match_payments.py`, Sequencing Step 3) against the full Relay history (89 files) and populated 58 `type=revenue, event=payment` rows into `model/data/ledger-revenue.csv` (sum $30,023.25) plus 32 paired `type=overhead, category=Stripe, subcategory=payment-processing-fee` rows into `model/data/ledger-overhead.csv` (sum $409.96), per Approach A's tiered design (`model/PHASE7-PLAN.md` Section 3(b)). The classification gate passed exactly: all 94 settled `Receive`/`Receive-transfer` rows (deduplicated) resolved to either a payment event or a named exclusion, zero residual. Full-history extension revised or corrected several things a partial sample had implied: **deduplication** — 9 transactions were exact duplicates across the overlapping `Relay (Partial) 2026-07-01` and `... 2026-07-01 to 2026-07-06` export windows (both accounts 3549 and 8735); confirmed via exhaustive tuple-matching across all 264 raw rows that no other overlapping-window duplicates exist anywhere in the 89 files. **Batching** — Finding 2's claim of "no case of batching... in the sample" does not hold across the full history: a 2025-08-27 Women Life Freedom LLC deposit ($1,228.75) exactly sums invoices #11 and #16, tagged `"Invoices 11, 16 [batch]"`. **Broader exclusion list** — all 36 excluded rows ($20,907.84) break down as the two categories Finding 4 already documented, at their correct deduplicated counts — internal account transfers (`Income`, the 8735→3549 sweep, **20** rows/$19,266.85 — not 21; one instance was itself one of the 9 duplicate rows removed above) and `USAA FSB TRNSFER` (1 row/$205.00) — plus four categories tier-4 checking against the full history (not just the partial-July sample) surfaced that Finding 4 never documented: owner cash injections (`USAA CLASSIC CHECKING` "ACH Pull," 4 rows/$391.00), payroll-tax-related refunds/credits (`GUSTO PAYROLL` "TAX...", `GUSTO` "INV 264893", `United States Treasury` "F-941...": 5 rows/$368.47 combined), bank account-verification micro-deposits (`ACCTVERIFY`, 4 rows/$1.04), and vendor/corporate-card refunds (`Business File`, `The Home Depot`, 2 rows/$675.48). (20+1+4+5+4+2 = 36, reconciling exactly against the classification gate; an earlier verbal summary of this work miscounted the internal-transfer category as 21/$19,471.85, before fully applying the corrected dedup direction — the underlying script and ledger output were never affected, only that spoken tally.) Every unmatched or investigation-needed item is tagged explicitly in the ledger, not buried: two Women Life Freedom LLC deposits ($932.67 on 2025-04-08, $922.00 on 2025-05-14) predate the invoice PDF's coverage window entirely (`"unmatched — predates invoice PDF coverage window [unmatched]"`); one ($265.33, 2025-06-11) falls inside the window but matches no invoice or invoice-combination (`"unmatched — needs investigation [unmatched]"`); Martha Picarello's and Joel Beauvais's deposits are $0.75 and $0.05 short of their closest invoice respectively and were deliberately **not** fuzzy-matched (`"unmatched — $0.75 short of invoice #2, gap unexplained [unmatched]"` and the equivalent for Joel/#1). A single Stripe `Spend`-type debit (–$38.16, 2026-04-08) was found and excluded from revenue events entirely, not netted into any tier — added as new Follow-Up #18 in `CONTEXT.md` (plausibly a chargeback/reversal, not asserted as fact; needs owner investigation). Corrected `model/PHASE7-PLAN.md`'s proposed-category paragraph: an earlier draft described the paired Stripe-fee row as pairing a *back-computed gross* with the fee, which would have made the payment event's own `amount` overstate real bank cash and broken D5a's bank-reconciliation requirement — that was never implemented; the payment event's `amount` is always the actual net deposit, and the gross is back-computed only to size the separate overhead fee row (`gross = (net + $0.30) / (1 - 0.039)`). The fuller per-account balance-delta reconciliation gate (matching `reference/cash-consolidated-2026-07-06.csv`) was **not** run — it requires Sequencing Step 4's labor/overhead/materials/capital population to exist first, since it must account for Spend-side ledger events this pass doesn't populate.
*Basis:* `model/match_payments.py` (this pass, new file); direct parse of all 89 `reference/Relay*.csv` files; `model/data/revenue-invoices.csv`; `model/PHASE7-PLAN.md` Section 3(b) Findings 1–7 and Approach A; `CONTEXT.md` D5a; owner-confirmed decisions 1–8 on match-confidence tagging, fee-netting correction, deduplication rule, and the specific unmatched/near-miss/exclusion tags, 2026-07-09.

**H-045 · 2026-07-09 · REVISION**
This session's retroactive HISTORY.md arithmetic audit (per the new CLAUDE.md self-check) flagged H-028's stated OpEx→Reserve automation ceiling ("~$8,000") against its own cited basis: $4,656.77 (May 2026 actual OpEx peak) + $4,000.00 (operating floor) = **$8,656.77** — a ~$657 gap, larger than ordinary rounding. The owner has now confirmed this was always a deliberate round-down to a working figure, not a drifted or miscomputed value. **No correction to H-028 is made or needed; H-028 itself remains untouched, per the append-only rule.** Recorded here so a future reader hitting the same $8,656.77-vs-"~$8,000" gap doesn't re-flag it as an unresolved error. Separately checked whether the OpEx maximum-balance-threshold automation H-028 describes is tracked anywhere else in the repo as its own deferred item: it is not — `CONTEXT.md`'s Follow-Up list (Section 7) has no separate numbered entry for it; the only tracked status is H-028's own "Status: open, pending model + #10," tying it to the Follow-Up #10 surcharge-structure decision and the Phase 7 model build, not to a distinct follow-up of its own. When that automation session happens, **$8,656.77** is the traceable, addend-derived figure to start from; "~$8,000" is the owner's intentional rounded working number, not a competing or superseding value.
*Basis:* `HISTORY.md` H-028 (checked directly, not edited); owner confirmation, 2026-07-09; `CONTEXT.md` Section 7 checked directly for a separate Follow-Up entry (none found). Arithmetic self-check applied to this entry's own stated sum before staging: $4,656.77 + $4,000.00 = $8,656.77, confirmed by direct addition.

**H-046 · 2026-07-10 · DATA**
Populated Sequencing Step 4 (`model/populate_step4.py`, new file): labor, overhead, materials, and capital ledger events, per the confirmed design. **Labor** (`model/data/ledger-labor.csv`): 66 rows — 33 two-week periods, 2025-04-08 to 2026-07-06 (matching the ledger's overall real-data range), two rows per period (10 person-hours crew-lead × $25.00 = $250.00; 10 person-hours crew-member × $20.00 = $200.00, never pre-blended, per D2), all tagged ESTIMATE. Total: 33 × ($250.00 + $200.00) = **$14,850.00** — a sanity-check figure only, not gated against anything external. **Overhead** (`model/data/ledger-overhead.csv`): 47 new rows added on top of the 32 Stripe fee rows from H-044 (79 total) — Gusto's FEE line, **17 rows** (May 2025–Jul 2026, real varying amounts: $158×3 + $58 + $94 + $100×2 + $109×5 + $115×4 + $127 = **$1,958.00**, verified by direct addition against the 17 rows in the file — only the latest, 2026-07-02, matches the $127/mo figure `fixed-overhead.md` currently states); General Liability via payee "Simply Business," **15 rows** (Apr 2025–Jul 2026: $26.25×5, one $37.50, then $33.75×9 from Nov 2025 on — the current $33.75 rate does not hold historically); Workers' Comp, one lump-sum row, $544.00 on 2025-12-09 (payee "Employers Insura," an exact match — this is a real, sourced cash date `fixed-overhead.md` itself doesn't state, since that file only gives the *next* renewal, 2026-11-30); the merged Copilot→Homeworks CRM line, 13 rows (12 months at $139.00/mo, Jun 2025–May 2026, payee "Copilot" — owner-confirmed as Homeworks under a prior name — plus one row at the new $299.00 Growth-plan rate). **Correction to this session's own prior scoping-pass narration:** that pass stated "16 and 14 months respectively" for the Gusto and General Liability counts — re-verified directly against the parsed Relay data for this population pass, the correct counts are **17 and 15** (May 2025 alone had three separate Gusto FEE charges in quick succession, $158 each, on the 5th/7th/15th, which the earlier narrative undercounted). **Billing-anchor discrepancy, flagged rather than silently written past:** the framing that "the first Homeworks-rate debit landed 2026-07-02" does not match the data — the $299.00 charge is dated **2026-06-25**, still on Copilot's original ~25th-of-month cadence; the 2026-07-02 charge is a separate, smaller ($77.16) Homeworks charge that remains unexplained and is **not** included in this merged line. Squarespace: one row, $25.00 on 2026-07-02 (exact match); earlier months are an acknowledged gap, not backfilled. Commercial Auto and equipment maintenance remain **BLOCKED, no ledger row** — no confirmed business-side cash-out date exists for Commercial Auto (owner confirms historical payments were personal-card; future company-card payments will be bookable once they occur in Relay), and equipment maintenance has no stated allocation basis, per Section 6 item I. **Materials** (`model/data/ledger-materials.csv`): one BLOCKED $0.00 placeholder row, dated to the ledger's overall start (2025-04-08), per D1; the $422.26 Home Depot order and five other Home Depot charges found in Relay ($295.74, $591.48, $21.41, $107.59, $106.90, $315.36) remain excluded, pending Follow-Up #7. **Capital** (`model/data/ledger-capital.csv`): two ACTUAL rows. (1) Chainsaw equipment, $408.00, 2025-08-17, payee "USAA CLASSIC CHECKING," ref "Chainsaw Equipment From Amazon." (2) Owner reimbursement, truck-and-insurance advance — **searched the full Relay history for the two described transactions (~$1,000 insurance-related, ~$1,200 truck-related) and found neither as separate transactions.** Found instead **one** transaction, $2,200.00 on 2026-04-09 (same payee, ref "ACH Push - Sent By Cyrus Beauvais"), matching the *combined* total exactly — booked as a single combined row, explicitly flagged in its own source field as one transaction rather than two, since the data does not support a two-transaction split. **Arithmetic reconciliation, confirmed and shown, not just asserted:** $5,000.00 (truck, H-025) + $1,000.00 (insurance, rounded from the $999.00 Commercial Auto premium) = **$6,000.00** total advanced by the owner; $1,000.00 + $1,200.00 = **$2,200.00** already repaid (the single transaction found above); $6,000.00 − $2,200.00 = **$3,800.00** remaining — matches the currently-tracked truck-debt figure (Section 6 item D, `strategy/strategic-plan.md` Section 9) exactly. Xavier's payout, the owner's remaining $3,800 truck-debt balance, and the H-025 truck bookkeeping entry remain untouched — no ledger rows, per the existing projection-layer-only / excluded-entirely decisions (`model/PHASE7-PLAN.md` Section 3(c)). Electric blower: confirmed not bookable — the latest Relay data available is 2026-07-06, before the Aug–Sep 2026 target window. **Reconciliation gates run:** the originally-designed "sum to $696.58/month" overhead gate does not apply as stated, and this is a design finding, not an oversight — two of the six lines (Workers' Comp, Commercial Auto) are lump-sum-dated cash events, not monthly debits, per `fixed-overhead.md`'s own accrual-vs-cash distinction, so no calendar month's ACTUAL rows can sum to the full $696.58 monthly-equivalent by construction. The four genuinely-monthly lines' current rates sum to $127.00 + $33.75 + $299.00 + $25.00 = **$484.75**, but Gusto/General Liability/Squarespace's latest confirmed rows fall in July 2026 while Homeworks' falls in June 2026 — no single month currently has all four at their current rate simultaneously, so this figure is reported as the current-state total, not a passed/failed single-month check.
*Basis:* `model/populate_step4.py` (this pass, new file); direct parse of all 89 `reference/Relay*.csv` files (via `model/match_payments.py`'s parser); `reference/fixed-overhead.md` (re-read fresh); `CONTEXT.md` D1, D2; `model/PHASE7-PLAN.md` Section 3(c); owner confirmation on the Copilot/Homeworks vendor identity and the Commercial Auto personal-card history, 2026-07-10.

**H-047 · 2026-07-10 · TOOLING**
Wrote and ran `model/build_model.py` (new file), generating `model/financial-model.xlsx` (gitignored, per D7) with seven sheets: Monthly P&L, Plan vs Actual, Quarterly Rollups, Revenue by Service, Revenue by Customer, Reconciliation, Raw Ledger. **Resolved the open July-labor-gap question first:** checked every one of the 33 two-week labor periods across the full 2025-04-08–2026-06-30 range, not just July — 14 of 33 periods straddle a calendar-month boundary (an ordinary consequence of 14-day periods not aligning to ~30-day months), and every month from 2025-04 through 2026-06 still receives either 2 or 3 period-starts ($900.00 or $1,350.00) under period-start attribution — none reduced or zeroed. **July 2026 is the only affected month, confirmed as an end-of-data-range artifact, not a recurring pattern:** the last period starts 2026-06-30 (correctly counted in June); the next would start 2026-07-14, past the ledger's current 2026-07-06 boundary. No proportional split was implemented; the Monthly P&L sheet instead carries a one-line note on July's row explaining the $0 labor figure. **Monthly P&L** shows Billed and Collected side by side, never merged (D5a), with every category split into `_actual`/`_estimate` columns and Materials shown as an explicit "BLOCKED" state (with a distinguishing note for its one real placeholder row vs. every other month's true absence) rather than a bare $0. **Blended labor rate** is computed at build time as `sum(amount)/sum(quantity)` per period — confirmed constant at $22.50/hr throughout (rates never changed), never a stored constant in the script. **Plan vs Actual** is built as an explicit BLOCKED stub, not populated: no plan/assumptions CSV exists yet in `model/data/`, and `strategy/strategic-plan.md`'s quarterly tables are prose, not a data-layer file D6 would allow the projection layer to reference — hardcoding them into the script was rejected rather than done silently. **Revenue by Service/Customer** built only from `event=invoice` rows, since payment rows carry match-confidence tags in `subcategory`, not service classification. **Reconciliation tab**, scoped per this session's Sequencing Step 4 verification pass, ran four checks, all passing: (1) revenue invoice total $28,316.57 vs. the $28,316.57 anchor, exact; (2) revenue payment classification restated by reference to H-044's already-proven gate (58 payment events + 36 exclusions = 94, zero residual), not re-run; (3) overhead's 79 ACTUAL rows, summing to **$5,376.46** (verified: Stripe $409.96 + Gusto $1,958.00 + General Liability $472.50 + Workers' Comp $544.00 + Homeworks/Copilot $1,967.00 [12×$139.00 + $299.00] + Squarespace $25.00 = $5,376.46, matching the file exactly), each row individually sourced to a Relay transaction; (4) both capital rows ($408.00, $2,200.00) trace to specific named transactions. The tab states explicitly what it does **not** verify: labor's ESTIMATE hours (no time-tracking to check against), materials' BLOCKED placeholder, the still-BLOCKED Commercial Auto/equipment-maintenance lines, and — restated from the Sequencing Step 4 scoping pass — the full per-account balance-delta gate, which was **not** attempted because ~85% of real settled Spend-side Relay cash ($44,164.11 of $51,738.61) has no ledger row at all by design (real Gusto payroll `NET`/`TAX`, internal transfers, uncaptured small vendor spend); running it would fail predictably, not diagnostically. **Idempotency — a real bug found and fixed, not just asserted:** the first build attempt was *not* byte-identical across reruns; diffing two runs' unzipped contents isolated the difference to `docProps/core.xml`. Root cause: openpyxl's writer (`openpyxl/writer/excel.py`) unconditionally re-stamps the `modified` timestamp to `datetime.now()` inside `save()`, which cannot be overridden by setting `workbook.properties` beforehand. Fixed by post-processing the saved file — `normalize_xlsx_for_determinism()` rewrites `docProps/core.xml` with a fixed timestamp and rebuilds the zip with fixed per-entry metadata and a fixed entry order. Re-verified after the fix, with a real wall-clock gap between runs: both runs produced the identical MD5 checksum. `openpyxl==3.1.5` (already pinned in `requirements.txt`) was sufficient; no new dependency was needed.
*Basis:* `model/build_model.py` (this pass, new file); `model/data/ledger-{revenue,labor,overhead,materials,capital}.csv`; direct inspection of `openpyxl/writer/excel.py`'s source to find the idempotency root cause; direct diffing of two build runs' unzipped output; `CONTEXT.md` Section 9, D6, D7; `model/PHASE7-PLAN.md` Section 3(d).

---

**H-048 · 2026-07-10 · DATA**
Owner supplied two real Gusto payroll journal exports (per-employee, per-pay-period hours/rate/gross/taxes/net), filed to `reference/` as immutable sources: `tupelo-landscaping-llc-payroll-summary-2025-04-11-to-2026-04-11.csv` and `tupelo-landscaping-llc-payroll-summary-2026-04-11-to-2026-07-09.csv`, both byte-identical to the originals (verified by MD5 before and after the move). Confirmed directly, not assumed: the two files' paydays don't overlap (file 1 ends 04/10/2026, file 2 begins 05/05/2026, a 25-day gap) — the shared "2026-04-11" boundary in both filenames is the report-generation cutoff, not a duplicated period. Added a "Payroll journal exports" section to `reference/README.md`; deliberately did **not** give these the dated-snapshot + refresh-procedure treatment `CATALOG-UPDATE.md`/`REVENUE-UPDATE.md` get, since payroll data doesn't yet feed any script-regenerated derivative — that decision is separate and deferred. **A comprehensive sweep of every RETURNED transaction in the full 89-file Relay history (not just the ones found while investigating a narrower question) surfaced a real, already-committed bug.** All 6 RETURNED Relay transactions, cross-checked against all 5 ledger files: one $0.09 bank-verification bounce (immaterial, already correctly unbooked); three payroll `NET` bounce-then-resubmit pairs ($508.84, $557.69, $650.23 — corroborated exactly by the payroll files' own "Reversal Payroll" markers or, in the $508.84 case, by Relay alone since the export carried no marker for it) — none of these are booked anywhere in the current ledger, since labor is still a synthetic ESTIMATE with no Relay linkage, so they carry no current correction; and **two Gusto administrative-FEE bounce-then-resubmit charges, both wrongly booked as ACTUAL in `model/data/ledger-overhead.csv`** (2025-05-05 $158.00, FEE 563163; 2025-05-07 $158.00, FEE 598582 — both bounced same-day per a matching RETURNED receive, with only the third attempt, 2025-05-15 FEE 923930, actually clearing). **Root cause:** `model/populate_step4.py` filtered Relay Spend-side rows on `status == SETTLED` alone — a bounced-then-returned debit is itself still marked SETTLED on the Spend side; only a separate, later RETURNED *Receive* row reveals it never actually cleared, and nothing cross-checked the two. **Fixed at the code level, not just the data:** added `match_payments.exclude_reversed_spend()` (new function, shared by both scripts) to drop any settled Spend row matched by payee + an exact amount match + a 3-day window to a RETURNED Receive, before any ACTUAL row is built from it; wired into `populate_step4.py`'s `main()`. "Reversed the same transaction" is a binary fact, not an approximate one, so amounts are compared as exact integer cents (`round(amount*100)`), never a floating-point epsilon window — an earlier draft of this function used `abs(a-b) < 0.01`, which is a materially different check: it falsely matched an unrelated $0.08 charge against a $0.09 return (their float difference, `0.009999999999999995`, happens to land just under 0.01, even though $0.08 ≠ $0.09). Caught and corrected before commit, not shipped as a "harmless quirk." Re-ran `populate_step4.py` twice after the exact-match fix, with a fresh parse each time: both runs excluded exactly the same **6** rows — the 3 payroll `NET` pairs, the 2 Gusto FEE pairs, and the $0.09 ACCTVERIFY (which does have its own exact-amount RETURNED match) — with the $0.08 false positive gone, and produced the identical corrected ledger both times. **Correction, verified exactly:** Gusto overhead rows 17 → 15; Gusto subtotal $1,958.00 → **$1,642.00** ($58×1 + $94×1 + $100×2 + $109×5 + $115×4 + $127×1 + $158×1 = $1,642.00, checked against the file); total overhead $5,376.46 → **$5,060.46** — a correction of exactly **$316.00**, matching the two removed $158.00 rows. Regenerated `model/financial-model.xlsx` (gitignored, not staged) so the workbook isn't left stale against the corrected data. **Corrected `CONTEXT.md` D4**: real payroll data shows Konji held $20/hr for **all 20** of his real, completed pay periods in both files (through 06/15–06/28/2026, payday 07/07/2026, the most recent completed period); the negotiated $25/hr rate takes effect starting the next period, not yet run as of the exports. The original "$25/hr base from day one" wording is struck through, not deleted, per this repo's changelog-preservation convention. **Documented, as a factual finding only:** the owner confirmed `Beauvais, Xavier` in payroll is the same person as `CONTEXT.md`'s business-partner Xavier, doing real paid hourly work throughout alongside the equity arrangement (now ended, per the owner, as of April 2026). Verified directly against the payroll data rather than taking the owner's account alone: Xavier's rate was $25/hr in every period from 2025-03-24 through the period ending 2026-05-31, moving to $20/hr starting the 06/15–06/28/2026 period (payday 07/07/2026) — the exact same period Konji's $20/hr is still current in. **The equity payout figure, the April 2026 end-date's significance, and any ledger/projection-layer treatment of Xavier's payout remain explicitly deferred to a dedicated future session — nothing about them was decided or built here.** No labor-ledger rebuild from real payroll data was attempted in this pass; that stays separately scoped, per the instruction governing this one.
*Basis:* `reference/tupelo-landscaping-llc-payroll-summary-*.csv` (this pass, new files); `model/match_payments.py` (`exclude_reversed_spend`, new function, this pass); `model/populate_step4.py` (updated, re-run twice); `model/data/ledger-overhead.csv` (corrected); direct parse of all 89 `reference/Relay*.csv` files; `CONTEXT.md` D4 (corrected) and the Xavier "Key people" entry (updated); owner confirmation on the Xavier employee/partner identity and the equity arrangement's April 2026 end, 2026-07-10.

---

**H-049 · 2026-07-10 · DATA**
Rebuilt `model/data/ledger-labor.csv` entirely from the two real Gusto payroll exports (H-048), fully replacing the old 66-row synthetic ESTIMATE ledger (33 two-week periods × 2 role rows, per D2's original design) with **130 ACTUAL rows** mirroring the source exactly. **`model/data/employee-role-map.csv`** (new file, 16 role-period rows for 15 distinct employees) uses a time-varying schema — `employee,role,effective_date` — because a static one-role-per-person file cannot express Xavier's real Gusto title change: Crew Lead through his last $25/hr period (05/18–05/31/2026), then Crew Member starting his first $20/hr period (06/15/2026, re-verified directly against the payroll rows this pass). Checked directly whether any other employee needed the same treatment: Konji's rate is $20/hr in all 20 of his real completed periods with no title change evidence, so he gets one constant "Crew Lead" row; Katherine Driscoll's rate changed $20→$30 in her latest period, but the owner confirmed only her *current* title ("Landscape Consultant"), never a prior different one, so she also gets one constant row — flagged explicitly as an asymmetry with Xavier's case, not assumed away. Nickname/legal-name variants (Katie/Katherine, Konji/Konjinet, Nate/Nathaniel) are recorded as an `alias` column; the ledger's own `category` field uses payroll's exact "Last, First" string. **`model/populate_labor_from_payroll.py`** (new file) is the loader: maps columns by name (`dict(zip(header, row))`), never fixed position, since the two files' schemas differ (File 1 has a `Commission` column File 2 lacks; the employer-tax columns appear in a different order). It skips the two "Reversal Payroll"-marked payday-blocks entirely (05/19–06/01/2025 payday 06/11/2025; 08/11–08/24/2025 payday 08/29/2025), relying on each period's separate, clean resubmission payday instead, since a reversed period's own "Payroll Totals" row reflects only the reversal batch, not zero. **A new payday-block type was discovered mid-build and had to be handled, not assumed away:** a single unmarked `"Tax Reconciliation Payroll"` block (payday 03/31/2026) carries a $0-wage, employer-tax-only correction (a VA Unemployment true-up, three employees, combined **-$5.37**) — the parser now recognizes it as its own block type and the loader emits no labor row for it (blank, not "0.00," hours/rate fields — structurally distinct from a genuine $0-hour idle period) but does emit its tax delta to the overhead ledger. `amount` is the recorded Gross Earnings value directly, never `quantity × unit_rate` re-derived, per the $0.08 rounding gap already found in H-048 for Xavier's period. The mirror-the-source design holds throughout: 10 genuinely empty payday-blocks (period-boilerplate rows with zero employees listed, not even $0 rows — a real feature of the source, confirmed by direct inspection of one, 04/21–05/04/2025) get no ledger rows at all; dismissed employees (Littlejohn, last appearance period ending 08/10/2025, 5.47 hrs; Daschke, last appearance the same period, 2.42 hrs — both re-confirmed directly this pass) simply stop appearing, with no trailing $0 rows. **Labor total: $17,343.84 across 130 rows** (independently re-summed directly from the written CSV, not just the build script's own printed total). **Employer-payroll-tax-burden** (new overhead category, `category="Gusto", subcategory="employer-payroll-tax-burden"`, one row per pay period, aggregating Employee Taxes + Employer Taxes across all employees that period — kept in `ledger-overhead.csv`, not `ledger-labor.csv`, so labor's `amount` keeps meaning "wages to a person," per the earlier-scoped design): **25 rows, $3,093.17 total** (independently re-summed from the file), added on top of the 77 pre-existing overhead rows for **102 total** overhead rows (32 Stripe + 15 Gusto payroll-provider-fee + 15 General Liability + 13 Homeworks/Copilot + 1 Workers Comp + 25 employer-payroll-tax-burden + 1 Squarespace = 32+15+15+13+1+25+1 = 102, checked by direct count against the file). **A real bug was found and fixed while integration-testing against `model/populate_step4.py`, not left latent:** that script's `OWNED_OVERHEAD_CATEGORIES` filter matched on `category` alone, so re-running it after this pass silently deleted the new employer-payroll-tax-burden rows (both share `category="Gusto"` with the payroll-provider-fee rows it owns). Fixed by switching the filter to `(category, subcategory)` pairs; re-ran both scripts in both orders afterward and confirmed the ledger converges to the same 102-row state regardless of run order. **Full-history reconciliation gate run, not just spot-checked:** across all 25 non-reversed, non-empty payday-blocks, the 24 with nonzero Net Pay matched a specific Relay `GUSTO` `NET` transaction exactly (24/24); all 25 have nonzero combined tax, of which 24 matched a specific Relay `GUSTO` `TAX` transaction exactly, with **one genuine exception, not forced or dropped**: the $5.37 Tax Reconciliation block has no discrete matching Relay transaction anywhere near its 03/31/2026 payday, and the neighboring periods' own NET/TAX figures already reconcile exactly to their own separate Relay transactions, so the $5.37 was not silently absorbed into an adjacent period either — booked as ACTUAL from the payroll source regardless, since the source figure itself is not in question, only its cash-side trace. This becomes the standing hard gate for this ledger going forward. **`model/populate_step4.py` updated:** removed the now-superseded `build_labor_rows()` synthetic ESTIMATE generator and its call in `main()` entirely (dead code, not left commented out), since labor is now owned by `populate_labor_from_payroll.py`; docstring updated to redirect. **`model/build_model.py`'s stale July-2026 note was re-examined, not blindly deleted as the task assumed:** re-verification showed July 2026 still shows $0 labor even with real data, because the ledger dates rows by period-start (accrual convention) and the last real period starts 2026-06-15 (06/15–06/28/2026, paid 07/07/2026) — the next real period (06/29–07/12/2026) simply hadn't been processed/paid yet when the payroll export was pulled (export window ends 07/09/2026). The old note (written against the synthetic generator's fixed 14-day cadence) was removed, but a new, corrected note was written in its place explaining the real reason, rather than leaving July's $0 unexplained. **`CONTEXT.md` D2 reframed via strikethrough** (original text preserved, not deleted, per this repo's convention): the 2-person/50-50/$25-$20/~20-person-hours model is now stated explicitly as a forward-looking projection-layer assumption only, not a description of historical actuals; historical labor is now ACTUAL, sourced from real payroll data. Follow-Up #2 updated to note it no longer gates labor-actuals capture the way it did when D2 was written. **New Follow-Up #19** documents the known scope limitation, per the task's explicit instruction not to treat this as solved: payroll actuals carry no job/customer attribution, and cover all hours combined (recurring + one-off project work), broader in scope than D2's original recurring-only estimate — not a like-for-like replacement. **New Follow-Up #20**, split out from #19 rather than bundled with it (owner-flagged: a scope decision and a small unresolved bank-trace gap are different kinds of open items, and this repo's convention is one Follow-Up per distinct issue — closing one should not read as implicitly closing the other), documents the $5.37 unreconciled tax-correction gap on its own. The "What is real vs. estimated" summary and `model/PHASE7-PLAN.md`'s Labor paragraph were both updated to match, replacing the old "coarse sanity check, cannot override the estimate" framing with this pass's hardened, full-history-verified gate. Regenerated `model/financial-model.xlsx` (gitignored, not staged).
*Basis:* `reference/tupelo-landscaping-llc-payroll-summary-*.csv` (re-parsed fresh this pass); `model/populate_labor_from_payroll.py` (new file); `model/data/employee-role-map.csv` (new file); `model/data/ledger-labor.csv` (fully replaced); `model/data/ledger-overhead.csv` (25 rows added); `model/reconcile_payroll_relay.py` (new file, full-history gate); `model/populate_step4.py` (bug fixed, labor generator removed); `model/build_model.py` (stale note replaced with a corrected one); `CONTEXT.md` D2 (reframed), Follow-Up #2 (updated), Follow-Ups #19–20 (new); `model/PHASE7-PLAN.md` Section 3(c) (updated); direct parse of all 89 `reference/Relay*.csv` files via `model/match_payments.py`.

---

**H-050 · 2026-07-10 · REVISION**
Pre-review cleanup pass resolving four loose ends left after H-049, ahead of a "what's left before this model is genuinely usable" review. **(1) Reconciliation tab's labor scope, re-derived fresh, was stale.** `model/build_model.py`'s Reconciliation tab and its own module docstring both still stated "Labor: 100% ESTIMATE. No time-tracking data exists to check hours against" — true before H-049, false after it. Confirmed the tab has zero dependency on `reference/` raw files or `model/match_payments.py` (checked its imports directly) — it only reads the five `model/data/ledger-*.csv` files, matching its own stated contract ("reads `data/`, generates the workbook") and the precedent already set by Check 2, which references H-044's payment-classification gate by citation rather than re-running it inline. **Recommendation applied: keep `model/reconcile_payroll_relay.py` standalone** (preserves the D6/D7 data-layer/projection-layer boundary; avoids duplicating Relay-parsing logic that already lives in the payroll scripts) and fix the tab's text, following Check 2's own reference-not-rerun pattern, rather than importing the gate's logic to re-run at every build. Added **Check 5**: cites `model/reconcile_payroll_relay.py`'s own gate result (24/24 Net Pay + 24/25 tax exact matches, the one exception being the known $5.37 correction, Follow-Up #20) rather than re-parsing `reference/` inline. Rewrote the "What this tab does NOT verify" section to drop the stale ESTIMATE line and cite Follow-Ups #19–20 instead. **(2) `reference/PAYROLL-UPDATE.md`** (new file) written now that the payroll-integration decision has been made (Sequencing Step 5, H-049) — `reference/README.md`'s payroll section previously and correctly deferred writing this "until the payroll-integration decision is made" and said to revisit "the same session that decision is made." Follows the same steps-then-cadence-then-what-it-gates-on structure as `CATALOG-UPDATE.md`/`REVENUE-UPDATE.md`: how to pull a fresh Gusto export (its ~1-year rolling window will eventually age the current two files out), re-run `model/populate_labor_from_payroll.py` (watching for an `[WARNING] ... not found in role map` line — a new hire needing an `employee-role-map.csv` entry before proceeding) and `model/reconcile_payroll_relay.py` (expecting zero *new* exceptions beyond the known $5.37 gap), and what a re-run gates on. `reference/README.md`'s payroll section updated to point to it instead of stating the procedure as deferred. **(3) Blended labor rate, reassessed against the real roster, was found to conflate three structurally different roles.** Checked directly: `ledger-labor.csv`'s `blended_rate` calculation in `build_model.py` summed every category in the ledger indiscriminately, meaning it now blends crew wages ($20–25/hr, Crew Lead/Crew Member) with Cyrus Beauvais's CEO-titled hours ($25/hr, 113.85 hrs, $2,846.33) and Katherine Driscoll's Landscape Consultant hours ($20→$30/hr, 13.50 hrs, $364.17) — neither a crew staffing cost, and Driscoll's role in particular a materially different, specialized rate. **Recommendation: scope the "blended labor rate" KPI to crew roles only (Crew Lead, Crew Member) — its purpose per D2 is a crew staffing-cost figure to inform future job-cost estimates, and CEO/Consultant hours are not that.** Implemented: `build_model.py` now computes `blended_rate` from crew-role rows only (aggregate check: $14,133.34 / 632.84 hrs = **$22.33/hr**, vs. the prior all-roles figure of $17,343.84 / 760.19 hrs = $22.82/hr — a small but real distortion, and a growing risk as the non-crew hour mix shifts) and reports non-crew labor dollars in a new, separate column rather than silently dropping them (aggregate check across all 17 months: crew $14,133.34 + non-crew $3,210.50 = **$17,343.84**, exactly matching H-049's independently-verified labor total; non-crew $3,210.50 = CEO $2,846.33 + Landscape Consultant $364.17, checked by direct addition). Column renamed "Blended crew labor rate ($/hr)" for clarity. **(4) `RUNBOOK.md`'s Phase 7 section was badly out of sync** — five of its seven checkboxes were still unchecked (`build_model.py` write/run, dependency reinstall, reconciliation tab, build execution, tab validation, data+script commit) despite all of that work being done across H-046 through H-049, and the "Populate what's real" checkbox one line above still cited stale figures (labor: "66 ESTIMATE rows," overhead: "79 ACTUAL rows"). All six now checked, each citing the specific H-number(s) it was actually done in (`build_model.py`: H-047; reconciliation tab, broadened twice: H-047 then H-049; build run: H-047/H-048/H-049/this pass; validation: H-047/H-048/H-049; commits: `8024cc4`/`febe13e`/`93b482a`/`07852f6`), and the stale labor/overhead figures corrected to the current state (labor: 130 ACTUAL rows per H-049; overhead: 102 ACTUAL rows, checked as 77 pre-existing + 25 new = **102**, matching H-049's own arithmetic). The "Done when" line updated from "Phase 7 has not begun" to reflect that Phase 7's original scope is complete and this pass's fixes are standing maintenance on an already-built model, not remaining setup.
*Basis:* `model/build_model.py` (Reconciliation tab Check 5 added, docstring and "does NOT verify" section corrected, `blended_rate` rescoped to `CREW_ROLES`, new non-crew-labor column); `reference/PAYROLL-UPDATE.md` (new file); `reference/README.md` (payroll section updated); `RUNBOOK.md` Phase 7 (six checkboxes synced); `model/data/ledger-labor.csv`, `model/data/ledger-overhead.csv` (re-verified, unchanged by this pass — text/logic corrections only, no ledger rows added or removed); `model/reconcile_payroll_relay.py` (cited, not modified); `CONTEXT.md` Follow-Ups #19–20, D2 (cited, not modified this pass).

---

**H-051 · 2026-07-10 · TOOLING**
Process/tooling reliability pass closing every pipeline gap the prior "what's left" review surfaced, ahead of scoping the projection layer. **(1) The ledger-revenue.csv regeneration gap, the one genuinely non-trivial fix, designed and closed first.** Confirmed directly: `model/parse_invoices.py` only ever wrote `revenue-invoices.csv`/`revenue-line-items.csv`; `model/match_payments.py`'s own docstring already disclaimed touching invoice-side rows ("existing `event=invoice` rows are left untouched"). No script anywhere regenerated the 319 `event=invoice` rows (service/`BLOCKED — unmapped`/surcharge/tip) from a fresh parse. **Chose Option A** (a new, dedicated `model/build_ledger_revenue.py`) over folding this into `match_payments.py`, because match_payments.py's job (matching bank deposits against invoices) is conceptually distinct from this one (translating billed line items into ledger form, a natural continuation of `parse_invoices.py`'s own job) — mirrors this repo's established one-script-one-job pattern (`populate_step4.py` vs. `populate_labor_from_payroll.py` owning disjoint categories) and keeps the responsibility for "who builds invoice-side rows" unambiguous, exactly what was missing before. The transformation logic was reverse-engineered from the existing ledger, not guessed, and verified **byte-for-byte identical against all 319 existing invoice-side rows before being trusted** (0 mismatches): one row per line item (category = "BLOCKED — unmapped" if bundled else "service", amount = `line_net`, date = `invoice_date`); one additional row per line item with nonzero `line_surcharge` (148 of 152); one row per invoice with nonzero `tip` (19 of 68). Confirmed idempotent (identical MD5 across two consecutive runs) and confirmed it preserves `match_payments.py`'s 58 payment rows and 32 Stripe fee rows untouched (re-ran `match_payments.py` immediately after; both files' content — not just row count — verified identical to their pre-change state via a sorted-row comparison, with only harmless row-order differences). Fail-closed gate: built rows must sum to the $28,316.57 anchor (H-043) to the penny, verified via addend breakdown ($26,335.18 net service/materials revenue + $1,556.47 surcharge + $424.92 tips = **$28,316.57**, matching exactly). **(2) Every data-quality script made fail-closed.** `model/populate_labor_from_payroll.py`: the "employee(s) not found in role map" line changed from a printed `[WARNING]` to a `GATE FAILED` + `SystemExit(1)`, raised before either `ledger-labor.csv` or the tax-burden overhead rows are written — an unlisted employee means a role fell back to a placeholder, a real defect, not an expected exception. `model/populate_step4.py`: the "expected chainsaw + reimbursement rows, got fewer" check changed from a printed `WARNING` (after the file was already written) to a `GATE FAILED` + `SystemExit(1)` raised *before* `ledger-capital.csv` is written, since fewer than 2 rows means the known, historically-fixed Relay transactions weren't found. `model/reconcile_payroll_relay.py`: added a `KNOWN_EXCEPTIONS` allowlist (currently just the one documented $5.37 Tax Reconciliation gap, Follow-Up #20, matched by stable substring) — any exception *not* matching it now halts with `SystemExit(1)`, so a genuinely new unreconciled period can no longer pass silently while the one already-investigated, immaterial gap correctly keeps passing. `model/build_model.py`'s Reconciliation tab: Check 1 (revenue invoice total vs. the H-043 anchor) already computed a `check1_pass` boolean but never acted on it — now raises `SystemExit(1)` before `wb.save()` if it's `False`, so a broken anchor can never ship in a regenerated workbook. Checks 2–5 were confirmed to have no computed boolean of their own (they cite other scripts' gates, e.g. Check 5 citing `reconcile_payroll_relay.py`'s), so nothing further to harden there — those other scripts are the ones actually enforcing those checks, and are now themselves fail-closed. Every hardened check was tested with a synthetic failure (a broken anchor, a monkeypatched empty `KNOWN_EXCEPTIONS`, a removed role-map entry, a non-existent pipeline stage) and confirmed to halt with the correct nonzero exit code before any file was written; the one test that did write a file (a stale `employee-role-map.csv`, from `write_role_map()` running before the gate check) was caught, and the file was restored from git before real work resumed — no ledger CSV was affected by any test. **(3) `model/refresh_all.py`** (new file): a single orchestrator running all 7 stages (`parse_invoices.py` → `build_ledger_revenue.py` → `match_payments.py` → `populate_step4.py` → `populate_labor_from_payroll.py` → `reconcile_payroll_relay.py` → `build_model.py`) as subprocesses in the documented order, halting immediately on any nonzero exit and printing a completed/not-run summary. **A real bug was found and fixed during testing, not shipped:** the first version's own progress banners were invisible until the whole run finished whenever output wasn't a live terminal (redirected to a file, as this session's own test run was) — Python fully buffers a non-TTY stdout by default, so the parent script's `print()` calls sat unflushed while each child's own output flushed at its own exit, making every banner appear bunched at the end instead of interleaved with each stage's live output. Fixed by adding `flush=True` to every print in the orchestrator; re-tested with output redirected to a file and confirmed correct interleaving. Confirmed the orchestrator produces the same end state as manual sequential execution: ran it end-to-end and compared all five ledger files against a pre-orchestrator snapshot via sorted-row comparison — all five identical (377/102/130/1/2 rows respectively). **(4) Documentation staleness fixed.** `README.md`'s Structure section no longer describes `build_model.py`/the workbook as "not present yet — Phase 7"; added a new "Model data-refresh pipeline" section listing all seven pipeline stages and their roles. `SETUP.md` step 7(b)'s "has not been written yet" framing corrected, with a pointer to the new orchestrator. `RUNBOOK.md` line 111's leftover "not yet started" phrase (inside an already-superseded historical note on item F) corrected to state Phase 7 is complete. `reference/REVENUE-UPDATE.md` gained an explicit new step 6 (running `model/build_ledger_revenue.py`, flagged as "required and easy to forget") between the existing parse step and the diff-review step, plus a pointer to `model/refresh_all.py` as the simpler alternative — the documented procedure now actually matches what a real refresh requires. **(5) Follow-Up #14 (tips) marked resolved** in `CONTEXT.md`: it read as an open ask, but `ledger-revenue.csv` already carries a distinct `category="tip"`, 19 rows, summing to exactly the $424.92 the Follow-Up itself cites (re-verified fresh this pass, not assumed from the prior review) — closed in place, not deleted, per this repo's changelog-preservation convention.
*Basis:* `model/build_ledger_revenue.py` (new file); `model/refresh_all.py` (new file); `model/populate_labor_from_payroll.py`, `model/populate_step4.py`, `model/reconcile_payroll_relay.py`, `model/build_model.py` (fail-closed gates added); `README.md`, `SETUP.md`, `RUNBOOK.md`, `reference/REVENUE-UPDATE.md` (staleness fixed); `CONTEXT.md` Follow-Up #14 (marked resolved); direct inspection of `model/parse_invoices.py`/`model/match_payments.py` to confirm the regeneration gap; a full end-to-end orchestrator run compared against a pre-orchestrator ledger snapshot; synthetic failure tests of every hardened gate, 2026-07-10.

---

**H-052 · 2026-07-10 · TOOLING**
Projection-layer build (Part 2 of the projection-layer work, implementing `model/PROJECTION-PLAN.md` Section 6's ten decisions, all confirmed as recommended by the owner). **(1) Independent fix first, per Decision 2:** `CONTEXT.md` Section 3's "$4,908 is the strongest single-month figure to date" corrected — re-verified fresh against `model/data/ledger-revenue.csv` before writing anything (not reused from the scoping document's own number), confirming June 2026 billed revenue is **$6,019.89**, the real current peak. Original text struck-through-preserved, not deleted, per this repo's convention; the correction notes the business is ~60% of the way to the $10,000 target at its peak, not ~49%. **(2) New Follow-Up #21** added to `CONTEXT.md` (`cash_starting_balance`'s periodic re-verification need, Decision 9) — flagged, not solved, since the ledger's own net-cash arithmetic can't reconstruct the real bank balance (~85% of real Spend-side Relay cash has no ledger row) and this snapshot will drift stale the same way the $1,500→$1,400→$1,225.33 lineage did without periodic re-checks. **(3) `model/data/assumptions.csv`** (new file, originally 18 data rows per `PROJECTION-PLAN.md` Section 3's exact schema and rows, later 19 — see (6)): `peak_revenue_target` ($10,000); `cash_buffer_reserve` ($4,000) + `cash_buffer_truck_hedge` ($2,000); `cash_starting_balance` ($1,225.33, dated 2026-07-06, H-039 — the one deliberately-hardcoded snapshot, since it can't be derived from the ledger); Konji's full multi-row structure (3 `konji_hourly_rate` rows — current $20/hr, a "negotiated $25/hr" reference row with `effective_date` deliberately left blank per Decision 8 rather than guessed, and the post-phase-switch $25/hr row from 2027-06-01; `konji_revenue_share_pct` 6% for 2026-09-01–2027-05-31; canvassing rate/bonuses and winter-project rate, each capped at 2027-05-31); `xavier_payout` ($1,800) and `owner_truck_debt_repayment` ($3,800), each carrying a plain-text `trigger_condition` rather than a date, per Decision 5; two `anais_canvassing_rate`/`anais_backup_lead_rate` BLOCKED rows (Follow-Up #11); `crew_labor_model` (D2's $22.50/hr, kept for reference only, per Decision 3 explicitly NOT used in the calculation); `revenue_growth_rate_assumption` (75.9%, per Decision 10, its own labeled row rather than hardcoded into the script). A one-line header comment (`#`-prefixed, skipped by the new loader) notes that TARGET/POLICY rows sourced from `strategy/strategic-plan.md` must be kept in sync if that document's figures are ever revised. **(4) `model/build_model.py`'s `build_plan_vs_actual()` stub replaced** with the real calculation, per `PROJECTION-PLAN.md` Section 4: a new `load_assumptions()`/`get_assumption()` pair (the same time-varying-row-selection pattern `populate_labor_from_payroll.py`'s `role_for()` already established) reads the assumptions file; **actual months** (2025-03 through 2026-07, all 17 real months) are filled using the exact same collected/labor/overhead computation the Monthly P&L sheet already uses, not a separate recomputation; **projected months** (2026-08 through 2027-06, the plan's own horizon, ending at D4's phase-switch boundary) use seasonal-naive revenue (prior-year same month × the growth-rate assumption), a crew-labor and overhead trend recomputed fresh from the ledger's trailing 3 real months every build (Decision 3 — never a stored constant, per D6), Konji's revenue share applied via his time-varying assumption row's effective/end dates, and the Xavier/owner-truck-debt triggers evaluated against the month-by-month running projected balance exactly as Section 4 specified (first month balance clears $6,000 → Xavier's payout fires; first month *after that* it clears $6,000 again → the owner's repayment fires). **Three scenarios shown side by side, per Decision 6 — no single collapsed number:** Low (flat-continue the last full month's actual net, **−$943.12/mo**, never reaches the buffer at that rate); Mid (the full seasonal-cycle average, **$174.43/mo**, gap $4,774.67 ÷ $174.43 = **27.4 months**, checked by direct division); High (the detailed month-by-month table, starting from the $1,225.33 anchor). Anais's two BLOCKED rows are excluded from every sum with a stated caveat on the sheet's face (four caveat lines total, covering Anais, Konji's un-volumed canvassing/winter-project rates, the revenue-share simplification, and the starting-balance snapshot risk) — never folded in as $0, per Decision 4/Step 4's explicit requirement. **(5) Verification, per Step 5, before trusting any of the above:** cross-checked all 17 actual-month rows in the new sheet against the Monthly P&L sheet's own Collected/Labor/Overhead figures for the same months — **0 mismatches across all 17 months**, confirmed programmatically, not by spot-check. Independently recomputed the Step 5 worked example (August 2026) from the final generated workbook: outflow matched `PROJECTION-PLAN.md`'s worked figure exactly ($2,673.63 both), but revenue and net showed a small, fully-explained discrepancy — the code's $4,678.41 vs. the plan's $4,677.99 (**a $0.42 difference**, checked by direct subtraction), and net $2,004.79 vs. $2,004.36 (**a $0.43 difference**). Root cause isolated and confirmed, not just asserted: the plan document's hand-worked example used the raw unrounded YoY growth rate (75.88400500210722%, computed live from $5,789.56→$10,182.91), while `assumptions.csv` correctly stores the rounded, labeled **75.9%** value per Decision 10's own design (a clean, documented assumption cell, not a silently-carried float) — $2,659.70 × 1.7588400500210722 = $4,677.99 vs. $2,659.70 × 1.759 = $4,678.41, the entire gap accounted for by this one rounding difference and nothing else. Immaterial (0.01% of the projected figure) and not a bug — reported per Step 5's explicit instruction rather than silently absorbed. The Low and Mid scenario figures matched the plan document's own worked numbers exactly, with no rounding-path difference (they don't depend on the growth-rate assumption). Regenerated `model/financial-model.xlsx` (gitignored, not staged); re-ran the full `model/refresh_all.py` pipeline end to end afterward and confirmed zero ledger CSV changes (assumptions.csv is consumed only by `build_model.py`, not by any upstream pipeline stage). **(6) Crew-labor-seasonality investigation, before trusting the High scenario's negative-balance finding as final** — the owner explicitly instructed this check rather than accepting (4)/(5)'s result at face value. Investigated directly: does crew labor actually drop in winter the way revenue does, using the full ledger, not just the one seasonal cycle already cited? **Confirmed yes, and confirmed not a hiring-gap artifact.** Crew labor correlates with revenue seasonally — r=0.77 vs. billed revenue, r=0.69 vs. collected, over the cleanest 13-month window with full data coverage on both sides (June 2025–June 2026). The starkest evidence: crew labor and revenue both go to **exactly $0** in January and February 2026. Checked directly whether this predates Konji's hire (it doesn't): his first labor row is **2025-05-19**, nine months before the Jan/Feb 2026 gap; every other crew member also has zero rows in both months, confirming a genuine business-wide work stoppage, not a staffing-transition artifact. **This meant the flat trailing-3-month crew-labor trend used in (4) was wrong for winter months** — it would carry ~$1,870/mo of crew cost into a projected January or February when the real historical pattern shows it collapsing to zero, materially overstating projected winter cash burn. **Corrected: crew labor is now projected seasonal-naive**, matching revenue's own method (prior-year same-month × a growth-rate assumption), via a new `crew_labor_growth_rate_assumption` row (19th row in `assumptions.csv`) — its value, **92.8%**, is crew labor's own observed YoY growth (Apr–Jun 2025 $2,910.71 → Apr–Jun 2026 $5,611.83, checked by direct computation), computed the identical way as revenue's 75.9% but over the right base metric. Flagged as a new open decision (Decision 11, `PROJECTION-PLAN.md` Section 6), not silently chosen: crew labor's own rate differs materially from revenue's, plausibly because Konji's staffing ramp-up is a cost driver independent of revenue volume — using revenue's rate instead would risk understating near-term cost. **The corrected High scenario tells a materially different, more honest story:** re-run end to end, the projected balance now stays positive throughout (previous low point: deeply negative across Oct 2026–Apr 2027 under the flat-trend design; corrected low point: **$865.47** in April 2027) and **reaches the $6,000 buffer in June 2027 (month 11 of 11)**, triggering Xavier's $1,800 payout and ending the horizon at $4,527.14 — versus the pre-correction result of never reaching the buffer and ending at $4,340.28. Re-verified after this change: all 17 actual months still match the Monthly P&L sheet exactly (unaffected, since only projected months changed); the August 2026 worked example's outflow, revenue, and crew-labor figures were independently recomputed from the corrected code and matched exactly ($1,433.37 = $743.45 × 1.928, checked by direct multiplication; outflow $2,236.39; net $2,442.02). `model/PROJECTION-PLAN.md` Sections 3.6, 4, and 5 updated to describe the seasonal-naive method as the resolved design, not the flat-trend original, with the flat-trend framing explicitly marked superseded rather than deleted, per this repo's convention.
*Basis:* `model/data/assumptions.csv` (new file, 19 rows including the crew-labor-growth-rate row added in (6)); `model/build_model.py` (`build_plan_vs_actual()` rebuilt, `load_assumptions()`/`get_assumption()`/`month_add()` added, crew-labor projection changed from a flat trailing-3-month trend to seasonal-naive); `model/PROJECTION-PLAN.md` Sections 3.6/4/5/6 (implemented, then amended per the seasonality investigation); `CONTEXT.md` Section 3 (peak corrected) and new Follow-Up #21; direct fresh verification against `model/data/ledger-revenue.csv` before writing the peak correction; a full 17-month cross-check against the Monthly P&L sheet, run twice (before and after the crew-labor correction); an independent recomputation of the Step 5 worked example from the generated workbook, also run twice; a direct correlation check (crew labor vs. billed/collected revenue, cleanest 13-month window) and a direct check of Konji's first labor-row date to rule out a hiring-gap artifact; a full `model/refresh_all.py` pipeline run confirming zero ledger drift, 2026-07-10.

---

**H-053 · 2026-07-10 · REVISION**
Resolved Decision 11 (`model/PROJECTION-PLAN.md` Section 6, opened in H-052): the owner confirmed crew labor's seasonal-naive projection should use **revenue's own growth rate (75.9%)**, not crew labor's separately-observed rate (92.8%). **Unified the two assumption rows rather than keeping them as manually-synced duplicates** — considered explicitly, per the task's own instruction not to leave two independent rows holding the same number as a silent drift risk: removed the standalone `crew_labor_growth_rate_assumption` row entirely (`model/data/assumptions.csv`, 19 rows → **18 rows**, confirmed by direct count) and updated `revenue_growth_rate_assumption`'s label/source to document that it's now a single shared rate referenced by both the revenue and crew-labor calculations, with Decision 11's resolution and date cited directly in the row. `model/build_model.py` updated to match: the separate `crew_growth_rate_row`/`crew_growth_rate` lookup removed, the crew-labor projection line now reads `growth_rate` (the same variable revenue's projection already used) directly — a genuine single source of truth, not two numbers that happen to currently agree. **New High-scenario trajectory, re-run end to end:** the buffer target is now reached **one month earlier** — **May 2027 (month 10 of 11)**, not June 2027 (month 11) — triggering Xavier's $1,800 payout that same month (balance before payout $1,829.48 + $4,980.38 net = $6,809.86, checked by direct addition, clearing the $6,000 target). **For the first time, the owner's $3,800 truck-debt repayment also triggers within the horizon**, the very next month (June 2027: balance $5,009.86 + $1,146.78 net = $6,156.64, checked by direct addition, again clearing $6,000) — under both prior versions of this scenario (the original flat-trend design and the 92.8%-based seasonal-naive correction), the owner's payout never fired at all within the plan's stated horizon. The trajectory's low point also improved: **$1,338.17 in October 2026** (checked directly against the generated sheet), versus the 92.8%-based version's low of $865.47 in April 2027 — lower assumed crew-cost growth leaves more cash on hand throughout, not just at the end. Horizon-ending balance: **$2,356.64** (June 2027, after both payouts fire), versus $4,527.14 previously (June 2027, after only Xavier's payout had fired). **Re-verified before trusting this result:** all 17 actual months still match the Monthly P&L sheet exactly — **0 mismatches**, confirmed programmatically, unaffected since only projected months changed. Recomputed the August 2026 worked example independently: projected crew labor $743.45 × 1.759 = **$1,307.73**, checked by direct multiplication and matching the generated sheet exactly, using the identical method and precision as revenue's own $2,659.70 × 1.759 = $4,678.41 calculation (both now driven by the same 75.9% rate). **Checked the rounding-drift concern raised in review before making any change, per the task's explicit instruction not to fix something that isn't actually broken:** traced the High scenario's accumulation loop directly (`model/build_model.py`) and confirmed **zero `round()` calls exist anywhere inside it** — the running `balance` variable is carried at full float precision across every month, with `round(balance_start, 2)`/`round(balance_end, 2)` applied only at the point each row is appended to the worksheet for display. The described issue does not exist in this code; no fix was made, and none was needed. `model/PROJECTION-PLAN.md` Section 6's Decision 11 updated to state it is resolved (owner-confirmed), not open.
*Basis:* `model/data/assumptions.csv` (`crew_labor_growth_rate_assumption` row removed, `revenue_growth_rate_assumption` row updated, 19 → 18 rows); `model/build_model.py` (crew-labor projection now references the shared `growth_rate` variable directly); `model/PROJECTION-PLAN.md` Section 6 Decision 11 (marked resolved); direct re-run of the full `model/refresh_all.py` pipeline confirming zero ledger drift; a full 17-month Monthly-P&L cross-check; an independent recomputation of the August 2026 worked example; direct inspection of the accumulation loop's source to rule out the reported rounding-drift issue, 2026-07-10.

---

**H-054 · 2026-07-10 · REVISION**
Documentation/process pass, no ledger/code/model changes: two `CLAUDE.md` standing-rules additions and a new `CONTEXT.md` Follow-Up, motivated directly by this session's own findings. **(1) Assumption-cell sync guardrail** added as a new subsection under "Cross-reference check on structured edits" (the existing section this is a specific instance of, not a standalone rule): `model/data/assumptions.csv`'s TARGET/POLICY rows are sourced from `strategy/strategic-plan.md`/`CONTEXT.md`, not independently authored, and must be treated as a linked pair with their source figures — citing the Xavier Assumptions Log row's drift from the Equipment Investment Plan table (H-041) as the precedent this guards against, and H-052/H-053's own `assumptions.csv` build as the concrete thing it now governs. **(2) Three pipeline/data-source rules** added in a new "Pipeline and data-source rules" section, placed after the existing Model-update operating procedure: (a) refreshing any `reference/` source requires running `model/refresh_all.py` (or explicitly stating which downstream scripts are affected and why a full run isn't needed) before treating `build_model.py`'s output as current — never assume one source's refresh alone keeps every derived file in sync; (b) a new external data source must follow the established dated-snapshot + active-pointer + standing `*-UPDATE.md` pattern (`CATALOG-UPDATE.md`/`REVENUE-UPDATE.md`/`PAYROLL-UPDATE.md`), now a proven, repeated convention, not a one-off choice; (c) a new `assumptions.csv` row category or ledger schema field must be checked against `build_model.py`'s existing sheets — data invisible in the generated workbook is a silent gap, not a neutral addition. Stated concisely, per the task's own instruction, not restating the full history/reasoning behind each pattern, which already lives in the cited files. **(3) New Follow-Up #22** in `CONTEXT.md`: scopes a future repo-wide cross-reference/auto-sync audit, citing H-051's ledger-revenue.csv regeneration-gap finding as the direct precedent and motivation (a real, previously-undocumented dependency that went unsynced until caught, not a hypothetical concern). Names four concrete example dependency classes to catalog (assumptions.csv-on-strategy figures; RUNBOOK.md checkboxes on HISTORY.md entries; reference/README.md's active-snapshot pointers on the files they point to; CONTEXT.md Follow-Up statuses on the ledger's actual state) and the three-way classification each found dependency should get (automated fail-closed safeguard, documented manual `*-UPDATE.md` convention, or a real gap — H-051's own class of finding). Explicitly scoped as needing its own PHASE7-PLAN.md/PROJECTION-PLAN.md-style scoping document first, given its likely size, and explicitly not started in this pass.
*Basis:* `CLAUDE.md` (two new sections: the assumption-cell-sync subsection under "Cross-reference check on structured edits," and "Pipeline and data-source rules" after "Model-update operating procedure"); `CONTEXT.md` Follow-Up #22 (new); `HISTORY.md` H-041 (cited precedent for the assumption-cell-sync guardrail) and H-051 (cited precedent/motivation for Follow-Up #22); `README.md`'s "Model data-refresh pipeline" section and `model/refresh_all.py` (cited basis for the pipeline rules); `model/PHASE7-PLAN.md`/`model/PROJECTION-PLAN.md` (cited as the scoping-document precedent Follow-Up #22 should follow), 2026-07-10.

---

**H-055 · 2026-07-10 · REVISION**
Fixed the five findings `model/CROSSREF-AUDIT-PLAN.md`'s Section 3 catalogued (Follow-Up #22's initial audit pass), per confirmed decisions. Documentation/process edits only — no ledger, code, or model changes. **(1) `strategy/strategic-plan.md` line 141** (Konji's compensation): "$25/hr base from day one" struck through, not deleted, with a correction note citing H-048 — real payroll data shows $20/hr held for all real completed pay periods, $25/hr effective only from the next (not-yet-run) period. This duplicated `CONTEXT.md` D4's own text, which was corrected in H-048; this copy was missed at the time. A matching Revision Note row added to `strategy/strategic-plan.md`'s own changelog, per its established convention. **(2) `reference/PAYROLL-UPDATE.md` step 5** reworded: a new/unlisted employee must be added to `ROLE_MAP_ROWS` inside `model/populate_labor_from_payroll.py`, not to `model/data/employee-role-map.csv` directly — that file is fully regenerated from `ROLE_MAP_ROWS` on every run, so a direct CSV edit would be silently discarded on the next refresh and the same failure would recur. **A second, adjacent staleness was found and fixed in the same sentence, not separately scoped but too close to leave**: the step described the old pre-H-051 behavior ("watch for a `[WARNING]` line"), when the script has failed closed with `GATE FAILED` + a nonzero exit since H-051 — corrected to describe the actual current behavior. **(3) `reference/CATALOG-UPDATE.md`** gained a new step 8, mirroring `REVENUE-UPDATE.md`'s own step 6 (the H-051 fix): re-run `model/build_ledger_revenue.py` after `parse_invoices.py`, since a catalog-only refresh can change how existing invoice lines classify (service vs. `BLOCKED — unmapped`, `item` vs. `service` kind) even without new invoices, and skipping this step leaves the real ledger stale exactly the way H-051 already demonstrated. **(4) `model/PROJECTION-PLAN.md` Section 5** updated from the superseded 92.8%-based worked example to the resolved 75.9%-based figures already computed and verified in H-053 — transcribed, not recomputed: crew labor $743.45 × 1.759 = **$1,307.73**; outflow $1,307.73 + $803.02 = **$2,110.75**; net **$2,567.67** (the sheet's own value — noted explicitly that naively subtracting the two rounded, displayed figures, $4,678.41 − $2,110.75, gives $2,567.66, a $0.01 difference from the sheet's $2,567.67, because `build_model.py` rounds each column independently from full internal float precision for display, not by subtracting already-rounded values; caught during this transcription and documented rather than silently smoothed over). The stale "not yet owner-confirmed" language removed; the summary table and concluding narrative both updated to state the buffer is reached in **May 2027** (not June), with the owner's truck-debt repayment now also triggering the following month — matching H-053 exactly. **(5) `RUNBOOK.md` Phase 7's "Done when" parenthetical** reworded: "this pass" replaced with "originally written in the H-050 pass," and H-051 through H-054's subsequent work added to the list of standing-maintenance items it references, so the note no longer reads as if only H-046–H-049 exist. **Additional: resolved Open Decision 4** from `CROSSREF-AUDIT-PLAN.md` Section 4 — `CONTEXT.md` Follow-Up #22 updated to state this audit is **recurring, not one-time**: re-run at each future Revision Point (RP1 late July 2026, RP2 late December 2026, RP3 June 2027, per `strategy/execution-timeline.md`'s existing schedule). **Follow-Up #22 itself is explicitly left open/recurring, not marked resolved** — this entry closes out its *initial* pass only; each future Revision Point's audit should append its own findings to `model/CROSSREF-AUDIT-PLAN.md` (or a dated successor) and its own `HISTORY.md` entry, the same standing pattern as the `*-UPDATE.md` refresh cadences.
*Basis:* `strategy/strategic-plan.md` (line 141 corrected, Revision Note row added); `reference/PAYROLL-UPDATE.md` (step 5 reworded, two issues); `reference/CATALOG-UPDATE.md` (new step 8); `model/PROJECTION-PLAN.md` (Section 5 updated, transcribed from H-053, one rounding-path nuance caught and documented during transcription); `RUNBOOK.md` (Phase 7 "Done when" reworded); `CONTEXT.md` Follow-Up #22 (updated with Revision Point cadence, left open/recurring); `model/CROSSREF-AUDIT-PLAN.md` (the source of all five findings, cited throughout); `HISTORY.md` H-048 (cited correction being propagated), H-051 (cited precedent for findings 2–3), H-053 (source of the transcribed figures for finding 4), 2026-07-10.

---

**H-056 · 2026-07-10 · DECISION**
Logged, not fixed — new **Follow-Up #23**: reduce reliance on externally-authored prompts for routine repo maintenance. `HISTORY.md` logging (and other standing procedures) should trigger from `CLAUDE.md`'s own rules on Claude Code's own initiative, not require an explicit per-instance instruction; nearly every prompt this session had to separately ask for the `HISTORY.md` entry despite the History-logging rule already mandating one for every significant change. Scoped for a future session as a distinct question from Follow-Up #22 (which catalogs file/value dependencies, not behavioral self-triggering): audit whether each of `CLAUDE.md`'s other standing procedures (model-update loop, arithmetic self-check, cross-reference check, pipeline-refresh rule) is actually self-triggering or currently depends on explicit invocation. Carries an explicit implementation note, written in now regardless of timing: the likely fix is a proactive self-check Claude Code runs at session start and after every meaningful change, asking whether a standing rule now applies — a behavior-design change requiring its own dedicated session, not an afterthought bolted onto a smaller fix.
*Basis:* `CONTEXT.md` Follow-Up #23 (new), owner instruction, 2026-07-10.

**H-057 · 2026-07-10 · REVISION**
Resolved **Follow-Up #11** for Anais's canvassing rate and backup crew-lead rate, per owner confirmation; her regular-wage reduction is explicitly NOT part of this fix. **(1) `model/data/assumptions.csv`:** canvassing pay is confirmed a general crew policy, not Konji-specific, so the three formerly Konji-specific rows were renamed/reframed rather than duplicated — `konji_canvassing_rate` → `crew_canvassing_rate` ($15/hr), `konji_canvassing_bonus_fall` → `crew_canvassing_bonus_fall` ($15/client), `konji_canvassing_bonus_spring` → `crew_canvassing_bonus_spring` ($20/client) — values unchanged, only the ID/label generalized, `end_date=2027-05-31` carried over unchanged (flagged in the source note as inherited from Konji's own phase-switch boundary, not independently re-confirmed for a policy now stated as general). Confirmed via direct grep that no code reads these IDs (`build_model.py` only references them in caveat prose, not `get_assumption()` lookups), so the rename breaks nothing functionally. Anais's `anais_canvassing_rate` (previously BLOCKED, $0.00) was **removed outright**, not updated in place — she is now covered by the same `crew_canvassing_rate`/bonus rows as Konji, per the single-source-of-truth principle already applied to the shared revenue/crew-labor growth rate (Decision 11, H-053): one row two people reference, not two rows that could drift. `anais_backup_lead_rate` (Anais-specific, no Konji equivalent) was updated from BLOCKED/$0.00 to **CONTRACTED/$25.00/hr**, with a source note that it is reference-only (no client-volume/hours assumption exists to project against, same treatment as Konji's own un-volumed canvassing bonuses/winter-project rate) and is not consumed by the trajectory calculation. Net row count: 18 → 17 (one row removed, none added — three renamed in place, one updated in place). **(2) `model/build_model.py`:** the Plan vs Actual sheet's caveat text (previously: "Anais's canvassing/backup-lead cost (BLOCKED, Follow-Up #11) is excluded entirely") rewritten to state both rates are resolved and reference the shared policy, while noting neither is summed into the projection figures for the same no-volume-assumption reason as Konji's own un-volumed canvassing/winter-project rate — plus a new caveat line stating Anais's regular-wage reduction is pending, not yet effective, no row or ledger change made. Rebuilt `model/financial-model.xlsx` (gitignored, not staged): the Plan vs Actual sheet's caveat block was inspected directly post-build and reads correctly; all 17 actual months are numerically unchanged (this was a labeling/assumptions change only, not a calculation change — no code path sums the canvassing/backup-lead rows either before or after this fix). Re-ran the full `model/refresh_all.py` pipeline end to end: all 7 stages passed, confirming no ledger CSV was affected. **(3) `CONTEXT.md`:** Follow-Up #11 marked resolved for the canvassing/backup-lead portions, with the regular-wage reduction explicitly called out as still pending (no effective date, no change applied); Anais's Key People entry updated to match, mirroring how Konji's own not-yet-run rate change is described (D4). **(4) `strategy/strategic-plan.md`:** the Target Org Structure table's Anais row placeholder ("$/hr + per-sale") replaced with the real rate/bonus structure and $25/hr backup-lead rate (Function column also updated to note she substitutes as crew lead, matching `CONTEXT.md`'s existing description, so Comp and Function don't contradict each other); Konji's own compensation paragraph reworded so canvassing reads as a shared crew policy rather than a personal term, with a new paragraph stating the shared policy and citing Follow-Up #11's resolution; a Revision Note row added per the document's convention. **Explicitly out of scope, per owner instruction:** Anais's regular wage stays at $25/hr in all ledger, payroll, and assumption data — no row, ledger, or payroll change was made for the pending $25→$20/hr reduction; it remains pending until the owner reports the change has actually taken effect. **Known residual staleness, flagged not fixed:** `model/PROJECTION-PLAN.md` (Section 3.6/6) and `model/CROSSREF-AUDIT-PLAN.md` (Section 2) both still name the old `konji_canvassing_rate`/`anais_canvassing_rate`/etc. row IDs in prose — both are point-in-time design/audit documents, not live dependencies (nothing reads them programmatically), and neither was in the edit scope given for this fix; noted here so it isn't a silent gap.
*Basis:* `CONTEXT.md` Follow-Up #11 (existing), owner-confirmed rates, 2026-07-10.

**H-058 · 2026-07-10 · REVISION**
Clarity-only fix to `strategy/strategic-plan.md`'s Target Org Structure table, flagged as a residual issue in H-057's own report: the "Backup lead | Recruited | ... | Redundancy; cross-trained" row and Anais's row both describe backup-lead coverage, and H-057's edit to Anais's row (adding "substitutes as crew lead when Konji unavailable") made the two read as more similar than before, without distinguishing them. Verified against `CONTEXT.md` and the Execution Timeline before touching wording, per instruction, rather than assuming: these are two distinct, real commitments — Anais's occasional, informal crew-lead substitution (an existing arrangement, now priced per H-057) vs. a still-unfilled, formally recruited redundant weekday-lead position (Sequencing Map's "Secure a redundant crew lead," `strategy/execution-timeline.md`'s active backup-lead-search track, and the Owner Transition Plan checklist's unchecked "Backup lead recruited and cross-trained" item). No facts changed. The "Backup lead" row was relabeled "Backup lead (full redundancy — not yet filled)," with its Who cell changed from "Recruited" (ambiguous — could read as already-filled) to "Not yet recruited," and its Function cell now cross-references Anais's row explicitly. Anais's Function cell was lightly reworded to state her substitution is occasional/informal and distinct from the recruited role. Checked the same document's Owner Transition Plan checklist and Headcount Trajectory section for the identical ambiguity, per the instruction not to fix it in one place and leave it elsewhere: both already list the recruited backup and Anais as separate items/entities (e.g. Headcount Trajectory's Q4 2026 line already reads "...+ recruited backup (training) + crew ... + Anais," and the checklist's two items don't share the term "backup lead"), so no further edits were needed there. A Revision Note row added to `strategy/strategic-plan.md`'s own changelog, per its established convention.
*Basis:* Owner instruction (post-H-057 readability follow-up), 2026-07-10.

**H-059 · 2026-07-11 · REVISION**
Resolved Xavier's payout structure (previously "~$1,800, exact figure not yet confirmed" since original repo setup; H-048 confirmed his equity arrangement ended April 2026 but explicitly deferred everything else to a dedicated session — this is that session), per extensive owner-guided scoping. Restructured into two components, calculated separately but paid as a single aggregate amount, per owner instruction. **(1) Equity-buyout component — known, final: $1,014.09.** Owner's framing: 45% of company cash-in-hand at departure, deliberately excluding business assets (the truck) and measured as the balance immediately before the 2026-04-09 $2,200 truck/insurance-reimbursement debit (account 3549/OpEx, memo "ACH Push - Sent By Cyrus Beauvais", confirmed real in `reference/Relay 2026-04-01 #3549.csv`), so the truck purchase doesn't deflate the pool his share is based on. Reconstructed directly from the raw Relay statements, not estimated: balance in account 3549 immediately before the debit = $53.54 (after) + $2,200.00 (debit) = **$2,253.54**; the other 3 active accounts (8735 Income, 3550 Reserve, 8737 Taxes) all show $0.00 at that point (Reserve/Taxes have never had any activity, per H-026; Income's last transaction before 4/9 zeroed it on 4/8). Total base: **$2,253.54**. 45% × $2,253.54 = **$1,014.09** (arithmetic self-check: 2253.54 × 0.45 = 1014.093, rounds to $1,014.09). Written into `model/data/assumptions.csv`'s `xavier_payout` row (value 1800.00 → 1014.09, status TARGET → CONTRACTED), which is the row `build_model.py`'s High-scenario trajectory actually *consumes* (`get_assumption(assumptions, "xavier_payout")`, not just caveat text) — re-ran the full pipeline and confirmed the payout event still fires 2027-05 and the owner's truck-debt repayment still fires 2027-06, unchanged from before this session; only the dollar amounts/ending balances shifted lower. **(2) Revenue-share bonus component — BLOCKED, pending real data.** 10% of net (surcharge-excluded) revenue from the three recurring accounts Xavier cultivated and kept running while the owner was away — Women Life Freedom LLC, Alissa Moore, Martha Picarello ("WMP") — modeled as 4 mutually-exclusive scenarios the owner will choose between once computable: flat 10% over a 3-month or 6-month window, or a declining 15%/10%/5% schedule over the same two window lengths. None can be computed yet: the data layer (`model/data/revenue-invoices.csv`) only extends real invoices through 2026-06-29, and there is no existing per-customer revenue-projection methodology in this repo (only a whole-company YoY rate). Owner explicitly declined to project the unelapsed remainder — decided to wait and compute for real once each window closes. Added 9 new rows to `model/data/assumptions.csv`: `xavier_revenue_share_pct` (10%), `_tier1/2/3` (15%/10%/5%), and the 4 BLOCKED `xavier_revenue_share_wmp_*` scenario rows (all reference-only, not consumed by `build_model.py`, same treatment as Konji's un-volumed canvassing bonuses). **(3) Anacostia Community Boathouse Association — separate, one-time, also BLOCKED.** New real customer found during this session's investigation (not previously in the data layer): first payment $2,332.00, received 2026-05-26 (`reference/Relay 2026-05-01 #8735.csv`, ref "Invoice 48 - ACBA"); CRM lead-source data shows it was registered 2026-05-02 via "Personal Connection," plausible corroboration of the owner's account that Xavier helped source it. Owner considered three framings (flat discretionary amount / 10% of first invoice only / exclude entirely) and chose **10% of the first invoice's net amount only, one-time, not ongoing** — Anacostia does not get the 3-month/6-month treatment WMP gets, since the owner describes Xavier's actual post-departure involvement there as minimal (one meeting, one preliminary workday). BLOCKED because Anacostia isn't yet in `model/data/revenue-invoices.csv` (built from an invoice PDF that predates this account) — only aggregated, dateless CRM exports exist in `reference/`, so the exact net-of-surcharge figure for the first invoice can't currently be isolated. New row `xavier_anacostia_share` added, BLOCKED, $0.00.

**Same-day correction pass (owner-supplied after initial build):** two corrections applied on top of the above, before anything was staged. **Departure date corrected: 2026-04-28 → 2026-04-30** (still owner-recollection, not independently verified — only the specific date changed). **Confirmed the equity-buyout figure is unaffected**: its anchor is the 2026-04-09 truck-reimbursement transaction, not the departure date itself, so $2,253.54/$1,014.09 stand without recalculation. **Revenue-share windows switched from a day-exact anchor to calendar-month boundaries**, starting 2026-05-01 (the first full calendar month after the corrected departure date) — an owner-requested simplification for easier tracking, not incidental rounding. 3-month window now May–June–July 2026, closes **2026-07-31** (was 07-28); 6-month window now May–October 2026, closes **2026-10-31** (was 10-28); declining-schedule tiers re-expressed as full calendar months (3mo: May=15%/June=10%/July=5%; 6mo: May–June=15%/July–Aug=10%/Sep–Oct=5%). Updated the 4 `xavier_revenue_share_wmp_*` rows' `effective_date`/`end_date` (now 2026-05-01/2026-07-31 or 2026-05-01/2026-10-31) and `trigger_condition`/label text accordingly, plus every departure-date and milestone-date citation in `CONTEXT.md`, `strategy/strategic-plan.md`, and `model/build_model.py`'s caveat text (`strategy/execution-timeline.md` had no date citations to correct — checked directly, none found). **WMP actual-to-date partial figure re-derived fresh from the new 2026-05-01 anchor**, not adjusted arithmetically from the old figure, per instruction: direct re-query of `model/data/revenue-invoices.csv` for WLF/Moore/Picarello invoices dated 2026-05-01 through the data layer's max date (2026-06-29) returns **$4,689.08 net across 13 invoices** — identical to the prior 2026-04-28-anchored figure. Confirmed why, not just asserted: a direct check for WMP invoices dated 2026-04-28 through 2026-04-30 (the gap between the old and new anchors) returns zero rows, so no invoice was excluded by narrowing the start date. Arithmetic self-check on the 13 addends: 1,293.60 + 160.00 + 180.00 + 80.00 + 633.33 + 240.00 + 437.90 + 80.00 + 80.00 + 120.00 + 1,043.25 + 261.00 + 80.00 = $4,689.08, confirmed by direct re-addition. Still explicitly partial — no window has closed — remains BLOCKED, not a final answer for either scenario.

**`CONTEXT.md`:** Section 6 item D fully rewritten to describe the two-component structure and both corrections; Xavier's Key People bullet updated; new **Follow-Up #24** added, tracking the two BLOCKED revenue-share milestones (2026-07-31, 2026-10-31) and the separate undated Anacostia data-pull prerequisite. **`strategy/strategic-plan.md`:** Equipment Investment Plan Phase 3 row and Section 9 Financial Assumptions Log row split into two rows (equity-buyout: resolved; revenue-share: BLOCKED), both updated for the date corrections; two Revision Note rows added (initial structure, then the correction pass). **`strategy/execution-timeline.md`:** Demand-Triggered Watch List's Xavier row updated with a pointer to the new figures/structure (light touch — checked directly for date citations needing correction, found none). **`model/build_model.py`:** Plan vs Actual caveat text rewritten to explain the new figure, what's excluded and why, and the corrected milestone dates. **`model/financial-model.xlsx`** (gitignored, not staged) regenerated via the full `model/refresh_all.py` pipeline twice (once per pass) — all 7 stages passed both times, zero impact on any ledger CSV; the High-scenario trajectory was directly inspected after each regeneration and confirmed unchanged in trigger timing (Xavier payout 2027-05, owner truck-debt 2027-06) both before and after the correction pass. **A proactive reminder was proposed, then created in a follow-up cleanup pass (same day, 2026-07-11):** before creating, ran `/schedule list` and confirmed explicitly (not assumed) that zero scheduled routines existed for this repo — the prior pass's own account of itself ("proposed but not yet created") was verified true, not taken on faith; no stale routine existed for the original, now-superseded 07-28/10-28 dates, so nothing needed deleting. Created two one-time Claude Code scheduled cloud-agent routines, both firing 10am ET (14:00 UTC): `trig_014XfF1mHXZjq78LMk64YUZL` (2026-07-31T14:00:00Z, 3-month window) and `trig_01AZ3cQitkVfBisEyynZKcLX` (2026-10-31T14:00:00Z, 6-month window) — each deliberately scoped to only open a GitHub issue on this repo flagging the closed window (citing the specific BLOCKED `assumptions.csv` rows, `CONTEXT.md` Follow-Up #24, and this entry), explicitly instructed not to compute figures, edit any file, or commit anything, per this repo's "never auto-commit" rule. The 6-month routine's prompt additionally checks whether the 3-month routine's outcome already resolved the WMP revenue-share question entirely before opening a (possibly redundant) issue of its own.
*Basis:* Owner-supplied payout structure and departure-date correction, this session (2026-07-11); direct reconstruction from `reference/Relay 2026-04-01 #3549.csv`/`#8735.csv`/`#3550.csv`/`#8737.csv`; direct re-query of `model/data/revenue-invoices.csv`; `reference/Relay 2026-05-01 #8735.csv` and `reference/revenuebycustomer__January_1_2026__July_1_2026.csv`/`services__...csv`/`source__...csv` for the Anacostia finding; `CONTEXT.md` H-048 (prior deferral) and Follow-Up #24 (new); `HISTORY.md` H-026 (Reserve/Taxes zero-balance precedent).

---

**H-060 · 2026-07-12 · REVISION**
Two Follow-Ups addressed in a status-check-driven session (read-only status check first, then this action pass). **(1) Follow-Up #21 (`cash_starting_balance` re-verification) — attempted, found no new data, figure left unchanged.** The owner reported no new Relay data had been pulled since the $1,225.33/2026-07-06 figure; this was verified directly against the files rather than taken as given, per the entire point of this Follow-Up. Every `Relay*.csv` file in `reference/` was listed and checked: the latest transactions across all four active accounts (3549, 8735, 3550, 8737) are dated **2026-07-06** — the two `"Relay (Partial) 2026-07-01 to 2026-07-06"` pulls for 3549 (head row: 7/6/2026, balance $1,225.33) and 8735 (head row: 7/6/2026), with 3550 and 8737 carrying zero rows throughout (continuity with H-026's $0-balance finding). This is identical to the date already backing `reference/cash-consolidated-2026-07-06.csv` and `model/data/assumptions.csv`'s `cash_starting_balance` row. No newer or partially-incorporated pull exists anywhere in `reference/`. Since no new source data exists, the figure (value **$1,225.33**, as-of date **2026-07-06**) was **left exactly as-is** — re-stamping today's date without a new consolidation would misrepresent the check as a fresh pull when none was possible, masking the figure's real 6-day staleness rather than surfacing it. `CONTEXT.md` Follow-Up #21 updated with a dated note recording that this attempt was made and its outcome (no new data), leaving the underlying "does this need a standing update procedure" question exactly as open as before. **(2) Follow-Up #15 (billed-vs-collected tension) — closed, on the practical question only.** Per the preceding status check's finding: this Follow-Up's underlying tension (every invoice showing Total Due = 0.00 in the CRM, against a real $12,726.01 billed / $10,973.57 collected gap) is already resolved in practice by this repo's own D5a architecture — `model/match_payments.py` (H-044) reconciles collected-cash events against actual settled Relay bank deposits, never against the CRM's own "Total Due" field, so the model's collected-cash figure was never dependent on trusting the CRM's self-reported accounting. **Explicitly not closed as part of this:** the original, separate claim of *why* the CRM's own fields show this pattern (a CRM timing/recording artifact was always the plausible-but-unverified guess) remains genuinely unverified and is now stated as such directly in the Follow-Up text, rather than implicitly resolved by association. `CONTEXT.md` Follow-Up #15 marked RESOLVED with both claims distinguished explicitly, citing Section 4/Section 6 item F and `model/match_payments.py`/H-044 as the mechanism.
*Basis:* Direct listing and head-row inspection of every `reference/Relay*.csv` file, this session (2026-07-12); `reference/cash-consolidated-2026-07-06.csv`; `CONTEXT.md` Section 4/Section 6 item F (D5a architecture); `HISTORY.md` H-026 (Reserve/Taxes zero-balance precedent), H-039 (prior consolidation), H-044 (`match_payments.py` build).

---

**H-061 · 2026-07-12 · DATA**
Owner supplied a Stripe "balance transaction history" export (74 rows: 40 `charge`, 33 `payout`, 1 `refund`, 2025-07-07 through 2026-07-03) — Stripe's own transaction record, independent of both Relay and the Homeworks CRM. Filed to `reference/` as an immutable source (`stripe-balance-history-2025-07-07-to-2026-07-03.csv`), with a new "Stripe balance-history export" section added to `reference/README.md`, per this repo's established new-external-source pattern (same treatment as the Gusto payroll onboarding, H-048). **Per instruction, this pass is investigation and filing only** — Follow-Up #17 was not resolved, no `CONTEXT.md` conclusion was edited, and no ledger/pipeline code was built; findings are recorded here so a future session can act on them deliberately. **Whether this file needs its own refresh procedure (like `PAYROLL-UPDATE.md`) is left flagged, not decided** — noted in the new README section, since no pipeline script currently reads it.

**Investigation findings, comprehensive (not a sample) across all rows unless noted:**

(1) **All 32 non-refund payout rows already reconcile exactly to existing ledger rows.** Every payout's net amount matches, by date and dollar amount, an existing `ledger-revenue.csv` "unattributed [aggregated]" Stripe payment row (0 unmatched of 32) — full confirmation that H-044's Stripe-side ledger population is complete and correct at the aggregate level.

(2) **All 40 charges reconcile to the account's real 3.9%+$0.30 Stripe rate.** Independently recomputing each charge's fee from its `Amount` via the formula and comparing to the recorded `Fee`: 36 of 40 match exactly; the remaining 4 deviate by exactly $0.01 each, randomly distributed across different invoices and dates (standard per-charge rounding, not a pattern). `Net = Amount - Fee` holds exactly on all 40/40.

(3) **A real, quantified error found in the existing ledger's fee-batching: total $2.56 undercount, not evidence of a hidden fee.** `model/match_payments.py` (H-044) had no per-charge visibility — it back-solved one fee figure per payout day, applying the formula's $0.30 flat component once per day. This export shows 7 of the 32 payouts actually batch 2–3 individual charges (each carrying its own $0.30), so the true summed per-charge fee exceeds the ledger's single back-solved figure whenever a payout batches more than one charge. Quantified across the full history: single-charge-batch rounding noise = **$0.05** (7 payouts, ±$0.01 each) + multi-charge-batch undercount = **$2.51** (7 payouts, 2–3 charges each) = **$2.56 total** (arithmetic self-check: 0.05 + 2.51 = 2.56, confirmed). Not built or corrected in this pass — flagged for a future fix, the same way H-048 flagged the bounced-charge bug before deciding whether/how to correct it.

(4) **The `stripeProcessingFee`/`customerProcessingFee` (metadata) columns are Homeworks-internal estimate fields with no real cash effect, and they do not reflect this account's real Stripe rate.** `customerProcessingFee (metadata)` is 0 on all 38 populated rows — a defined-but-unused field, never actually charged to a customer. `stripeProcessingFee (metadata)` is systematically **lower** than the real `Fee` on all 38 populated rows (average effective rate ≈2.99%, vs. the real, verified 3.90% rate — roughly a full percentage point apart, confirmed by linear regression across all 38 rows, max residual $0.0046) — evidence Homeworks' own invoice-side fee estimate uses a generic, lower assumed rate, not this account's actual negotiated 3.9%+$0.30. This has no bearing on real revenue or cash (Amount/Fee/Net, the fields that actually settle, are unaffected) and is unrelated to Follow-Up #17's Homeworks-*direct* card-channel question (see finding 7).

(5) **The $38.16 Stripe debit (Follow-Up #18) is now fully explained: a genuine partial refund, not a chargeback.** The file's one `refund` row (-$38.16, 2026-04-07, "REFUND FOR CHARGE (Invoice#40)") is a partial refund against the $254.40 charge for Invoice#40 (originally net $244.18 after the standard fee). Stripe then pulled the $38.16 back from the connected bank account the next day (2026-04-08) to cover it, appearing in the balance history as an unusual **positive** `payout` row (`Amount: +38.16`, the only positive payout among all 33) — this is the exact transaction Relay recorded as the lone Spend-type Stripe debit that Follow-Up #18 could not previously explain. **Not written into `CONTEXT.md` in this pass, per instruction** — flagged here as a resolved-in-fact finding for the owner to fold in explicitly.

(6) **Independent cross-validation of two existing figures, both confirmed exact.** (a) For all 40 charges, `invoice gross (from model/data/revenue-invoices.csv) + tip (metadata) == Stripe Amount`, exactly, to the penny, 40/40 — a full independent confirmation of the existing revenue ledger's invoice-gross figures from a wholly separate source. (b) Summing `tip (metadata)` across all 40 charges gives **$424.92** — an exact match, to the penny, to the existing `ledger-revenue.csv` tip total (Follow-Up #14, 19 rows, all payment methods). Since this Stripe file covers card payments only, the exact match reveals every tip this business has ever received came through a card payment — a new, previously-undocumented fact, surfaced as a byproduct of the cross-check, not asserted from absence.

(7) **No evidence of a distinct Homeworks-direct settlement channel; consistent with the prior status check's finding.** Every `paymentMethodType (metadata)` value populated is `card` (38/38 populated rows); every charge's `Transfer` field points to a `po_...` payout ID that settles through the same Stripe rails already confirmed in Relay. This file adds no new payee/channel signature beyond Stripe — it does not change the prior conclusion that no live, currently-uncaptured Homeworks-direct card fee exists in the data (this session's earlier status-check finding, pending Follow-Up #17's actual owner-supplied schedule).

(8) **Every invoice number referenced (44 distinct) exists in `model/data/revenue-invoices.csv`** — no data-layer gap. **The `Transfer`/`po_` linkage lets all 32 "unattributed [aggregated]" Stripe payment rows in `ledger-revenue.csv` be re-attributed to specific invoices** (already demonstrated in findings 1 and 6a above) — a genuine future data-quality improvement this file makes possible, not built in this pass.

*Basis:* `reference/stripe-balance-history-2025-07-07-to-2026-07-03.csv` (this pass, new file); full-file programmatic reconciliation against `model/data/ledger-revenue.csv`, `model/data/ledger-overhead.csv`, and `model/data/revenue-invoices.csv`, this session (2026-07-12); `HISTORY.md` H-044 (existing Stripe ledger population), H-048 (filing-convention precedent), Follow-Up #14 (tip total), #17 (card-fee schedule, not resolved here), #18 ($38.16 debit, not written up here).

---

**H-062 · 2026-07-12 · REVISION**
Investigated an AI chatbot's claim ("1% Homeworks fee, deducted automatically per transaction, same mechanism as Stripe's own fee") against H-061's Stripe export, then acted on H-061's findings for Follow-Up #17/#18 and the $2.56 fee-batching undercount. **The chatbot itself was never treated as a source** — every claim was tested against the actual export.

**(1) The "separate, stacked deduction" reading of the 1% claim is not supported by the data.** Re-verified fresh (not recalled from H-061): `Destination Platform Fee` is empty on all 74 rows of every type in the export — the exact column Stripe's own schema would use to carry a platform-level cut, unused throughout. `Net = Amount − Fee` holds exactly, zero residual, on all 40 charges (`Amount − Fee − Net = $0.00` on every row, re-verified directly). There is no room in the real, settled transaction chain — from customer-facing charge through to the actual bank deposit already independently confirmed against Relay (H-061 finding 1) — for a second deduction to occur anywhere.

**(2) The "blended into one number" reading cannot be ruled out from this file, and is not asserted either way.** A single reported 3.9%+$0.30 `Fee` is arithmetically indistinguishable between "the account's true, whole negotiated Stripe rate" and "a lower true Stripe rate (e.g. 2.9%) plus an undisclosed Homeworks markup, pre-combined before ever being reported" — both produce identical numbers here. Checked whether the data layer holds any independent Homeworks-reported net-of-fees figure to test against Stripe's actual `Net` (the chatbot's suggested "Finances → Payments page" comparison): `model/data/revenue-invoices.csv`'s `net_subtotal` is the pre-surcharge service price (an unrelated concept, confirmed via `net_subtotal + surcharge == gross` on 68/68 invoices) and `amount_paid` equals `gross` exactly on 68/68 invoices — no field anywhere in the repo represents a Homeworks-computed post-card-fee net. This specific comparison is **not checkable from files already in this repo** and requires the owner to pull that report directly from Homeworks (or check Stripe's own dashboard for this connected account's raw rate) — recommended as the concrete next step, not closed on inference.

**(3) Follow-Up #17 updated to reflect both findings precisely, left open (not closed either direction).** `CONTEXT.md` now states the stacked-deduction claim is ruled out, states plainly what remains genuinely unresolvable from existing files, and names the two direct verification paths ((a) Stripe's own dashboard rate for this account, (b) a Homeworks billing/merchant-agreement statement) rather than accepting or rejecting the chatbot's claim without evidence.

**(4) Follow-Up #18 ($38.16 debit) resolved in `CONTEXT.md`**, per H-061 finding 5 — a genuine partial refund on Invoice#40, not a chargeback. No ledger correction needed; `model/match_payments.py`'s existing exclusion of this row from revenue-payment events was already the correct treatment.

**(5) The $2.56 fee-batching undercount (H-061 finding 3) fixed at the code level, not just the data — the durable-fix pattern this repo requires (`CLAUDE.md`'s model-update procedure).** A first attempt hand-corrected the 14 affected rows directly in `model/data/ledger-overhead.csv`; re-running `model/refresh_all.py` silently reverted every correction, because `model/match_payments.py` regenerates these exact rows from Relay data on every run — a hand-edit to a script-derived file is not durable, the same category of mistake `CLAUDE.md` already prohibits for the generated workbook. **Corrected by fixing `model/match_payments.py` itself**: added `load_stripe_fee_lookup()`, which sums true per-charge fees from `reference/stripe-balance-history-*.csv` by payout (keyed on date + net amount) and — when a match exists — uses that true sum instead of the single back-solved-per-day estimate; the original 3.9%+$0.30 back-solving formula is kept as an explicit fallback for any future payout not yet covered by a Stripe export pull, so the script never fails outright, it only reverts to the same estimate it always used before this fix existed. Re-ran the full 7-stage pipeline twice consecutively: `[stripe-fee] 32/32 fee rows use true per-charge sums ... 0 fall back`, both runs, identical MD5 on `ledger-overhead.csv` and `ledger-revenue.csv` across both runs (idempotent, confirmed not asserted). New Stripe fee total: **$412.52** (was $409.96; arithmetic self-check: $409.96 + $2.56 = $412.52, confirmed), across the same 32 rows. `model/financial-model.xlsx` regenerated (gitignored, not staged); no other ledger file or figure affected — only the 32 Stripe `payment-processing-fee` overhead rows' `unit_rate`/`amount`/`source` fields changed.

*Basis:* `reference/stripe-balance-history-2025-07-07-to-2026-07-03.csv`; direct re-verification of `Destination Platform Fee` and `Amount − Fee − Net` across all 40/74 rows, this session (2026-07-12), not recalled from H-061; `model/data/revenue-invoices.csv` schema check (`net_subtotal`, `amount_paid` fields); `model/match_payments.py` (`load_stripe_fee_lookup`, new function, this pass); two consecutive full `model/refresh_all.py` runs, MD5-compared; `HISTORY.md` H-044, H-048, H-061; `CONTEXT.md` Follow-Up #17 (updated, left open) and #18 (resolved).

---

**H-063 · 2026-07-12 · REVISION**
Follow-Up #17 fully resolved, closing the composition question H-062 had explicitly left open. **Owner-confirmed, independently verified (2026-07-12) — not sourced from the AI chatbot testimony H-062 investigated:** Stripe's true, raw processing cost for this account is 2.9% + $0.30/charge; Homeworks adds a 1% platform markup on top for any plan tier below Enterprise (this account is on Growth). 2.9% + 1% = 3.9% (arithmetic self-check: confirmed), exactly matching the combined rate this ledger has used since H-044 — **the rate itself was never wrong, and no dollar figure changes as a result of this entry.** Re-verified directly, not assumed: the 32-row Stripe `payment-processing-fee` total in `model/data/ledger-overhead.csv` remains **$412.52** (unchanged from H-062's correction), and `git diff` confirms zero data-file changes this pass — this entry is a documentation/composition resolution only.

**Distinguishing the two sources, explicitly:** H-062 tested the chatbot's claim against the Stripe export and found the *stacked-second-deduction* reading unsupported, while stating plainly that the *blended-single-rate* composition (whether 3.9% was 100% Stripe or a Stripe+Homeworks blend) could not be determined from the export alone — H-062's own text named exactly two paths to close it: the account's raw Stripe-dashboard rate, or a direct Homeworks billing/merchant-agreement statement. The owner's verification this session is that second path (or its equivalent), not a continuation of the chatbot exchange; the chatbot's own prior (unverified, third) answer happened to land on the same 2.9%+1% split, but that coincidence is not why this Follow-Up is closing — `CONTEXT.md`'s Follow-Up #17 text now states this distinction directly, per instruction, so a future reader doesn't mistake H-062's chatbot investigation for the actual resolving source.

**New open question surfaced, not decided or built here:** confirming the composition (not just the combined total) means the ledger's single `category="Stripe", subcategory="payment-processing-fee"` row is now *knowingly* blending two distinct cost drivers — true third-party processing cost (~2.9%+$0.30) and a plan-tier-driven Homeworks platform markup (~1%). Presented in `CONTEXT.md` as two options for the owner's sign-off: (a) leave it blended (no functional loss, since the combined rate was always applied correctly — only cost is that Stripe-cost vs. Homeworks-cost can't be reported separately), or (b) split into two ledger rows per Stripe payment event going forward, useful mainly for a future Enterprise-plan cost/benefit evaluation (eliminating the 1% is one of that plan's stated advantages). Recommendation stated as (a) unless the owner wants that visibility specifically — (b) would add real complexity to `model/match_payments.py`'s fee logic for a split that changes no total. **Neither option implemented in this pass; pending owner choice.**

*Basis:* Owner verification, this session (2026-07-12); `HISTORY.md` H-044 (original combined-rate confirmation), H-061 (Stripe export filing), H-062 (chatbot-claim investigation, the two paths to closure it named); `model/data/ledger-overhead.csv` (re-checked, confirmed unchanged at $412.52); `CONTEXT.md` Follow-Up #17 (resolved, with the categorization question presented for sign-off).

---

**H-064 · 2026-07-12 · REVISION**
Comprehensive retrospective + cross-reference audit for the Stripe balance-history integration (H-061–H-063), mirroring the Gusto payroll retrospective pattern (H-048). Part A (read-only) below; Part B (cross-reference audit) and its fixes follow.

**Part A — retrospective: what would be different if this export had existed from the start of Sequencing Step 3.** Re-read `model/PHASE7-PLAN.md` Section 3(b) in full (Findings 1–7, Approach A, the original tiered design) fresh, not from memory.

(a) **Tier 3 ("aggregated") would likely never have existed as a separate tier.** Stripe's own `Description` field cites invoice numbers directly (e.g. "Invoice#74") — structurally the same citation signal Finding 1 already uses for tier-1 matching; tier 3 exists only because that citation lives in Stripe's own records, not in Relay's `Reference` field, which carries none of it. Quantified, not estimated: of the 32 `[aggregated]` Stripe payment events, **all 32 (100%)** can be re-attributed to specific invoice(s) using the export's `Description`/`Transfer` linkage — 25 map to exactly one invoice, 7 map to a batch of 2–3 invoices (would need to split into multiple rows, mirroring the existing tier-2 "batch" pattern).

(b) **Finding 3's original "~71% attributable / 29.11% aggregated" framing is superseded, not by correction but by scope.** That figure was a CRM-derived cross-check (`services__..._1.csv`) for a narrower Jan–Jul 2026 window. Recomputed directly against the full ledger (`model/data/ledger-revenue.csv`, all 58 payment-event rows, $30,023.25 total, 2025-03 through 2026-07): currently **56.81%** ($17,055.05) directly attributed, **32.83%** ($9,856.20) Stripe-aggregated, **10.37%** ($3,112.00) genuinely unmatched non-Stripe rows (arithmetic self-check: 17055.05 + 9856.20 + 3112.00 = 30023.25, confirmed). If all 32 Stripe rows were converted, attribution would rise to **89.63%** ($26,911.25 = $17,055.05 + $9,856.20, confirmed), leaving only the unrelated 10.37% unmatched.

(c) **The fee-computation design (H-062) is exactly what Sequencing Step 3 would have built from day one, not a later correction to a different original design.** The 3.9%+$0.30 back-solving formula was always a stand-in for missing per-charge visibility (`model/PHASE7-PLAN.md` Finding 7), never a deliberate choice on its own terms. No other design element would have differed — the payment event's `amount` staying the actual net deposit (never a back-computed gross) was always correct per D5a, independent of how the paired fee is sourced.

(d) **Nothing else in Sequencing Step 3's original scoping assumed aggregated-only Stripe data.** Finding 6's two-era routing rule (OpEx pre-2026-07-07, Income from) is a Relay-side bank fact, unaffected by per-charge Stripe visibility. Findings 1/2/4/5 (citation matching, amount-lag matching, exclusions, name-field disagreement) are all unrelated to Stripe.

**Reconciliation gate (Step 3, `build_model.py`'s Check 2) — checked, does not need updating.** Check 2's stated scope ("revenue payment classification completeness... 58 classified + 36 exclusions, zero residual, per H-044") is about payment-event *existence*, not fee-dollar accuracy — unaffected by H-062/H-063's fee-sourcing change, since the classification counts themselves didn't change. Noted, not built (new functionality, not a stale-content fix, out of this pass's scope): the Reconciliation tab currently has no line surfacing the Stripe fee true-sum/fallback coverage split — a candidate future addition mirroring Check 3/4's informational style, not a gap in what Check 2 already claims.

**Part B — cross-reference audit.** Grepped the full repo for "3.9%", "2.9%", "Homeworks fee", "stripeProcessingFee", "aggregated", "unattributed" outside `CONTEXT.md`/`HISTORY.md`. Hits in `reference/PAYROLL-UPDATE.md` and `model/populate_labor_from_payroll.py`/`populate_step4.py` were unrelated false positives (payroll/materials "aggregated" usage). Genuine hits and their resolution:

- **`reference/README.md`'s Stripe section** — "refresh cadence, flagged not decided" resolved: `reference/STRIPE-UPDATE.md` written (new file), following the established `CATALOG-UPDATE.md`/`REVENUE-UPDATE.md`/`PAYROLL-UPDATE.md` pattern exactly, since this is now a confirmed load-bearing pipeline input (H-062) — not a novel case warranting a different treatment. One genuine difference from the other three procedures, stated explicitly in the new file: this refresh has **no fail-closed gate** (deliberate — the fallback formula always produces a valid result), so it's a precision refresh, not a correctness one.
- **`model/PHASE7-PLAN.md`** — Finding 3 and the Finding-7-adjacent "Built" paragraph annotated with dated "Update, 2026-07-12" notes, following this document's own established inline-correction convention (already used for the labor/capital sections) — pointing to H-061/H-062/H-063/H-064 and this session's re-quantified attribution figures, without rewriting the original findings.
- **`model/match_payments.py`'s docstring** — tier-3 description updated to note per-charge attribution is now possible via the Stripe export (not from Relay data alone) but not implemented, pointing to the new Follow-Up #25 for scope.
- **A real, previously-unflagged latent bug found and fixed while reviewing `load_stripe_fee_lookup()` for this audit:** no deduplication existed against overlapping export windows — a future `stripe-balance-history-*.csv` refresh with any date overlap against the current file would have double-counted every charge/payout appearing in both, exactly the class of bug Relay's own loader already guards against. Fixed by deduplicating on Stripe's own `id` column (a true unique identifier, more robust than Relay's heuristic tuple-matching) before summing. Re-ran the full pipeline after the fix: `[stripe-fee] 32/32 ... 0 fall back`, unchanged from before the fix (`git status model/data/` empty) — confirms the fix is purely defensive, zero effect while only one export file exists.
- **`CONTEXT.md`'s Follow-Up for the H-061 finding 8 re-attribution opportunity — checked and found not to actually exist.** H-061 recorded the finding in `HISTORY.md` only; it was never promoted to a numbered `CONTEXT.md` Follow-Up. Created **Follow-Up #25** now, informed directly by Part A's quantified findings above (32/32 re-attributable, 89.63% would-be attribution) — explicitly scoped as a future rebuild of `match_payments.py`'s tier-3 logic, not built in this pass.
- **`model/CROSSREF-AUDIT-PLAN.md`** — a new "Addendum — 2026-07-12" section appended (not editing the original H-055-dated sections, preserving that document's own append-only convention) cataloging: the now-stale `match_payments.py` read/write-contract row (missing the new Stripe-export dependency, and genuinely gate-free unlike every other row), and Section 2.7's now-superseded #17/#18 statuses (accurately reflected their state as of H-055, not a drift finding) — so RP1's audit doesn't have to rediscover either from scratch.

**Nothing large was built.** Per instruction, the 32-row payment-event re-attribution itself stays fully deferred (`CONTEXT.md` Follow-Up #25) — everything fixed in Part B is a documentation correction, a pointer annotation, a new (but pattern-established, not novel) procedure file, or the one small, low-risk dedup-by-id code fix.

*Basis:* `model/PHASE7-PLAN.md` Section 3(b) (re-read fresh, this session); direct computation against `model/data/ledger-revenue.csv` (all 58 payment rows) and `reference/stripe-balance-history-2025-07-07-to-2026-07-03.csv` (payout-batch sizing, re-verified fresh, not recalled from H-061); `model/build_model.py`'s Reconciliation tab (Check 2, inspected); full-repo grep for the six specified terms; `model/match_payments.py` (`load_stripe_fee_lookup`, dedup fix); `reference/STRIPE-UPDATE.md` (new); `CONTEXT.md` Follow-Up #25 (new); `model/CROSSREF-AUDIT-PLAN.md` (Addendum, new section); two full `model/refresh_all.py` runs post-fix, `git status` confirming zero ledger-data drift.

---

**H-065 · 2026-07-12 · REVISION**
Five-point follow-up verification pass on the Stripe integration, closing gaps H-064's retrospective didn't explicitly cover.

**(1) Projection-layer sensitivity to the $2.56 fix — checked directly, not assumed from the small dollar size. A real, small, fully-reconciled shift found.** Re-ran `model/build_model.py` and compared the High-scenario trajectory against the last explicitly confirmed facts (`HISTORY.md` H-053, predating H-062's fix): buffer-reached/Xavier-payout month **May 2027 — unchanged**; owner-truck-debt month **June 2027 — unchanged**; low-point month **October 2026 — unchanged**. **The low-point balance did move: $1,338.17 (H-053) → $1,336.55 (this check), a $1.62 decrease.** Root cause isolated, not just observed: 8 of H-062's 14 corrected Stripe-fee rows fall within the trailing-3-month window (2026-04/05/06) the High scenario's flat overhead trend is computed from — 2026-04 +$0.01, 2026-05 +$0.33, 2026-06 +$1.28 (arithmetic self-check: 0.01+0.33+1.28 = 1.62, confirmed), raising the trend from ~$803.02/mo to the now-generated **$803.56/mo**, a **+$0.54/mo** increase (1.62÷3 = 0.54, confirmed). Applied across the 3 months from the projection's 2026-07 start to the October 2026 low point: 3 × $0.54 = $1.62 — exactly matching the observed balance shift, closing the loop. This is the first time this ripple was explicitly verified; H-062/H-063/H-064 regenerated the workbook each time but none checked the trajectory's key facts against a stated prior baseline the way this pass did. No other month's key fact changed.

**(2) `reference/fixed-overhead.md`** — confirmed its Homeworks line describes only the $299/mo CRM subscription (current contract; no stale $139/mo Copilot-era figure present). Added a clarifying bullet distinguishing this fixed monthly cost from the separate, transactional 1% Homeworks platform markup on card payments (`HISTORY.md` H-063) — the two share a name but are structurally unrelated (contract-term software cost vs. a per-charge cost scaling with card-paid revenue volume), and the markup is not part of this file's $696.58/mo total.

**(3) `README.md`'s "Model data-refresh pipeline" section — confirmed stale, same gap H-064's `CROSSREF-AUDIT-PLAN.md` addendum already caught once.** Step 3 of that section described `match_payments.py` as reading only Relay bank statements, omitting the `reference/stripe-balance-history-*.csv` dependency built in H-062. Updated to match what the addendum already documented, so the gap isn't left uncorrected in the one place a reader is actually likely to look first; also added `reference/STRIPE-UPDATE.md` to the refresh-procedure pointer list alongside the other three `*-UPDATE.md` files.

**(4) `model/build_model.py` grepped for any hardcoded `$409.96`/`$412.52` Stripe-total figure** — none found; the Reconciliation tab and every print statement compute from the live ledger, never a stored constant. Confirmed clean, no fix needed. (A repo-wide check found `$409.96` referenced only in `HISTORY.md`'s own H-062 before/after narrative — correct, expected, not a stale live figure.)

**(5) `strategy/strategic-plan.md`'s Financial Assumptions Log "Fixed monthly costs ~$696.58" row — confirmed correctly scoped, no change needed.** Sourced to `reference/fixed-overhead.md`, whose own component sum (127.00+33.75+45.33+166.50+299.00+25.00 = 696.58) never included Stripe. Grepped the full document for every "Stripe"/"Homeworks" mention — zero conflation found; every Homeworks reference is CRM-systematization language (rate matrix, lead tracking, time tracking, automations), never the card-processing fee.

*Basis:* Fresh `model/build_model.py` run and `model/financial-model.xlsx` inspection via `openpyxl`, this session (2026-07-12); `HISTORY.md` H-053 (original trajectory baseline, predating the fix) and H-062 (the 14 corrected rows and their dates/deltas); `reference/fixed-overhead.md` (edited) and its own component-sum check; `README.md` (edited); `model/build_model.py` and repo-wide grep for `409.96`/`412.52`; `strategy/strategic-plan.md` (grepped, unedited).

---

**H-066 · 2026-07-13 · REVISION**
Four-part owner-directed session: filed an updated Homeworks catalog export (tax-rate correction), added a Virginia sales-tax-registration sequencing item, resolved Follow-Up #10 (sales-tax/surcharge treatment), and logged a new Follow-Up for an upcoming Stripe-fee customer-passthrough policy.

**(1) Catalog refile — `reference/service-catalog-2026-07-13.csv`, superseding the 2026-07-09 snapshot.** Owner corrected the catalog's tax settings to match Virginia's actual legal basis: previously 20 of 22 catalog rows carried 6% tax (a blanket-tax practice); now only the 3 `item`-kind rows (`Bagged Mulch`, `Landscaping Blocks (stone)`, `New Plants`) carry 6%, and all `service`-kind (labor) rows carry 0% — confirmed directly against the actual export (not assumed from the owner's description), including catching a discrepancy the description didn't mention: **"Weeding Maintenance" was discontinued**, dropping the row count from 22 to 21. Owner-confirmed this is a genuine merge into the existing "Weeding" item (an unused separate name for the same recurring-weeding concept), not data loss — resolved the same way every other historical catalog rename has been handled: added `Weeding Maintenance,Weeding` to `model/data/service-name-map.csv` as a legacy-name alias (not a rename or reclassification), and removed the now-orphaned `Weeding Maintenance,service` row from `model/data/catalog-type-map.csv` (21 rows remain: 3 `item` + 18 `service`). Updated `reference/README.md`'s active-snapshot pointer and `reference/CATALOG-UPDATE.md`'s "Known limitation" section (this was its first real occurrence, not hypothetical). Ran the full pipeline (`model/parse_invoices.py` → `model/build_ledger_revenue.py` → `model/refresh_all.py`, all 7 stages) — all gates passed. **Confirmed no historical dollar figure changed**: `git diff` shows exactly 6 rows changed in `model/data/revenue-line-items.csv` (the `service` column only, `Weeding Maintenance`→`Weeding`; `service_raw`, `line_net`, `line_surcharge`, `line_gross` all untouched) and exactly 12 rows changed in `model/data/ledger-revenue.csv` (the `subcategory` column only, same rename, 6 `service` + 6 `surcharge` rows — matches the 6 line items exactly, 1:1); `model/data/revenue-invoices.csv` and `model/data/ledger-overhead.csv` show zero diff. This is a going-forward catalog config change, not a retroactive correction, exactly as expected.

**(2) `strategy/execution-timeline.md` — new sequencing item, Dec 16–31 bucket.** Added an `[ANCHOR]` item to confirm Virginia sales-tax registration/permit status for the 3 now-taxable items, deadline **2026-12-31** (owner-confirmed as the applicable filing-year boundary for this business — not defaulted to calendar year without checking; no fiscal-year statement exists anywhere else in the repo, so this was asked rather than assumed). Cross-referenced to `CONTEXT.md` Follow-Up #26 (new, below).

**(3) Follow-Up #10 (`CONTEXT.md`) — RESOLVED.** Historical treatment: re-verified the full-history surcharge total fresh against the ledger rather than reusing a prior figure — **$1,556.47 across 148 `category="surcharge"` rows** in `model/data/ledger-revenue.csv`, cross-checked against `model/parse_invoices.py`'s own independently-computed summary output (`Surcharge: $1,556.47`) — two independent computations agree to the penny. This is **not** the same figure as the $701.43 previously cited in `CONTEXT.md` item F, and that's not a discrepancy to resolve: item F's $701.43 was always explicitly scoped to the narrower Jan–Jul 2026 collected-vs-billed reconciliation window, not the full ~13-month invoice history (2025-06 through 2026-06) this resolution needed — confirmed by re-reading item F's own text before use, not assumed compatible. Under Virginia's rule, labor was never taxable, so almost none of this retained surcharge was ever a legitimate tax collection — it is now recorded as **permanent retained revenue, not a liability**, closing the original "retained revenue vs. pass-through liability" question. **Strength of the "no remittance obligation" conclusion, checked rather than assumed:** queried `model/data/revenue-line-items.csv` directly to confirm what fraction of the $1,556.47 sits on the 3 item-kind (material) catalog rows — **0%**, re-confirming H-038's "zero billed revenue on materials" finding fresh rather than citing it from memory. Breaking the total down by `kind` surfaced a caveat worth stating precisely rather than overclaiming: **42.7% ($665.20, 88 rows)** sits on cleanly-mapped `service`-kind lines (conclusion fully complete for this portion); the remaining **57.3% ($891.27, 64 rows)** sits on bundled/unmapped lines that Follow-Up #6/#13 already established cannot be decomposed into item vs. service — so the conclusion is strong (no material item has ever appeared as a distinct billed line anywhere) but not independently verifiable for that bundled portion. Added to Follow-Up #10's text rather than left only in this log entry, since it materially qualifies the strength of the resolution's central claim. Forward-looking treatment ties to (1) above. One dependency flagged: Follow-Up #26 (VA registration) must close before the newly-correct forward collection can be properly remitted.

**(4) Follow-Up #26 (new) and #27 (new), `CONTEXT.md`.** #26 documents the VA sales-tax registration action item (cross-referenced from (2) and from Follow-Up #10's resolution), not urgent per owner instruction, deadline 2026-12-31. #27 documents the new Stripe-fee customer-passthrough policy (customer reimburses the 2.9%+$0.30 Stripe-proper portion only; the 1% Homeworks markup stays an absorbed cost) with five explicit open questions for the owner, one of which was investigated (not guessed) before being left open: `reference/stripe-balance-history-2025-07-07-to-2026-07-03.csv`'s `customerProcessingFee (metadata)` column, re-checked directly against the export this session, is unused across all 40 charge rows (38 carry literal `0`, 2 are blank — exactly matching H-061's original finding), but that export predates this policy and cannot confirm whether the field is what will populate once real transactions occur under it. No ledger logic, `assumptions.csv` row, or schema change was built for #27, per explicit instruction — investigation and documentation only.

Also updated in this pass for cross-reference accuracy (structured-doc edits triggered the standing cross-reference check): `CONTEXT.md` Follow-Ups #4 and #6, whose "22 current/22-item catalog" phrasing is now stale — appended brief dated notes pointing to the new 21-row count rather than rewriting their original H-038-dated findings.

*Arithmetic self-check:* $1,556.47 (148-row direct ledger sum) = $1,556.47 (parse_invoices.py's independent summary print) — confirmed matching, not merely asserted. Catalog row-count arithmetic: 22 (prior) − 1 (Weeding Maintenance discontinued) = 21 (current) — confirmed against a fresh `csv.DictReader` count of the actual file, not the diff alone. `catalog-type-map.csv`: 22 (prior) − 1 (removed row) = 21 = 3 `item` + 18 `service` — confirmed by direct count. Ledger-diff row counts: 6 line items × 1 field changed = 6 diff lines in `revenue-line-items.csv`; 6 line items × 2 ledger rows each (service + surcharge) = 12 diff lines in `ledger-revenue.csv` — both confirmed against the actual `git diff` output, not estimated. Surcharge-by-`kind` breakdown: $665.20 (88 rows, `service`) + $891.27 (64 rows, bundled/unmapped) + $0.00 (0 rows, `item`) = $1,556.47, confirmed by direct re-addition, matching the 148-row ledger total exactly. This breakdown was computed across all 152 rows in `model/data/revenue-line-items.csv` (88 + 64 = 152, matching that file's own known row count), not just the 148 with nonzero surcharge — the 4-row gap between 152 and 148 is real and already explained by a prior finding, not a new discrepancy: checked directly, exactly 4 rows carry `$0.00` line_surcharge (3 "Bagging" lines + 1 "Lawn Maintenance" bundle line, all `surcharge_pct=0.0`) — precisely the same 3+1 = 4-row inconsistency already documented in Follow-Up #16, re-confirmed here rather than assumed. 665.20 ÷ 1556.47 = 42.74% ≈ 42.7%; 891.27 ÷ 1556.47 = 57.26% ≈ 57.3% — both percentages confirmed by direct division, not estimated.

*Basis:* `reference/service-catalog-2026-07-13.csv` (new) vs. `reference/service-catalog-2026-07-09.csv` (diffed directly, both via `csv.DictReader` and raw `diff`); `model/data/service-name-map.csv` and `model/data/catalog-type-map.csv` (edited); `reference/README.md` and `reference/CATALOG-UPDATE.md` (edited); full `model/refresh_all.py` run this session (2026-07-13); `git diff` on `model/data/revenue-line-items.csv`, `model/data/ledger-revenue.csv`, `model/data/revenue-invoices.csv`, `model/data/ledger-overhead.csv`; `model/data/ledger-revenue.csv` direct query (148 surcharge rows) vs. `model/parse_invoices.py` console output; `CONTEXT.md` item F (re-read for scope, unedited) and Follow-Ups #4, #6, #10 (edited), #26, #27 (new); `strategy/execution-timeline.md` Dec 16–31 section (edited); `reference/stripe-balance-history-2025-07-07-to-2026-07-03.csv` re-queried directly for the `customerProcessingFee (metadata)` breakdown.

---

**H-067 · 2026-07-13 · MILESTONE**
Follow-Up #22's RP1 (Revision Point 1, late July 2026) cross-reference audit — closeout. Mirrors H-055's original pass: `model/CROSSREF-AUDIT-PLAN.md` documents the methodology; this pass re-verified all 5 of H-055's own findings are still fixed (confirmed by direct grep against current file content, not assumed), then ran a fresh catalog across 4 areas — `CONTEXT.md` vs. `strategy/*.md`; `assumptions.csv` TARGET/POLICY rows vs. sources; `CLAUDE.md`'s rules vs. actual practice; `HISTORY.md`'s own entries for dangling references.

**Result: 6 findings, 5 requiring a content fix (Findings 1–5, all fixed and committed separately/in this pass) + this entry itself (Finding 6, a typo-note only). 0 undocumented (c)-class gaps** — same clean result as H-055's original pass; every problem found was a documentation-accuracy issue (class d), not a missing safeguard.

**Findings 1–2 (`strategy/strategic-plan.md`, Executive Summary + Q1 2027 milestones):** Xavier's payout was still cited as "~$1,800 (`HISTORY.md` H-021)" in two places — the placeholder H-059 superseded on 2026-07-11. Corrected to the real structure (**$1,014.09** final equity-buyout + a still-**BLOCKED** revenue-share component) in both locations, original text struck through per this repo's changelog-preservation convention, not deleted.

**Finding 3 (`strategy/strategic-plan.md` Q1 2027 table + `strategy/execution-timeline.md` Monthly Financial Targets table) — the most significant finding.** Both tables were last reconciled 2026-06-17, predating the real projection-layer build (H-052/H-053, 2026-07-10/11), and neither was ever updated after. Checked directly against the live `model/financial-model.xlsx`'s "Plan vs Actual" sheet (High scenario), fresh, not reused from any prior session figure: `strategy/strategic-plan.md`'s Q1 2027 table showed cash comfortably above the $6,000 buffer target (Jan $6,671 / Feb $6,668 / Mar $6,159); the real model shows cash well below target and still falling (Jan $4,967.11 / Feb $4,163.56 / Mar $3,001.59) — the buffer is not reached until **May 2027**, not within Q1. `strategy/execution-timeline.md`'s parallel 12-month table was checked rather than assumed stale in the same way, per instruction — found stale across its *entire* horizon, not just Jan–Mar, so the whole table (not just three rows) was superseded with the real Aug 2026–Jun 2027 trajectory, low point **$1,336.55 in October 2026**. Both original tables preserved in place, with a dated "Superseded 2026-07-13" block and corrected table added immediately below each — no deletion.

**Finding 4 (`strategy/execution-timeline.md`, "### March 2027").** A `[DT]`-tagged "Xavier equity payout (~March)" bullet both cited the same stale ~$1,800 figure and violated the Legend's own definition of `[DT]` as dateless. Resolution chosen: **removed** (struck through with a dated note), not relocated — a correct, current entry for this exact fact already existed in the Demand-Triggered Watch List table (`$1,014.09` + BLOCKED revenue-share, correctly citing H-059), so relocating would have created a duplicate rather than fixing anything.

**Finding 5 (`CLAUDE.md`, File conventions).** The lowercase-kebab-case naming rule's exception clause named only the 5 conventional root files, but 7 non-root uppercase files (`reference/*-UPDATE.md` ×4, `model/*-PLAN.md` ×3) are long-standing, intentional, repeated practice never written into the rule. Updated the clause to name this second category explicitly.

**Finding 6 (this entry).** `HISTORY.md` H-033's `*Basis:*` line cites `` `reference/revenue-line-items.csv` and `reference/revenue-invoices.csv` `` — these files actually live at `model/data/revenue-line-items.csv` and `model/data/revenue-invoices.csv` (confirmed by direct file check). Flagged as an authoring-time citation typo in that original entry (2026-07-06). **H-033 itself is not edited** — it is a committed, append-only entry; this note is the correction, per this repo's append-only convention for post-commit corrections.

*Arithmetic self-check:* 5 content-fix findings (1–5) + 1 typo-note finding (6, this entry) = **6 findings total**, matching the count stated above and in the prior findings-report chat turn. 0 (c)-class gaps, matching H-055's original 0 — both pass counts stated as bare assertions here are corroborated by the findings list itself (every one of the 6 items above is class (d), none is class (c)), not just declared.

*Basis:* `model/CROSSREF-AUDIT-PLAN.md` (methodology, re-read; not edited this pass); direct `grep`/file reads across `CONTEXT.md`, `strategy/strategic-plan.md`, `strategy/execution-timeline.md`, `strategy/operational-toolkit.md`, `model/data/assumptions.csv`, `CLAUDE.md`, `HISTORY.md`; `model/financial-model.xlsx`'s "Plan vs Actual" sheet via `openpyxl` (re-derived fresh twice this pass, both times matching); `HISTORY.md` H-021, H-033, H-041, H-053, H-055, H-059, H-065 (all re-read, not recalled).

---

**H-068 · 2026-07-14 · DECISION (tracked, not resolved)**
New Follow-Up #28 added to `CONTEXT.md` Section 7: an open design question for achieving seamless, low-manual-effort cross-file synchronization for *routine* updates (a biweekly check-in fact, a fresh data pull) — not just the periodic Follow-Up #22 Revision-Point audit, which is a deep, deliberate quarterly pass, not a lightweight everyday mechanism. Explicitly not merged into or renumbered as part of Follow-Up #23 (self-triggering of already-documented standing procedures) — related but distinct in scope, and the relationship between the two is itself named as one of #28's own open sub-questions, not resolved here. A short cross-reference note was added to #23's own text pointing to #28, per this repo's cross-reference-legibility check (`CLAUDE.md`, "Cross-reference check on structured edits") triggered by this structured-document edit.
No implementation was built — no changes to any script, `CLAUDE.md`, or `check-ins/check-in-template.md`. Four sub-questions logged as open, not answered: (a) whether this should be a lightweight checklist run at the start of every biweekly check-in rather than only at Revision Points; (b) whether it should be scoped narrower than a full #22-style audit — a fast per-fact check versus the full periodic pass; (c) whether full automation (script/CI) is worth it now given D9's lean-tooling stance, or whether this stays manual-but-reliably-invoked; (d) how this relates to/overlaps with #23. No date or deadline attached — tagged UNSCHEDULED in the list, consistent with how other non-urgent open items are presented.
*Basis:* `CONTEXT.md` Section 7, new item 28; Follow-Up #22 and #23 (both re-read for scope before drafting, not recalled); `check-ins/check-in-template.md` (referenced, not edited).

---

**H-069 · 2026-07-14 · NOTE (tracked, not resolved)**
Appended a short factual note to the end of Follow-Up #23's existing text in `CONTEXT.md` Section 7 (text preserved, not restructured or shortened): H-068's self-initiated cross-reference addition (linking #28 back to #23, citing `CLAUDE.md`'s "Cross-reference check on structured edits" rule without being explicitly instructed to) is recorded as a data point for #23's future design session. Framed explicitly as a small, real instance of the self-triggering behavior #23 asks about — evidence the gap is partial/inconsistent, not evidence #23 is solved or that the broader gap (`HISTORY.md` logging, the model-update loop, the arithmetic self-check, the pipeline-refresh rule) has closed. Follow-Up #28 itself was not touched by this note.
*Basis:* `CONTEXT.md` Section 7, item 23 (edited); `HISTORY.md` H-068 (cited, re-read not recalled).

---

### Undated revision

**H-019 · REVISION — Ledger schema: boolean `paid_flag` replaced by separate invoice/payment events.**
An earlier draft of the atomic ledger schema used a binary `paid_flag`. It was replaced by distinct invoice (billed) and payment (collected) event rows, because a boolean cannot represent partial payment and would overstate cash — the ~$1,752 billed/collected gap in current data is real. This redesign is folded into D5a (H-012).
*Basis:* `CONTEXT.md` Section 9, "Design note."

---

### Undated tooling

**H-020 · TOOLING — Repository hosted under a dedicated business GitHub account.**
`Tupelo-Ops` lives under a free GitHub account registered to the business email, not the owner's personal or work account, so the repository is a clean, portable business asset. In progress as of the GitHub transition.
*Basis:* `CONTEXT.md` Section 7 item 12(a); Section 10 step 1.

---

### Undated foundational decisions (recorded as of the pre-migration synthesis; exact session dates not specified in `CONTEXT.md`)

**H-007 · DECISION — D1: Materials/fuel explicit-zero placeholder.**
Model materials and fuel as explicit `$0` lines tagged BLOCKED rather than back-solving an estimate, because no per-job categorized data exists yet.
*Basis:* `CONTEXT.md` Section 4, D1.

**H-008 · DECISION — D2: Labor itemized by role, never blended.**
Assume 2 people per job (crew lead + crew member), 50/50 hours, at atomic rates ($25/hr lead, $20/hr member); ~20 person-hours per two-week period for recurring work, flagged ESTIMATE until time-tracking is live.
*Basis:* `CONTEXT.md` Section 4, D2.

**H-009 · DECISION — D3: Konji's revenue share is 6% of billed revenue.**
Konji leads essentially all work he is present for; his 6% share applies to billed (not collected) revenue on jobs he leads, beginning September 2026.
*Basis:* `CONTEXT.md` Section 4, D3.

**H-010 · DECISION — D4: Two-phase crew-lead cost structure.**
Konji's negotiated terms apply through May 2027; after that the crew-lead position reverts to a generic $25/hr line with no revenue share or bonuses, modeled as a phase switch.
*Basis:* `CONTEXT.md` Section 4, D4.

**H-011 · DECISION — D5: Atomic ledger + derived views.**
A single atomic actuals ledger is the source of truth; every summary (P&L, rollups, revenue by service/customer, blended labor rate) is derived from it by formula, never typed in — preventing the same fact from existing in multiple places and drifting.
*Basis:* `CONTEXT.md` Section 4, D5.

**H-012 · DECISION — D5a: Billing and payment are separate atomic events.**
An invoice (billed) and each payment against it (collected), including partial payments, are separate ledger events. Relay bank statements, not CRM payment records, are the source of truth for collected cash.
*Basis:* `CONTEXT.md` Section 4, D5a.

**H-013 · DECISION — D6: Data layer vs. projection layer, separated.**
The CSV data layer (actuals + flagged estimates) is physically separate from the forward projection layer, which may only reference derived data-layer values or labeled assumptions — never a hardcoded number.
*Basis:* `CONTEXT.md` Section 4, D6.

**H-014 · DECISION — D7: Generated-workbook build method.**
`build_model.py` reads plain-text CSV data and generates the Excel workbook as disposable output, enabling human-readable Git diffs and reproducible builds. The generated workbook is gitignored; dated snapshots are committed to `reference/` only at genuine milestones. Rejected alternative (hand-maintained single `.xlsx`) preserved the binary-diff problem this decision avoids.
*Basis:* `CONTEXT.md` Section 4, D7.

**H-015 · DECISION — D8: Self-reconciliation tab, honestly scoped.**
The generated workbook includes a tab verifying revenue and collected cash against CRM exports and Relay statements. It explicitly does not yet verify itemized fixed overhead or materials/fuel, for lack of underlying per-job/contract data.
*Basis:* `CONTEXT.md` Section 4, D8.

**H-016 · DECISION — D9: Tooling — lean now, heavyweight orchestration later.**
Use GitHub + Claude Code in VS Code via Codespaces, with a lean project-specific `CLAUDE.md`. Multi-agent orchestration (Ruflo/claude-flow) is deferred until a task justifies parallel agents; it serves only as a structural reference for what `CLAUDE.md` can contain.
*Basis:* `CONTEXT.md` Section 4, D9.

**H-017 · DECISION — D10: RAG readiness now, adoption on trigger.**
Keep the repository retrieval-ready through free structural conventions (plain text, clean sections, front-matter, atomic files). Adopt an actual vector database only when the corpus outgrows the context window or recurring semantic search becomes necessary; keep the eventual choice tool-agnostic rather than locking into RuVector early.
*Basis:* `CONTEXT.md` Section 4, D10.

**H-018 · DECISION — Cross-reference-legibility review check adopted.**
`CLAUDE.md` and future structured-document edits are governed by a triggered check: verify that a reader arriving at a changed section can reach any justification, dependency, or definition it relies on without already knowing where it lives.
*Basis:* three external review rounds of `CONTEXT.md` in which every surviving defect was cross-reference/join drift, not content error (`CONTEXT.md` Section 6 item G3).
