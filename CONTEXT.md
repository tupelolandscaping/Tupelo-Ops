# CONTEXT.md — Tupelo Landscaping LLC · Business Operations & Financial Model Repository

**Document purpose:** This is the single handoff document that carries the full context, locked decisions, standing rules, and work queue for this project from its prior home (a Claude "project" workspace) into a GitHub repository executed through Claude Code in VS Code. It is written to be understood by a reader — human or AI — who has **no prior context**. Acronyms are expanded on first use; quantitative claims are traced to a source file wherever an in-repo source exists (where a figure is owner-supplied or awaiting verification, it is labeled as such rather than given a false citation).

**Status:** Draft for review (revision 6). Nothing in the repository has been built or edited yet. This document is the specification for what will be built.

---

## 1. How to Use This Document

- This is the **memory bridge**. A fresh Claude Code session starts with no conversational history; this document supplies it. Read it fully before acting.
- Decisions recorded in Section 4 are **locked** unless explicitly revisited. Do not silently reverse them.
- Section 6 ("Decided, Not Yet Applied") is a **reconciliation-and-setup queue to be completed before the model rebuild.** It contains preparation tasks (converting files, correcting stale content, authoring repo files). The model rebuild itself (item E) is the *output* of clearing that queue and is sequenced in Section 10, not performed midway through the queue.
- Standing rules (Section 5) govern all work and override convenience.

---

## 2. Project Genesis & Evolution

**Origin.** This project began as a strategic analysis of a small landscaping business, commissioned by the owner, spanning nine domains (financial modeling, labor/org design, marketing/acquisition, equipment, owner-transition, risk, pricing/unit-economics, operations/systematization, and legal/compliance). That analysis was synthesized into four core artifacts, all reconciled to one another on **2026-06-17**:
1. A **One-Year Strategic Plan** (July 2026 – June 2027).
2. An **Operational Toolkit** (templates, checklists, setup guides).
3. An **Execution Timeline** (dated milestones, tripwires, revision points).
4. A **Financial Model** (multi-tab Excel workbook).

**Evolution over the following weeks.** The project matured through iterative working sessions:
- The service catalog in the CRM (customer-relationship-management platform; see Section 3) was consolidated from a fragmented set of entries to ~16 clean categories, so that reporting KPIs (key performance indicators) stopped reading zero. (The exact pre-consolidation raw count is to be confirmed during the revenue-mapping session — Follow-Up #6 — because two different raw counts have appeared in working notes.)
- The weekday crew-lead hire (Konji, see Section 3) moved from "conversation underway" to **fully finalized terms**.
- The starting-cash assumption was corrected from a planning guess to a confirmed bank figure.
- A structured **biweekly check-in system** was established, with a reusable template and a variance framework (STAY / ADJUST / ESCALATE; the operative rules live in `check-ins/check-in-template.md`, not in this document).
- Most recently (the sessions immediately preceding this document): a decision to **rebuild the financial model** on a fully traceable architecture, and to **migrate the whole project into a Git repository** executed via Claude Code — the transition this document enables.

**Why the rebuild.** The original model mixed source data and derived outputs in the same cells, and embedded some costs (notably labor) inside blended margin percentages. This made numbers hard to trace and allowed the same fact to exist in multiple places and drift. The rebuild (Section 9) fixes this structurally.

---

## 3. Business Snapshot & Objectives

**The business.** Tupelo Landscaping LLC is a residential and commercial landscaping company based in **Arlington, Virginia, USA**. It operates one truck (a 2008 Ford F-150, ~214,000 miles), a reliable core crew of roughly four workers (high-school and gap-year age), and uses **flat-rate pricing** set from internal hourly estimates (hourly rates are never disclosed to clients). Customer acquisition has historically been owner-led door-to-door canvassing.

**The owner.** A junior at Washington University in St. Louis, studying Organizational & Strategic Management and Philosophy-Neuroscience-Psychology; interested in management consulting and organizational psychology; a self-described beginner in technical subjects (coding, tooling) with a growing interest in AI. **The binding constraint of the entire plan:** the owner relocates out of state for school in **January 2027** and returns each summer. From January 2027, the business must run and grow **without the owner physically present**.

**Objectives (this plan window, July 2026 – June 2027):**
1. Roughly double peak monthly revenue — anchoring on the **actual May 2026 peak of $4,908** toward a **~$10,000/month** peak (roughly 2×). **Correction, 2026-07-10 (`model/PROJECTION-PLAN.md` Section 2.1, found during projection-layer scoping):** the peak has since moved — June 2026 billed revenue is **$6,019.89** (verified fresh against `model/data/ledger-revenue.csv` before writing this correction), higher than the $4,908 figure this had been citing as "the strongest single-month figure to date." The original $4,908 anchor is preserved above, not deleted, per this repo's changelog-preservation convention; June 2026 is now the actual strongest month on record, putting the business ~60% of the way to the $10,000 target at its peak, not ~49%. This anchor is a single-month actual, not a multi-season average, and will keep moving as more seasons of data accumulate — `model/build_model.py`'s projection layer computes the current peak-to-date fresh from the ledger at every build rather than storing a static figure, specifically so this citation cannot go stale again the way this one did.
2. More importantly, rebuild the business to **operate owner-absent** before the January 2027 relocation.
- (The long-range ~$20,000/month ambition is treated as a 2028-or-later horizon, not a within-year target.)

**Current economic position.** *The revenue-mix and client-concentration figures below derive from the CRM revenue-by-customer and P&L exports and the model's data-validation rows, and are from the **2026-06-17 snapshot** — flag for recomputation on fresh data during the rebuild. The billed/collected totals and the cash reading are **current as of 2026-07-01** (more recent than that snapshot).*
- Revenue mix by **service type**: **~58% project work / ~42% recurring maintenance**. **Residential recurring mowing is ~5%** — and it is the intended growth engine, so the growth engine effectively starts near zero.
- **Client concentration is a material risk:** the top three clients represent ~79% of revenue; the single largest client (a commercial account) represents ~42%. *Note on classification:* the project/maintenance split above is by service type, while concentration is by customer. Several clients — including the large commercial accounts — contribute to **both** project and recurring work depending on what was done for them, so a single client can span both service categories. Customer-level and service-level figures therefore should not be expected to partition identically. Exact per-client service composition is to be verified against the files during the rebuild.
- Revenue Jan 1 – Jul 1 2026: **$12,726.01 billed / $10,973.57 collected** (per current Homeworks exports; see Section 6 item F). The billed-collected gap (~$1,752) is real and is the reason billing and collection are modeled as separate events (D5a).
- Cash on hand: **$1,225.33 as of 2026-07-06** — the verified, bank-sourced total across all four active Relay accounts (3549 OpEx $1,225.33; 8735 Income $0.00, instant-swept; 3550 Reserve $0.00; 8737 Taxes $0.00), independently computed from the statement files, not asserted from a single figure (`reference/cash-consolidated-2026-07-06.csv`; see `HISTORY.md` H-039). This supersedes the $1,400 single-account interim reading (2026-07-01) — the Relay consolidation (Follow-Up #9) that reading awaited is now complete, and this figure is ACTUAL, not a placeholder. Going forward, cash is read from the **actual monthly bank statements** — those statements, not the CRM's "billed" figures, are the source of truth for cash (CRM "billed" is unreliable for cash timing; see the anchor-jump note in Section 6 item F).

**Key people.**
- **Konji** — Primary crew lead (weekday recurring work is his core, but he leads essentially all work he is present for; see D3). Accepted; terms finalized (Section 4). The single most time-sensitive hire for enabling the owner's departure. Becomes the primary local operator once the owner relocates.
- **Anais** — Part-time client acquisition / canvassing and backup driver (already a listed insured driver on the auto policy, and to remain so). Substitutes as crew lead only when Konji cannot be present for a given job (e.g., weekend project work he is unavailable for). **Canvassing rate and backup crew-lead rate owner-confirmed and resolved, 2026-07-10 (H-057, Follow-Up #11):** canvassing pay is a general crew policy shared with Konji, not person-specific — $15/hr plus $15 per fall client signed and $20 per spring client signed (`model/data/assumptions.csv` `crew_canvassing_rate`/`crew_canvassing_bonus_fall`/`crew_canvassing_bonus_spring`, generalized from what were previously Konji-specific rows); her backup crew-lead rate is $25/hr (CONTRACTED, `anais_backup_lead_rate`), for the expected-rare jobs she leads when Konji cannot be present. *Separately, her regular wage change is still pending, not yet effective:* she is currently paid $25/hr (real, in payroll/ledger data); the owner intends to reduce this to $20/hr but has not done so yet — no effective date exists. Same treatment as Konji's own not-yet-run negotiated-rate change (D4): dormant until the owner confirms the reduction has actually taken effect, at which point the real payroll data and any relevant assumption row are updated from that fact, not asserted in advance.
- **Xavier** — Business partner with a deferred, interest-free equity arrangement, treated as patient capital (functions as equity for cash-buffer purposes because it carries no due date or interest), which ended **2026-04-30** (owner's best recollection of the exact date, corrected from an initial ~2026-04-28 recollection — per H-059; still owner-recollection, not independently verified) when the owner returned home. **Owner-confirmed, 2026-07-10 (H-048): Xavier is the same person as "Beauvais, Xavier" in the real Gusto payroll exports** — he has done real, paid hourly work alongside this equity arrangement throughout, per the owner, and continues as a regular hourly employee after the equity arrangement's end (rate moved $25/hr → $20/hr starting the 06/15–06/28/2026 payroll period, unrelated to the equity settlement). **Payout structure owner-confirmed and resolved for its known portion, 2026-07-11 (H-059):** see `model/data/assumptions.csv`'s `xavier_payout` row and the Section 6 item D rewrite below — replaces the prior flat "~$1,800, exact figure unconfirmed" placeholder with a two-part structure: a known $1,014.09 equity-buyout floor, plus a revenue-share bonus (WLF/Moore/Picarello + Anacostia) that remains **BLOCKED pending real data** at the 3-month (2026-07-31) and 6-month (2026-10-31) milestones, calendar-month-aligned — see Follow-Up #24.

**Key tools & platforms.**
- **Homeworks** — the primary CRM and accounting platform (revenue tracking, accounts-receivable aging, customer-source tracking, and job costing). "Growth" plan purchased. Referred to throughout as "the CRM." Job Costing (per-job cost tracking) is **not yet activated** (target: end of July 2026). Time-tracking has **10 prepaid seats**; see Section 7 #2 for the seat-vs-headcount question (unresolved, deferred to a post-transition session — no headcount figure is asserted here).
- **Relay** — the business banking platform. Bank statements (actual cash movement) are the truest cash signal and the third leg of reconciliation (model ↔ CRM ↔ bank). Five-account Profit First structure — 8735 Income, 3549 OpEx, 3550 Reserve, 8737 Taxes; 8736 (Owner's Comp) deleted in Relay 2026-07-05. Only Income and OpEx are historically active; the other three confirmed $0 across all 17 months of statements (canonical account map: reference/README.md — update there first if accounts change). Consolidation (Section 7 #9) **closed 2026-07-06** — see `HISTORY.md` H-039.
- **Excel** — the current financial model.
- **GitHub + Claude Code + VS Code (via GitHub Codespaces)** — the destination execution environment (this migration). A **Codespace** is a cloud development machine hosted by GitHub; the owner opens it through the VS Code Desktop app, which acts as a window onto that cloud machine, and saves work by committing and pushing to GitHub.
- **Google Business Profile (GBP)** — active, but currently generates no reviews and no leads.
- **Squarespace** — current website host (tupelolandscaping.com), on monthly billing pending a migration decision (not in scope here).

---

## 4. Locked Decisions (with condensed reasoning)

Each decision below is settled. The reasoning is preserved so the choice can be audited and so a reviewer understands *why*, not just *what*. Where a decision references a chosen approach from a set of considered alternatives, the rejected alternatives (if any matter for audit) are recorded in `HISTORY.md`.

**D1 — Materials/fuel costs: explicit-zero placeholder.**
Model materials and fuel as explicit `$0` lines tagged `BLOCKED` ("not yet tracked"), rather than back-solving an estimate. *Reasoning:* no **per-job, categorized** materials/fuel data exists in any current file. Materials spend *is* being incurred and partially captured (e.g., a $422.26 Home Depot order bundling a backpack sprayer, PPE, and small tools), but only lumped into coarse expense buckets (Insurance, Miscellaneous, New Equipment, Payroll) without per-job attribution. A back-solved residual would be an inference dressed up as a measurement, which violates the standing rules. Real, attributable materials/fuel data becomes available only once Job Costing is activated. (See Follow-Up #4 for the item/service classification groundwork — `model/data/catalog-type-map.csv` — laid 2026-07-09 ahead of that activation.)

**D2 — Labor: itemized by role, never a raw blended entry.**
~~Assume every job has **2 people (one crew lead + one crew member)**, with hours split **50/50**, flagged as an **ESTIMATE**. Role rates are atomic inputs — **crew lead $25/hour, crew member $20/hour** — and any blended labor figure must be a *derived output*, never a typed-in number. Current working estimate of recurring-work labor: **~20 person-hours per two-week period**, covering **recurring work only** (one-off projects are excluded because they cannot be forecast ahead of time). "Person-hours" means total labor across all workers on a job, not clock-hours (a 2-person, 1-hour job = 2 person-hours). *Reasoning:* the owner requires every number traceable to base inputs; the previous model buried labor inside blended margins with no reference to the rate cells. These estimates are to be **replaced with actuals once time-tracking is live** (see Section 7 #2 for the seat question that conditions this).~~ **Reframed, 2026-07-10 (H-049):** the struck-through text is now a **forward-looking projection-layer assumption only** — for quoting and staffing future jobs — not a description of actual historical labor cost. Historical labor is now sourced directly from real Gusto payroll data (`reference/tupelo-landscaping-llc-payroll-summary-*.csv`), itemized per employee per pay period as **ACTUAL** rows in `model/data/ledger-labor.csv` (built by `model/populate_labor_from_payroll.py`; role attribution via the time-varying `model/data/employee-role-map.csv`, needed because Xavier's Gusto title changed from Crew Lead to Crew Member coinciding with his $25→$20/hr rate drop). This real data is **broader in scope, not a like-for-like replacement**: it covers every employee (not a fixed 2-person model) and all work combined, recurring and one-off project work alike (the original estimate was recurring-only) — see Follow-Up #19 for the resulting limitation (no per-job/per-customer attribution in payroll data). Blended labor rate remains a derived output, never typed in, per this decision's original requirement.

**D3 — Konji leads the work he is present for; his 6% share is on billed revenue.**
Konji leads essentially all work he is present for — recurring and project — which in practice is expected to be most work; Anais substitutes only when he cannot be there. His **revenue share is 6% of billed revenue** on jobs he leads (billed, not collected — chosen so the payroll base is a fixed, invoice-time number). It **begins September 2026** (not before), once he is running operations independently.

**D4 — Two-phase crew-lead cost structure.**
Through May 2027: Konji's full negotiated terms apply. **After May 2027:** the crew-lead position reverts to a generic cost line — $25/hour, no revenue share, no bonuses. Build this as a phase switch in the model. (Konji's finalized terms: ~~$25/hr base from day one~~ — **correction, 2026-07-10 (H-048):** real Gusto payroll data shows Konji held **$20/hr for all 20 of his real, completed pay periods** in both payroll exports, through 06/15–06/28/2026 (payday 07/07/2026, the most recent completed period as of these exports); the negotiated $25/hr rate takes effect starting the *next* payroll period, which had not yet run when the exports were pulled. "From day one" was never accurate to the real pay history — the original text is struck through, not deleted, per this repo's changelog-preservation convention. 6% revenue share on jobs he leads from September 2026 through end of May 2027; canvassing at $15/hr plus $15 per fall client signed and $20 per spring client signed; winter business-side project work at $25/hr; committed through end of May 2027; transitions to fuller hours in March 2027, no later than end of March. He is unlikely to relocate with family to Atlanta, so continuation beyond May is plausible but not counted on.)

**D5 — Model architecture: atomic ledger + derived views.**
A single **atomic actuals ledger** is the source of truth: one row per atomic financial event. Every summary (P&L, quarterly rollups, revenue-by-service, revenue-by-customer, labor blend) is **derived from that ledger by formula** — never typed in. *Reasoning:* single-source-of-truth prevents the same fact from existing in multiple places and drifting (the failure mode that previously caused an accrual entry to be misread as a cash event). Traceability becomes structural rather than a discipline to maintain.

**D5a — Billing and payment are separate atomic events.**
An invoice is one ledger event (revenue billed); each payment against it is its own ledger event (revenue collected), including partial payments. **Billed revenue = sum of invoice events; collected revenue = sum of payment events.** *Reasoning:* a binary "paid/unpaid" flag cannot represent partial collection and would overstate cash whenever an invoice is partly paid — precisely the error the architecture exists to prevent. Separate events let both billed and collected trace to their own real rows, so any figure references a verified number regardless of which one a calculation needs (Konji's 6% reads billed events; cash-on-hand reads payment/bank events). For this business, given manual CRM entry, the **Relay bank statements are the source of truth for collected cash** — payment events are reconciled to and governed by the bank, not the other way around. This is why bank consolidation (Follow-Up #9) was made a pre-build gate: standing up the collected/cash view on unreconciled CRM payment data would rebuild the cash side on the very unstable base this architecture exists to escape. (Closed 2026-07-06 — see `HISTORY.md` H-039.)

**D6 — Data layer vs. projection layer, separated.**
The **data layer** (CSV files: actuals plus clearly-flagged estimates) is physically separate from the **projection layer** (the forward model). The projection layer may reference only (a) values derived from the data layer or (b) cells in a clearly-labeled assumptions file — it can never contain a hardcoded number. *Reasoning:* the owner no longer wants projections resting on an unstable base; making hardcoding structurally impossible is stronger than relying on discipline.

**D7 — Model build method: generated workbook.**
The source of truth is plain-text CSV data files; a Python script (`build_model.py`) reads them and generates the Excel workbook as output. *Reasoning:* Git can show human-readable diffs of every data change (impossible with a binary spreadsheet), the build is reproducible, and the projection layer cannot hardcode numbers. The workbook is disposable output — if lost or corrupted, it is regenerated, not repaired. The one tradeoff (trusting generated output) is mitigated by D8. (This was chosen over the alternative of hand-maintaining a single `.xlsx`, which would preserve the binary-diff problem; the rejected alternative is logged in `HISTORY.md`.) The generated `financial-model.xlsx` is **gitignored, not committed**: it is build output, and tracking a binary that an agent regenerates frequently would pollute `git status`/diffs with per-build noise — the exact problem this architecture avoids. Reproducibility is guaranteed by a pinned `requirements.txt`, not by a stored (possibly stale) binary. Dated `.xlsx` snapshots are committed to `reference/` **only** at genuine milestones — deliberate, append-only artifacts, never per-build. (A future option, not adopted now: a CI action that builds on push and attaches the workbook as a downloadable artifact.)

**D8 — Self-reconciliation tab (scope stated honestly).**
The generated workbook includes a tab that checks its generated totals against source figures and flags mismatches. **What it can verify:** revenue (against CRM revenue exports) and collected cash (CRM payment events reconciled against the Relay bank statements, which are the cash source of truth per D5a). **What it cannot yet verify:** itemized fixed overhead and materials/fuel, because the CRM expense taxonomy is only four coarse buckets and per-job cost data does not yet exist. For overhead to be reconcilable, the underlying contract figures must be added to `reference/` (see Section 6 item I). *Reasoning:* closes the loop between "reproducible" and "verified" where source data exists — and states plainly where it does not, so the self-check is not mistaken for full coverage.

**D9 — Tooling: lean now, heavyweight orchestration later.**
Use GitHub + Claude Code in VS Code (via Codespaces), with Git operated through VS Code's built-in Source Control panel (no separate desktop Git client). Write a **lean, project-specific `CLAUDE.md`** now. The Ruflo / claude-flow multi-agent orchestration harness (owner to confirm the exact current tool names before relying on them) is used only as a **structural reference** for what a good `CLAUDE.md` can contain; its full apparatus is deferred until a task actually justifies parallel agents. *Reasoning:* match tool weight to problem weight — a small ledger-and-docs repo maintained by a beginner does not need a multi-agent swarm harness, and some orchestration-harness default directives (e.g., an instruction not to proactively create documentation) would conflict with this project's documentation-first nature.

**D10 — RAG readiness now, adoption on trigger.**
Keep the repository "retrieval-ready" through structural conventions (plain text, clean sections, front-matter metadata, atomic well-named files) that cost nothing extra. RAG (retrieval-augmented generation) is the pattern of storing documents in a searchable index and retrieving only relevant pieces at query time; a vector database enables semantic (meaning-based) rather than keyword search. Adopt an actual vector database only when a real trigger fires (the accumulated corpus outgrows the context window, or cross-log semantic search becomes a recurring need). Keep the choice tool-agnostic — do not lock into RuVector (a Ruflo subsystem) early. *Reasoning:* same weight-matching logic as D9, applied to retrieval infrastructure.

---

## 5. Standing Rules & Engagement Preferences

**Operating rules (govern all work):**
1. **Prompt before editing any repository file.** When a file is accessed in the course of answering, ask before modifying it. This matters increasingly as the business and timeline evolve away from initial predictions.
2. **Review all files for staleness during check-ins or any plan revision.** Sequencing/timeline files especially will drift from initial predictions as action items are carried out; keep them aligned to current progress.
3. **Verify quantitative structure before reasoning on it.** Before using any figure from the model (or any quantitative file) to support a conclusion, trace how it is actually computed — read the formulas and the cells they reference, not the displayed values. Do not infer a model's internal logic from memory, from what it "should" do, or from a verbal description; confirm it against the file. If a needed number is not explicitly modeled, say so and state what cannot be determined from the file alone. Never substitute a plausible assumption for a verified fact and present it as established. When needed information lives outside the available files, flag that and propose how to retrieve it before proceeding. **A confident wrong answer is worse than a flagged gap — surface uncertainty first, reason second.**

**Engagement preferences (how to communicate):**
- Layered answers: **direct answer first, then reasoning.** Formal, academic, analytical, educational tone. No motivational language, fluff, or shallow summaries. Optimize for precision, nuance, completeness, and learning value over speed.
- No hallucination or false certainty. Surface assumptions, tradeoffs, risks, and counterarguments. Prefer evidence-based reasoning; use consensus reasoning where evidence is limited. Use web research for high-stakes factual claims, vet sources, and note conflicts.
- Beginner-friendly explanations that still push toward expert depth. Concise step-by-step reasoning where helpful. **Iterative collaboration is preferred over one-shot comprehensive dumps.**
- Actively support the owner's goal of improving at AI use: better prompting, better questions, iterative refinement, token efficiency. Teach **engineering-first thinking** (structure first, then function). During ideation/brainstorming, do extra independent thinking and **push back or offer alternative directions** rather than reflexively agreeing.

---

## 6. Phase 1 Queue — Reconciliation & Setup (Decided, Not Yet Applied)

**These are finalized decisions and preparation tasks not yet written into the files. Complete this queue after repository setup and BEFORE the model rebuild.** The model rebuild (item E) is the *result* of this queue and is sequenced separately in Section 10 — it is not performed partway through the list. Each file edit is done inside the repository (after the baseline import) so it is captured as a reviewable Git diff.

- **A. Import the strategy documents (already Markdown).** `Strategic_Plan_2026-2027.md` → `strategy/strategic-plan.md` and `Operational_Toolkit.md` → `strategy/operational-toolkit.md`. *These arrive as Markdown (the source Word files were exported to Markdown outside the repo), so no in-repo Word conversion is needed — import as-is and kebab-case rename, exactly like the timeline and check-in template.* Commit them as part of the unedited baseline *before* applying items B–D, so the "before" state is preserved in history.
- **B. Update the Execution Timeline.** Mark the Konji crew-lead tripwire **resolved** (accepted late June 2026, terms finalized ahead of the ~July 7 deadline); mark the **backup-lead search as now active**; add a **Job Costing activation anchor for end of July 2026**; correct the **GBP status** to "active, but no reviews and no leads"; note there is **no residual Konji family-conversation gate**; add the **auto-insurance / Konji-as-insured-driver decision point** (see item J).
- **C. Update the Strategic Plan.** Replace the placeholder crew-lead compensation line in the Labor & Org section with Konji's finalized structure (D4); check off the "weekday crew lead secured" item in the Owner Transition checklist.
- **D. Update the Assumptions Log.** Xavier payout — **RESOLVED for its known portion, 2026-07-11 (H-059)**, superseding the prior flat "~$1,800, exact figure not yet confirmed" placeholder. Owner-confirmed structure: two components, calculated separately but paid as a single aggregate amount. **(1) Equity-buyout component (known, final): 45% of the company's cash-in-hand immediately before the 2026-04-09 $2,200 truck/insurance-reimbursement debit** (so the truck purchase — a business asset, explicitly excluded per the owner's own framing — doesn't deflate the cash pool his share is based on), reconstructed directly from `reference/Relay 2026-04-01 #3549.csv` and the other 3 active accounts: **$2,253.54** total → **45% = $1,014.09**. This is `model/data/assumptions.csv`'s `xavier_payout` value — a known floor, not the final total. **This anchor is the 2026-04-09 transaction, not the departure date, so it is unaffected by the departure-date correction below** (re-confirmed 2026-07-11, no recalculation needed). **Departure date corrected 2026-07-11: 2026-04-30**, not the initially-recalled ~2026-04-28 (still owner-recollection, not independently verified). **(2) Revenue-share bonus component (BLOCKED, pending real data): 10% of net (surcharge-excluded) revenue** from the three recurring accounts Xavier cultivated and kept running (Women Life Freedom LLC, Alissa Moore, Martha Picarello — "WMP"). **Window anchor switched 2026-07-11 to calendar-month boundaries, starting 2026-05-01** (the first full calendar month after the corrected 2026-04-30 departure) — a deliberate simplification for easier tracking, owner-requested, not incidental rounding; replaces the original day-exact anchor. Modeled as **4 mutually-exclusive scenarios** the owner will choose between once computable: flat 10% over May–June–July 2026 (closes 2026-07-31) or May–October 2026 (closes 2026-10-31); or a declining 15%/10%/5% schedule over the same two window lengths (May=15%/June=10%/July=5% for the 3mo version; May–June=15%/July–Aug=10%/Sep–Oct=5% for the 6mo version). None can be computed today — the data layer only extends actuals through 2026-06-29. The WMP actual-to-date partial figure was re-derived fresh from the new 2026-05-01 anchor (not adjusted from the old figure): **$4,689.08 net across 13 invoices — identical to the prior 2026-04-28-anchored figure**, confirmed by direct re-query of `model/data/revenue-invoices.csv`, not assumed (no WLF/Moore/Picarello invoices fall in the 2026-04-28–2026-05-01 gap). Still explicitly partial, not a final answer for either scenario. **Plus a separate one-time Anacostia Community Boathouse Association slice**: flat 10% of the *first* invoice's net amount only (not ongoing, since Xavier's post-departure involvement there was minimal — one meeting, one preliminary workday), also BLOCKED — the first payment is confirmed real ($2,332.00, 2026-05-26, per Relay), but Anacostia isn't yet in the data layer (`model/data/revenue-invoices.csv` predates this account), so the invoice's exact net-of-surcharge figure can't be isolated yet. See `model/data/assumptions.csv`'s `xavier_revenue_share_*`/`xavier_anacostia_share` rows and **Follow-Up #24** for the unblock plan. Trigger/sequencing unchanged: still demand-triggered, gated behind the full $6,000 buffer being intact. **Owner (Cyrus) truck-debt repayment:** amount **$3,800**, **unpaid**, **patient/interest-free** (treated as equity for cash-buffer purposes, same basis as Xavier — see `strategy/execution-timeline.md`'s Cash Buffer Policy, "Debt repayment (Xavier, owner)"), **demand-triggered**, sequenced **after** Xavier's payout is repaid (see `strategy/strategic-plan.md` Section 6's Equipment Investment Plan, Phase 3: "Xavier payout, then truck debt"), both gated behind the full $6,000 buffer ($4,000 reserve + $2,000 truck hedge) being intact — unaffected by the Xavier restructure above. Status: Track — NOT resolved. Starting-cash anchor: history is $1,500 (original planning guess) → $1,400 (2026-07-01, single-account interim ESTIMATE) → **$1,225.33 (2026-07-06, verified ACTUAL across all four active Relay accounts)** — the Relay consolidation (Section 10 step 5 / Follow-Up #9) that superseded the interim reading is now complete; see `HISTORY.md` H-039 and `strategy/strategic-plan.md` Section 9's Revision Note for the full history. Book it as ACTUAL.
- **F. Reconcile the stale revenue anchor.** The model's Jan–Jun revenue validation figure (`Assumptions!B43 = $9,562.71`, a billed-basis figure) was captured on 2026-06-17 and predates roughly two weeks of subsequently-recorded June invoicing. Current exports through 2026-07-01 show **$12,726.01 billed / $10,973.57 collected** (reconciled across the P&L, revenue-by-customer, and sales-tax reports). Supersede B43 during the rebuild; do not treat it as current. *Surcharge treatment (resolved 2026-07-06):* the $12,726.01 anchor is **gross** — the ~6% surcharge is embedded inside it, not layered on top. Net service revenue for the same Jan–Jul 2026 window is **$12,024.58**; surcharge is **$701.43**. Konji's 6% revenue share is computed on the net figure (see `HISTORY.md` H-031, H-032). The prior overstatement caveat (~$701 on this anchor, ~$42 on Konji's base) no longer applies — both figures are now verified, not estimated. *Note:* the increment (~$3,163 — about 25% of the $12,726 half-year total, or ~33% on top of the prior $9,562.71 figure) landed in the ledger within ~two weeks, itself direct evidence that "billed" is a lagging, unreliable cash proxy — reinforcing D5a.
- **G. Write `CLAUDE.md`** per the specification in Section 8.
- **G2. Write `SETUP.md`** per the specification in Section 8 — the standalone new-Codespace environment guide.
- **G3. Write `README.md` and `HISTORY.md`.** `README.md` is the repository map / entry point. `HISTORY.md` is the master append-only audit log (spec in Section 8). Seed `HISTORY.md` with the decisions and events already recorded in this document so the audit trail starts populated, not empty. Include one `DECISION` entry recording the **cross-reference-legibility review check** (Section 8): basis — three external review rounds of this document in which every surviving defect was cross-reference/join drift, not content error.
- **G4. Author the build-config files** — `.gitignore` (excluding `financial-model.xlsx` and transient build artifacts) and `model/requirements.txt` (pinned dependencies, e.g. openpyxl). **`requirements.txt` is created here, once** — the build step (E) only *appends* any further dependencies `build_model.py` turns out to need; it does not re-create the file. `.gitignore` must exist before the first build (step 6) so D7's gitignore rule holds on the first generated workbook.
- **H. Record the truck expense entry.** A $5,000 "New Equipment" CRM entry (April 13) is a **retroactive record of an already-owned asset** (the existing F-150) added to make the year-to-date P&L more accurate — it is **not a new capital outflow**. Do not model it as a future cash event.
- **I. Source itemized overhead.** Add the actual contract figures behind fixed overhead (insurance, workers' comp, commercial auto, payroll provider, CRM subscription) as a small sourced data file in `reference/`, so those figures trace and can be reconciled by D8. The equipment-maintenance line is an **allocation** — and an allocation requires a base and a method (e.g., a flat $X/month, or Y% of the truck's book value). Until that base and method are stated, it is **BLOCKED**, not a silently-modeled figure; once a basis is defined it becomes an **ESTIMATE** carrying that stated basis. (Per Standing Rule 3 and D1, an allocation with no stated basis is exactly the kind of inference the rules forbid presenting as modeled.) *Commercial auto:* the current premium is an ACTUAL sourced here; the prospective increase from adding Konji as an insured driver is tracked separately as BLOCKED pending a quote (item J). Current = ACTUAL, increment = BLOCKED.
- **J. Auto-insurance / Konji as insured driver.** Adding Konji to the drivers' policy raises the premium (**amount BLOCKED — requires an insurer quote; do not fabricate**). The current term ends **October 9, 2026**. This is a coverage/liability prerequisite, not a mere convenience: if Konji leads a truck-driving crew owner-absent while unlisted on the policy, a loss could be uninsured or a claim denied — a material liability exposure, distinct from the legality of driving itself (the precise legal/coverage characterization is **owner-to-verify with the insurer**, not asserted here). The timing decision (add before Oct 9 vs. wait) trades premium optimization against giving Konji enough supervised owner-absent reps before January 2027. Capture as a model input (BLOCKED pending quote) and a dated timeline decision point. *Scope:* Anais is already a listed insured driver and remains so — the same coverage logic applies to any driver operating owner-absent, so this policy question covers her as well, not only Konji.

- **E. Rebuild the financial model** (sequenced in Section 10, after items B–D, F, H, I, J) per the architecture in Section 9: atomic ledger with separate billing/payment events (D5a); itemized labor (D2); materials/fuel placeholder (D1); two-phase crew-lead cost (D4); reconciliation tab with honestly-scoped coverage (D8). **Both pre-build gates are now cleared and no longer block the build:** Gate B (revenue export-format + service mapping, Follow-Up #6) was substantially resolved 2026-07-06 — the flat ledger exists and is reconciled; the residual bundled-line classification gap (56.6% of gross) does not block it (see Follow-Up #6, `HISTORY.md` H-033, H-036). Gate A (bank-statement consolidation, Follow-Up #9) closed 2026-07-06 — verified cash across all four active accounts (`HISTORY.md` H-039). Phase 7 (the model build) is unblocked.

---

## 7. Open Follow-Up List

Items the owner has explicitly flagged to work through in dedicated sessions. These are not yet scheduled or scoped.

1. **Crew reduction** — letting go of workers who do not work consistently. (Related to #2.)
2. **Time-tracking seat question** — the CRM has 10 prepaid time-tracking seats. The reliable core crew is ~4, but the total roster (including inconsistent / turnover workers) has at times exceeded available seats; the exact reconciliation, which workers to keep, and what the true tracked headcount should be are **deliberately unspecified here** and reserved for a post-transition session. (No headcount figure is asserted in this document pending that session.) *Update, 2026-07-10 (H-049):* this no longer blocks labor-actuals capture the way it did when D2 was written — real Gusto payroll data now provides ACTUAL hours/pay per employee per period (see D2's reframing) independent of CRM time-tracking seats. What this question still determines is **job/customer-level time attribution** (Follow-Up #19), which payroll data cannot provide and which still depends on the CRM's time-tracking feature and its seat allocation.
3. **CRM "Revenue Forecast" page review** — the owner will share screenshots; the built-in forecast is not currently producing useful output.
4. **Job Costing activation** — the mechanics of turning it on and logging materials/fuel per job (target: end of July 2026). Groundwork laid 2026-07-09 (H-038): `model/data/catalog-type-map.csv` classifies all 22 current catalog items as `item` (materials/goods — `Bagged Mulch`, `Landscaping Blocks (stone)`, `New Plants`) or `service` (labor, the remaining 19); every line item in `model/data/revenue-line-items.csv` now carries this as its `kind` column. The 3 `item` rows are exactly the per-job materials line D1 currently zeroes out — once Job Costing is live and materials are logged per job, these become the first real, non-placeholder materials data rather than the coarse expense-bucket lumping D1 describes. *Update, 2026-07-13 (H-066):* the catalog's row count is now **21, not 22** — "Weeding Maintenance" was discontinued and merged into "Weeding" (see `reference/CATALOG-UPDATE.md`). The 3/19 item/service split is unaffected in kind, but the service count is now **18** distinct catalog rows (still 19 *conceptually*, since the merge didn't remove a service, just consolidated a duplicate name) — see Follow-Up #10 for the tax-rate change that prompted this refresh.
5. **Konji door-to-door package** — business cards, an owner-led training session, a short pitch script with default answers to common questions, and address-based lead tracking (using the existing CRM lead-capture setup). Immediate priority is *starting* lead generation (fall mowing, leaf cleanup, ad-hoc project offers; spring pre-sell deferred to after the fall season). Rationale for urgency: time not spent on lead generation is irreversibly lost.
6. **Revenue-by-service mapping** — *substantially resolved (2026-07-06); catalog formally sourced (2026-07-09).* The flat transaction-line ledger exists (`model/data/revenue-line-items.csv`, 152 rows extracted from the 68 source invoices), reconciled to the Homeworks billed anchor to the penny. The legacy→catalog name mapping is resolved (`model/data/service-name-map.csv`, 13 owner-confirmed renames; every line item carries both the literal `service_raw` header and the mapped `service`). The current Homeworks catalog is now a dated, sourced snapshot (`reference/service-catalog-2026-07-09.csv`, 22 items at capture; superseded 2026-07-13 by `reference/service-catalog-2026-07-13.csv`, 21 items — see Follow-Up #10 — active-snapshot pointer in `reference/README.md`; refresh procedure in `reference/CATALOG-UPDATE.md`) rather than an assumed list — all 7 canonical names used by the map were confirmed present in it. Zero-revenue catalog items are now a derived, traceable figure rather than an asserted one: cross-referencing all 22 catalog names against billed line items shows **7 catalog items with zero billed lines** — all 3 `item`-kind rows (`Bagged Mulch`, `Landscaping Blocks (stone)`, `New Plants`; 100% of materials) plus 4 `service`-kind rows (`Blowing`, `Material Pick-up`, `Seeding`, `Sod Placement`; 4 of 19 labor services). The zero-billed set skews toward materials — relevant to Follow-Up #4/D1. The only residual gap: 56.6% of revenue ($15,795.85 of $27,891.65) sits in bundled service lines ("General Maintenance," "Lawn Care," "Lawn Maintenance") that cannot be decomposed into catalog services (or classified `item` vs. `service`) at the line level — a limitation of historical invoicing practice, not of the data (see Follow-Up #13 for the forward fix). Service-resolved: 43.4%. **This gate no longer blocks the first model build** (see `HISTORY.md` H-030, H-033, H-036, H-037, H-038).
7. **CRM expense-tracking convention** — whether to log whole orders or individual line items, given manual entry, including back-dating past obligations and forward-dating future ones. (Example: the $422.26 Home Depot order bundling sprayer, PPE, and tools.)
8. **Model-P&L ↔ CRM-P&L reconciliation** — build a view cross-referencing the (more accurate) model-generated P&L against the CRM's P&L to reveal where the CRM's manual entries need correcting.
9. **Bank-statement (Relay) consolidation** — **CLOSED (Gate A cleared, 2026-07-06).** Account roles confirmed by owner (8735 Income, 3549 OpEx, 3550 Reserve, 8737 Taxes; 8736 Owner's Comp deleted); the three non-active accounts verified $0 across all 17 months (H-026). All four active accounts' current statements captured and independently reconciled: consolidated cash = **$1,225.33 as of 2026-07-06** (`reference/cash-consolidated-2026-07-06.csv`; `HISTORY.md` H-039), superseding the $1,400 single-account interim estimate. This was the last pre-build gate — **Gate B (Follow-Up #6) closed 2026-07-06 per H-033, and Gate A closes here — Phase 7 (the model build) is now unblocked.**
10. **Sales-tax / surcharge treatment — RESOLVED, 2026-07-13 (H-066).**

    **Historical treatment (locked in, not revisited).** The full-history surcharge collected under the old blanket-tax practice — charged on nearly every catalog line regardless of whether the underlying item was legally taxable — totals **$1,556.47 across 148 surcharge rows** in `model/data/ledger-revenue.csv` (re-verified fresh against the full ledger for this resolution, not reused from a stale figure; sums to the penny with `parse_invoices.py`'s own printed summary). This supersedes the narrower **$701.43** figure in item F, which was never a competing or stale number — it was always scoped to the Jan–Jul 2026 collected-vs-billed reconciliation window only, not the full ~13-month invoice history (2025-06 through 2026-06) this resolution needed. Under Virginia's actual rule, labor services were never taxable — only tangible goods are — so almost none of this historical surcharge was ever a legitimate sales-tax collection with a remittance obligation attached. **This retained revenue is permanent, not a liability owed to anyone: there is nothing to remit and no ongoing obligation tied to it.** This is the settled answer to the original "retained revenue vs. pass-through liability" question — no longer open. *Strength of this conclusion, checked directly rather than assumed to hold by default (2026-07-13):* querying `model/data/revenue-line-items.csv` directly confirms **0%** of the $1,556.47 sits on an explicitly-billed material-item line — H-038's "3 item-kind rows, zero billed revenue" finding still holds, re-confirmed fresh, not cited from memory. Of the total, **42.7% ($665.20, 88 rows)** sits on unambiguous, cleanly-mapped `service`-kind lines with no Virginia sales-tax basis at any point — for that portion, the conclusion is complete. The remaining **57.3% ($891.27, 64 rows)** sits on bundled/unmapped lines ("General Maintenance," "Lawn Care," "Lawn Maintenance," etc.) that Follow-Up #6/#13 already established cannot be decomposed into item vs. service at the line level — a bundled visit could in principle have included materials invisibly folded into its flat price, so this portion's "no tax basis" status, while very likely true (no material item has ever appeared as a distinct billed line in any invoice), is **not independently verifiable from this data** and should be read as strong, not fully complete.

    **Forward-looking treatment.** Effective with the 2026-07-13 catalog refresh (`reference/service-catalog-2026-07-13.csv`; see "Service catalog" in `reference/README.md`), the catalog now charges 6% tax on exactly the 3 material (`item`-kind) catalog rows — `Bagged Mulch`, `Landscaping Blocks (stone)`, `New Plants` — matching Virginia's actual legal basis. All 18 remaining `service`-kind catalog rows (labor) now carry 0%. This is a going-forward catalog configuration change, not a retroactive correction: historical invoices already reflect what was actually billed at the time and are untouched (confirmed directly — re-running the full pipeline after the catalog refresh produced zero dollar-amount changes to any historical ledger row; the only diff was a name consolidation, see H-066).

    **One remaining dependency.** Because the 3 material items are now correctly charging real Virginia sales tax, the business must be registered with Virginia Tax to legally collect and remit it — see Follow-Up #26, which this resolution now depends on for the *forward-looking* collection to be properly remittable. Not urgent per owner instruction; deadline 2026-12-31.
11. **Anais compensation terms** — **RESOLVED for canvassing rate and backup crew-lead rate, 2026-07-10 (H-057).** Canvassing pay is a general crew policy (not Konji-specific): $15/hr plus $15 per fall client signed and $20 per spring client signed, applying to anyone doing canvassing work, Anais included — the formerly Konji-specific `konji_canvassing_rate`/bonus rows in `model/data/assumptions.csv` were generalized to `crew_canvassing_rate`/`crew_canvassing_bonus_fall`/`crew_canvassing_bonus_spring` so both reference the same source rather than risking two rows drifting apart (same principle as Decision 11's shared growth-rate row, H-053). Her backup crew-lead rate is $25/hr (CONTRACTED, `anais_backup_lead_rate`), for the expected-rare jobs she leads when Konji cannot be present. **Still open, NOT resolved by this fix:** her regular-wage reduction ($25/hr → $20/hr) is planned by the owner but not yet effective — no effective date exists. No ledger, payroll, or `assumptions.csv` row for her regular wage was touched; this stays pending until the owner confirms the change has actually happened, at a future payroll refresh (same treatment as Konji's own not-yet-run negotiated-rate change, D4).
12. **Business-account consolidation** — moving business tooling off personal/work identities onto dedicated business accounts, so the business's digital assets are cleanly owned and portable. (a) **GitHub:** `Tupelo-Ops` lives under a free GitHub account registered to the business email — *in progress now* (see runbook Phase 1). (b) **Claude:** migrate to a Claude subscription under the business email, business-paid (~$20/mo by the owner's estimate, roughly offsetting the Squarespace spend) — *future, non-urgent*; note that the owner's current personal Claude Pro is paid by a third party, so untangling that arrangement is a separate relationship/expense matter, not a technical one. Claude Code authentication is independent of the GitHub account, so (b) does not block (a).
13. **Line-item billing going forward** — owner action: adjust all recurring visits to be billed by individual line item rather than bundled ("General Maintenance"), so future revenue is service-resolved at the source. This is the forward fix for the bundling ceiling identified in #6; it does not recover historical bundles. *Footnote (2026-07-09):* Homeworks has two pre-built, unbilled Package definitions — `General Maintenance` (Mowing, Weed Whacking/Edging, Blowing, Weeding, Hedge/Branch Trimming, Spraying) and `Lawn Care` (Mowing, Weed Whacking/Edging, Blowing) — see `reference/service-packages-2026-07-09.csv`. As of that snapshot the Packages feature has **never generated a bill**; it's a candidate future mechanism for this fix, not a current one, and is mechanically distinct from the informal bundle labels this item addresses despite sharing names (see `reference/CATALOG-UPDATE.md`'s explicit warning against conflating the two).
14. **Tips as separate income** — **RESOLVED.** Invoices show $424.92 in tips (Jun 2025–Jun 2026), outside the subtotal and absent from every CRM export. This is already resolved by the ledger build, not a remaining ask: `model/data/ledger-revenue.csv` carries a distinct `category="tip"`, 19 rows, summing to exactly $424.92 (verified fresh during the H-051 process-reliability pass, matching this Follow-Up's own cited figure to the penny). Kept as a closed record rather than deleted, per this repo's changelog-preservation convention.
15. **Billed-vs-collected tension** — **RESOLVED (practical question), 2026-07-12.** Every one of the 68 invoices in `model/data/revenue-invoices.csv` shows Amount Paid = This Invoice and Total Due = 0.00, yet the CRM reports $12,726.01 billed vs. $10,973.57 collected for Jan–Jul 2026. **Two distinct claims here — only one is closed.** (1) *Why the CRM's own fields show this* (a "Total Due = 0.00" flag alongside a real collection shortfall) remains genuinely unverified — no confirming detail exists anywhere in the repo, and no cause is asserted; it is plausibly a CRM timing/recording artifact but this is not established. (2) *Whether the model resolves the practical question* — yes: this is exactly what D5a's architecture (`CONTEXT.md` Section 4) was built to route around. Billed and collected are separate ledger events, and per Section 4/Section 6 item F, **Relay bank statements, not CRM payment records, are the source of truth for collected cash**. `model/match_payments.py` (Sequencing Step 3, H-044) already implements this: it reconciles the 58 payment-event rows in `model/data/ledger-revenue.csv` against actual settled Relay deposits, not against the CRM's "Total Due" field, so the model's collected-cash figure was never dependent on trusting the CRM's own (unverified) accounting of itself. Closing this Follow-Up on that basis — the model doesn't need to know *why* the CRM disagrees with itself to already be built on the correct source of truth for cash. If the CRM-side cause is ever independently confirmed, that would be new information, not a reopening of this item.
16. **Catalog/practice tax inconsistency** — the catalog prices Subcontractor Work at 0% tax, but historical "Subcontractor Planning" lines were charged 6%; conversely Debris Removal/Bagging is a 6% catalog item, yet **3 "Bagging" line items, across 2 invoices** (invoice 8's single line; invoice 38's two lines) were charged 0%, against 3 other "Bagging" lines (invoices 55, 58, 59) correctly charged the normal 6%. A second, previously undocumented instance of the same pattern: invoice 75's "Lawn Maintenance" bundle line was also charged 0% surcharge, though bundle lines are otherwise charged the normal rate. Both are the same underlying inconsistency — the surcharge applied against a line inconsistently with its catalog item's or label's expected rate — one on a specific catalog service (Bagging), one on a bundle label (Lawn Maintenance). The surcharge was applied inconsistently against the catalog's own rates. Dollar totals are unaffected (the surcharge is stored per-line as actually charged, not recomputed). Relevant to #10's surcharge-structure decision. Do not correct historical records.
17. **Homeworks card-processing fee schedule — RESOLVED (2026-07-12, H-063).** The combined rate used throughout this ledger, 3.9% + $0.30/charge, was never wrong and **no dollar figure changes as a result of this resolution** — what's newly confirmed is its *composition*. **Owner-confirmed, independently verified 2026-07-12 (not sourced from the AI chatbot testimony logged in H-062):** Stripe's true, raw processing cost for this account is **2.9% + $0.30/charge**; Homeworks adds a **1% platform markup** on top of that for any plan tier below Enterprise (this account is on Growth). 2.9% + 1% = 3.9%, matching the combined rate this ledger has used since H-044 to the decimal. The owner's own verification is what closes this Follow-Up — H-062's chatbot-testimony investigation had already ruled out a *separately-stacked* second deduction and left the *blended-single-rate* composition explicitly unresolved pending exactly this kind of direct confirmation; the chatbot's own (unverified, third) answer happened to land on the same 2.9%+1% split, but that alignment is coincidental to why this is now resolved, not the basis for it — this repo does not treat AI-chatbot assertions as ground truth (H-062). The Homeworks-*direct* (non-Stripe-routed) card-payment channel remains separately confirmed dormant — no evidence of any real transaction through such a channel anywhere in the full Relay or Stripe history (H-061/H-062).

    **Open design question this resolution surfaces, presented for owner sign-off, not decided or built here:** the ledger's single `category="Stripe", subcategory="payment-processing-fee"` row now knowingly blends two different cost drivers — true third-party processing cost (~2.9%+$0.30) and a Homeworks platform markup (~1%) charged specifically because of the plan tier. (a) **Leave it as one blended category (current state).** Simplest; no functional loss, since the combined rate was always applied correctly to every dollar figure — the only cost is that "what we pay Stripe" vs. "what we pay Homeworks" can't be reported separately. (b) **Split into two ledger rows per Stripe payment event going forward** (e.g. `category="Stripe"` at 2.9%+$0.30, `category="Homeworks"` at 1%) for precise cost attribution — relevant mainly if the owner ever evaluates the Enterprise plan's cost/benefit, since eliminating this 1% is one of Enterprise's stated advantages. **Recommendation: (a), unless the owner specifically wants Enterprise-tier cost/benefit visibility** — (b) adds real complexity to `model/match_payments.py`'s fee-computation logic for a split that changes no total and isn't needed for any currently-modeled decision. Not implemented pending the owner's choice.
18. **Unclassified Stripe debit, $38.16, 2026-04-08 — RESOLVED (2026-07-12, H-062).** Confirmed via Stripe's own transaction-level export (`reference/stripe-balance-history-2025-07-07-to-2026-07-03.csv`, H-061): this is a genuine **partial refund**, not a chargeback or dispute. Invoice#40 was originally charged $254.40 (net $244.18 after the standard fee, settled 2026-04-01); a $38.16 partial refund was issued against that same charge on 2026-04-07 ("REFUND FOR CHARGE (Invoice#40)"); Stripe then pulled the $38.16 back from the connected Relay account the next day (2026-04-08) to cover it — the exact transaction Relay recorded as the account's only Spend-type Stripe debit. No ledger correction needed: `model/match_payments.py` already excluded this row from revenue-payment events, which remains the correct treatment (it is a reduction against a charge whose net proceeds were already recorded, not a new/unaccounted cash event).
19. **Labor scope: payroll actuals have no job/customer attribution** — the real Gusto payroll data now sourcing `model/data/ledger-labor.csv` (D2, H-049) records hours and pay per employee per pay period only; it carries no per-job or per-customer breakdown, and it captures all hours combined — recurring and one-off project work alike — whereas D2's original estimate was recurring-work-only. Do not treat the payroll-actuals ledger as a like-for-like replacement of D2's estimate when a per-job or recurring-vs-project labor split is needed; that split still depends on the time-tracking seat question (Follow-Up #2).
20. **Unreconciled $5.37 employer-tax correction, payday 2026-03-31** — a single employer-tax-only correction (a "Tax Reconciliation Payroll" line in the Gusto export, a VA Unemployment true-up, zero wages) could not be matched to any discrete Relay transaction during H-049's full-history reconciliation gate (24 of 25 periods matched exactly on both Net Pay and combined tax; this is the sole exception). It was booked as ACTUAL from the payroll source regardless, since the source figure itself is not in doubt, only its cash-side trace. Distinct from Follow-Up #19: this is a small unresolved bank-trace gap on one correction, not a statement about the payroll ledger's overall scope — closing one should not read as closing the other.
21. **`cash_starting_balance`'s periodic re-verification** — the projection layer's `model/data/assumptions.csv` `cash_starting_balance` row (`model/PROJECTION-PLAN.md` Section 3.2/6 Decision 9) is a hardcoded snapshot by design (the ledger's own net-cash arithmetic can't reconstruct the real bank balance, since ~85% of real settled Spend-side Relay cash has no ledger row). This means it will drift stale the same way the $1,500→$1,400→$1,225.33 starting-cash lineage already did, unless re-verified periodically against a fresh Relay consolidation (the same process H-039 used). Flagged, not solved: whether this needs its own standing update procedure (mirroring `CATALOG-UPDATE.md`/`REVENUE-UPDATE.md`/`PAYROLL-UPDATE.md`) or an ad hoc re-check at each Cash Buffer Policy check-in is still an open question. *Re-verification attempted, 2026-07-12 (H-060):* checked every Relay file in `reference/` directly (not assumed) — the latest transaction across all four active accounts is still 2026-07-06 (the two `"...to 2026-07-06"` partial pulls for 3549/8735; 3550 and 8737 remain $0 throughout, per H-026), identical to the date already backing `cash_starting_balance`. No newer, unincorporated pull exists to re-verify against. **The figure itself (value and as-of date) is left unchanged** — re-stamping today's date without new source data would misrepresent the check as a fresh consolidation when no new consolidation was possible. Actual staleness as of today: 6 days. Next attempt should follow a fresh Relay pull (owner action).
22. **Repo-wide cross-reference/auto-sync audit — RECURRING, not one-time; initial pass complete (H-055).** H-051's ledger-revenue.csv regeneration-gap finding (a real, undocumented dependency between `parse_invoices.py`'s output and the actual ledger, silently unsynced until caught) was the direct precedent and motivation for this item. Scoped in `model/CROSSREF-AUDIT-PLAN.md` and executed once (H-055): catalogued dependencies across the `*-UPDATE.md` procedures, the full pipeline's read/write contracts, `assumptions.csv`'s TARGET/POLICY rows, `RUNBOOK.md` vs. `HISTORY.md`, `reference/README.md`'s active-snapshot pointers, `CONTEXT.md`'s own Follow-Up statuses, `CLAUDE.md`'s fail-closed claims, and cross-file schema assumptions — five stale-documentation findings surfaced and fixed (H-055), zero undocumented (no-safeguard-at-all) gaps found this round. **Cadence, resolving this item's own open decision 4: re-run this audit at each future Revision Point** (RP1 late July 2026, RP2 late December 2026, RP3 June 2027 — `strategy/execution-timeline.md`'s existing schedule), not left as a one-time catch-up — the failure mode this audit hunts for (a correction applied to one copy of a fact, not its duplicate) is structural to having any duplicated information at all, and will recur as `strategy/`, `CONTEXT.md`, and the projection layer keep evolving together. **This Follow-Up stays open/recurring, not resolved** — each Revision Point's audit should append its own findings to `model/CROSSREF-AUDIT-PLAN.md` (or a dated successor) and a new `HISTORY.md` entry, the same pattern as the `*-UPDATE.md` refresh cadences.
23. **Reduce reliance on externally-authored prompts for routine repo maintenance.** `HISTORY.md` logging and other standing procedures should trigger automatically from `CLAUDE.md`'s own rules, not require an explicit instruction each time (from the owner or from an external AI drafting prompts on the owner's behalf). Nearly every prompt this session has had to separately request "write the HISTORY.md entry" as an explicit step — but `CLAUDE.md`'s History-logging rule already says every significant change gets one; the rule exists, the compliance is just not yet self-triggering. Scope for a future session: audit `CLAUDE.md`'s other standing procedures (the model-update loop, the arithmetic self-check, the cross-reference check, the pipeline-refresh rule) for the same gap — is each one something Claude Code applies on its own initiative when relevant, or does it currently depend on being explicitly invoked each time? Distinct from Follow-Up #22 (which catalogs dependencies between files/values); this is about whether Claude Code's own documented behaviors are actually self-triggering versus needing external prompting to invoke them. **Note for whenever this is implemented (written in now, regardless of timing):** the deeper fix is likely a self-check Claude Code runs on its own initiative — at the start of every session, and after every meaningful change within a session — asking "does this warrant a `HISTORY.md` entry (or another standing procedure) per my own rules?" without waiting to be told. This is a behavior-design change, not a quick patch: it means adding an explicit instruction to `CLAUDE.md` that Claude Code itself should proactively check its own standing rules for applicability after any non-trivial action, rather than only applying them when a prompt happens to invoke them. Scope this as its own session when picked up — don't bolt it on as an afterthought to a smaller fix.
24. **Xavier revenue-share bonus components — BLOCKED, two milestone dates to revisit.** H-059 (2026-07-11) resolved Xavier's equity-buyout figure ($1,014.09, final) but left the revenue-share bonus components explicitly BLOCKED pending real data, per `model/data/assumptions.csv`'s `xavier_revenue_share_wmp_3mo_flat`/`_6mo_flat`/`_3mo_declining`/`_6mo_declining`/`xavier_anacostia_share` rows. **Windows are calendar-month-aligned, starting 2026-05-01** (the first full calendar month after Xavier's corrected 2026-04-30 departure — a deliberate simplification for easier tracking, owner-requested 2026-07-11, replacing the original day-exact anchor). **Two dated triggers to actively revisit, not just wait to be asked about again:** (a) **2026-07-31** — the 3-month (May–June–July 2026) WLF/Moore/Picarello window closes; compute the flat-10% and declining-15/10/5% 3-month scenarios from real data and present both so the owner can choose one. (b) **2026-10-31** — the 6-month (May–October 2026) window closes; same for the two 6-month scenarios. Anacostia's one-time share is blocked on a separate, undated prerequisite (pulling real invoice-level data for that customer into `model/data/revenue-invoices.csv`, following the established new-data-source pattern) and can be resolved independently of either date, as soon as that small data pull happens. A scheduled reminder is in place alongside this Follow-Up (see `HISTORY.md` H-059) so these dates surface proactively rather than depending on the owner remembering to ask — **two one-time Claude Code scheduled cloud-agent routines created 2026-07-11, confirmed via `/schedule list` that no stale routine existed for the original 07-28/10-28 dates before creating: `trig_014XfF1mHXZjq78LMk64YUZL` (fires 2026-07-31T14:00:00Z) and `trig_01AZ3cQitkVfBisEyynZKcLX` (fires 2026-10-31T14:00:00Z), each scoped to only open a GitHub issue on this repo flagging the closed window — never to compute figures or edit/commit anything.**
25. **Stripe payment re-attribution — deferred, quantified opportunity (H-061 finding 8, formally logged as its own Follow-Up 2026-07-12/H-064).** All 32 of the `unattributed [aggregated]` Stripe payment-event rows in `model/data/ledger-revenue.csv` could be re-attributed to specific invoice(s) using `reference/stripe-balance-history-*.csv`'s `Description`/`Transfer` linkage — this was demonstrated, not just asserted (H-061 findings 1 and 6a: every payout's net ties exactly to an existing aggregated row, and every charge's invoice-gross-plus-tip ties exactly to its Stripe `Amount`). **Quantified impact if built:** of the 32 payouts, 25 map to exactly one invoice (a clean re-tag) and 7 map to a batch of 2–3 invoices (would need to become multiple payment-event rows, mirroring the existing tier-2 "batch" pattern already used for ACH matches) — 32/32, not a partial win. Recomputed fresh against the full ledger (not the narrower Jan–Jul CRM-derived estimate `model/PHASE7-PLAN.md` Finding 3 originally cited): of the $30,023.25 in total payment events, currently 56.81% ($17,055.05) is directly attributed, 32.83% ($9,856.20) is the Stripe-aggregated bucket, and 10.37% ($3,112.00) is a small number of genuinely unmatched non-Stripe rows (unrelated to this opportunity). Converting all 32 Stripe rows would raise attribution to **89.63%** ($26,911.25), leaving only the unrelated 10.37% unmatched. **Not built in this pass, deliberately** — this is a real rebuild of `model/match_payments.py`'s tier-3 classification logic (folding Stripe charges into the existing tier-1/tier-2-style matching, since Stripe's own `Description` field cites invoice numbers directly, structurally equivalent to tier 1's citation-match criterion), not a small fix, and should be scoped as its own session per this repo's standing practice for changes of this size.
26. **Virginia sales-tax registration/permit status — not urgent, deadline 2026-12-31 (H-066).** Follow-Up #10's resolution means the business is now correctly charging real Virginia/Arlington sales tax (6%) on the 3 material catalog items (`Bagged Mulch`, `Landscaping Blocks (stone)`, `New Plants`), effective with the 2026-07-13 catalog refresh. Legally collecting and remitting that tax requires the business to be registered with Virginia Tax. **Owner has explicitly deprioritized this** — not urgent now, but must be resolved before the next tax filing. Action: confirm Virginia sales-tax registration/permit status for these 3 items; register if not already done. **Deadline: end of calendar year 2026 (2026-12-31)** — owner-confirmed as the applicable filing-year boundary for this business, not a default assumption (Virginia sales-tax filings are themselves periodic, not annual, so this deadline is about registration being in place, not a specific periodic filing due date). See `strategy/execution-timeline.md`'s corresponding sequencing item.
27. **Stripe-fee customer passthrough — new policy, ledger design questions open, nothing built yet (2026-07-13/H-066).** Owner-described policy, effective going forward: for credit-card payments, the customer is now charged an additional amount equal to Stripe's real per-transaction fee (2.9% + $0.30), on top of their invoice total. The business itself still pays the full 3.9% + $0.30 to Stripe/Homeworks as before (unchanged) — the 1% Homeworks platform markup portion (see Follow-Up #17) is **not** passed through and remains an absorbed business cost; the customer's reimbursement covers only the 2.9%+$0.30 Stripe-proper portion. **Explicit, specific questions to answer before any ledger design is built — do not guess at any of these:**
    a. Does this fee reimbursement appear as its own distinct line item on the Homeworks invoice/receipt (a separate charge the customer sees), or is it applied invisibly at the payment-processing step without appearing as an invoice line?
    b. Is the reimbursement computed on the full charged amount (service + the 6% sales tax on taxable items + any tip), or only on the pre-tax/pre-tip service subtotal? This determines whether it's a percentage of a different base than Stripe's real fee is actually calculated on.
    c. Checked, 2026-07-13 (re-verified directly against the export, not reused from memory): `reference/stripe-balance-history-2025-07-07-to-2026-07-03.csv`'s `customerProcessingFee (metadata)` column is unused across all 40 charge rows — 38 carry a literal `0`, the remaining 2 are truly blank (matches H-061's original finding exactly). This export predates this policy, so it cannot yet confirm whether this exact field is what would populate once the new policy is live, only that no transaction under the new policy has occurred within its coverage window (through 2026-07-03). No newer Stripe export or Homeworks invoice reflecting a real transaction under the new policy exists in the repo as of this writing. **This can only be confirmed once real transactions occur under the new policy** and a fresh `reference/stripe-balance-history-*.csv` export (or equivalent) is pulled per `reference/STRIPE-UPDATE.md`.
    d. Should this reimbursement be booked as its own new ledger revenue category (parallel to how `tip` and `surcharge` are already separate categories in `model/data/ledger-revenue.csv`), or folded into an existing category? **Recommendation: its own new category** (e.g. `category="fee-reimbursement"`), parallel to `tip`/`surcharge` — it is neither a service sale nor a tax/tip, and blending it into an existing category would corrupt that category's own meaning (the same reasoning that already keeps `tip` and `surcharge` separate from `service`). Flagged as the owner's decision, not a default — not built.
    e. Does ACH/check payment get any equivalent treatment, or does this policy apply exclusively to card payments? Per the owner's description, it should apply to card payments only — **not yet confirmed against Homeworks' actual configuration**, only against the owner's stated intent.
    **Not built in this pass, deliberately** — no `model/build_ledger_revenue.py`/`model/match_payments.py` changes, no new `model/data/assumptions.csv` row, no ledger schema change. This Follow-Up exists purely to capture the open questions precisely so a future session can design the ledger treatment once they're answered.

---

## 8. File-Conversion Map, Repository Structure, and File Specifications

**File-conversion map** (note: "import as-is" means *content* is unchanged; files are still relocated into subfolders and renamed to kebab-case per the repo convention):

| Source file | Action | Repo destination | Reason |
|---|---|---|---|
| `CheckIn_Template.md` | Import as-is; rename | `check-ins/check-in-template.md` | Already Markdown; diffs cleanly |
| `Execution_Timeline_2026-2027.md` | Import as-is; rename | `strategy/execution-timeline.md` | Already Markdown |
| `Strategic_Plan_2026-2027.md` | Import as-is; rename | `strategy/strategic-plan.md` | Already Markdown; diffs cleanly |
| `Operational_Toolkit.md` | Import as-is; rename | `strategy/operational-toolkit.md` | Already Markdown; diffs cleanly |
| Homeworks CSV exports, the P&L report `report_profit_and_loss_2.pdf` (period: Jan 1 – Jul 1 2026), Relay bank statements | Import as-is | `reference/` | Raw source data; the audit trail / ground truth |
| `Landscaping_Financial_Model_3.xlsx` | Decompose into CSV data + build script | `model/` | Per D7 |

**File-naming convention:** all repository files use lowercase kebab-case (e.g., `execution-timeline.md`), except the conventional uppercase root files (`README.md`, `CLAUDE.md`, `CONTEXT.md`, `SETUP.md`, `HISTORY.md`). Original source filenames are not preserved inside the repo.

**Repository structure:**

```
Tupelo-Ops/                # Repository name; lives under the business's own free GitHub account (registered under the business email — not the owner's personal or work account), so the repo is a clean business asset
├── README.md              # Repository map and entry point
├── CLAUDE.md              # Operating instructions for Claude Code (spec below)
├── CONTEXT.md             # This document
├── SETUP.md               # New-Codespace environment setup for this repo (spec below)
├── HISTORY.md             # Master append-only audit log (spec below)
├── strategy/              # strategic-plan.md, operational-toolkit.md, execution-timeline.md
├── check-ins/             # check-in-template.md + dated check-in logs
├── model/
│   ├── data/              # Atomic ledger + assumption tables (CSV) — SOURCE OF TRUTH
│   ├── build_model.py     # Generates the workbook from data/
│   ├── requirements.txt   # Pinned Python dependencies (e.g., openpyxl) — the reproducibility guarantee
│   └── financial-model.xlsx  # GENERATED OUTPUT — gitignored; regenerated on demand, never hand-edited
├── reference/             # Raw source data (CRM exports, P&L report, bank statements, overhead contracts) — GROUND TRUTH, never edit; also holds dated .xlsx milestone snapshots (committed deliberately)
└── .gitignore             # Excludes the generated workbook and transient build artifacts
```

**`CLAUDE.md` specification (lean, project-specific).** It must contain:
- **Project identity** — a one-paragraph description of the business and the repository's purpose.
- **Standing rules and engagement preferences** — Section 5, reworded as operating instructions for Claude Code (prompt before editing files; review for staleness during check-ins; verify quantitative structure before reasoning; never hardcode numbers into projections; flag gaps rather than guess).
- **Architecture summary** — the ledger-and-derivation model (D5, D5a, D6) in brief.
- **Model-update operating procedure** — explicit, step-by-step instructions (see the workflow in Section 9). When the owner asks for a change, Claude Code edits the *data or script*, regenerates the workbook, shows what changed, and pauses for review — it never hand-edits the generated `.xlsx`. **Confirmed default behavior:** (1) never hand-edit the generated workbook — fix the data or the script and regenerate; (2) after any data-file edit, immediately regenerate the workbook so data and workbook are never out of sync; (3) report what changed; (4) stop for owner review before committing — never auto-commit. The owner may override per-instance (e.g., "batch several edits before regenerating"), but this is the standing default.
- **Ground-truth rule** — `reference/` contains raw source data (CRM exports, bank statements, overhead contracts) and is **never edited**; it is the source against which everything reconciles.
- **File conventions** — plain text (Markdown/CSV) preferred; every quantitative row carries a status tag (`ACTUAL` / `ESTIMATE` / `BLOCKED`); estimates are always flagged; kebab-case naming.
- **Cross-reference check on structured edits** — when editing or revising any multi-section document, verify the *joins*, not only the parts: after a change, confirm that a reader arriving at the changed section can reach any justification, dependency, or definition it relies on without already knowing where that lives (a reason stated in one section must be *pointed to* from the section where a reader would ask). Unlinked or contradictory cross-references — not wrong content — are the dominant failure mode of structured documents. This is a triggered check (it fires when editing structured docs), not an always-on rule.
- **History-logging rule** — significant decisions, milestones, revisions, data changes, and tooling changes get one appended entry in `HISTORY.md` (the master log); per-file changelog tables are secondary convenience only.
- **RAG-readiness conventions** — clean semantic sections, front-matter metadata (date, type, status, tags), atomic well-named files.
- **Deferred tooling (stated affirmatively)** — this project runs a lean stack. Two capabilities are intentionally deferred: (a) multi-agent orchestration (Ruflo / claude-flow), per D9; and (b) a vector database / RAG retrieval layer (e.g., RuVector), per D10. State affirmatively that neither is in use, so the instruction holds regardless of any configuration outside the repository.

**Correctness note — machine-global Claude Code context.** Claude Code supports a machine-global instruction file (commonly `~/.claude/CLAUDE.md`, in the user's home directory rather than the repository) whose contents can apply across repositories on that machine — including this one. *The exact path and precedence order are a product detail that can change, so confirm them against current Claude Code documentation rather than treating the behavior described here as fixed.* If a machine-global Ruflo context file (from the owner's other setup) exists, it could apply here too, silently reintroducing behavior this project has deferred. The repo-level `CLAUDE.md` should therefore state explicitly that Ruflo/RuVector behaviors are deferred *for this project*, in case a global file says otherwise. This is a genuine correctness item, not housekeeping — the kind of hidden cross-contamination that is hard to debug later. **First, check whether that global file is actually visible to this environment** (inspect `~/.claude/CLAUDE.md`); if it does not exist or contains nothing relevant, this concern does not apply and can be disregarded.

**`SETUP.md` specification (new-Codespace environment setup, self-contained).** A dedicated, standalone guide the owner follows each time a fresh Codespace is opened. Written from scratch for this project; does not reference, derive from, or imply any external setup document. It must cover, at minimum:
- **Verify the dev environment** — confirm `node`, `npm`, and `git` return version numbers in the terminal.
- **Git identity** — set `user.name` and `user.email` so commits succeed (Codespaces do not always pre-populate these).
- **Claude Code** — confirm it is installed and authenticated to the correct account; the Claude Code editor extension is assumed already present.
- **Python + dependencies** — confirm Python is available, then install pinned build dependencies with `pip install -r model/requirements.txt` (e.g., openpyxl). Pinned versions — not a committed binary — are the reproducibility guarantee for the build.
- **VS Code extensions** — install **Python (Microsoft)** (bundles **Pylance**) and **Rainbow CSV** (essential); optionally **GitLens** and an **Excel viewer** (recommended). Markdown preview is built into VS Code.
- **Auto Save** — enable it so edits persist to the Codespace disk (persistence to GitHub still requires commit + push; a deleted/rebuilt Codespace loses uncommitted work).
- **First-run sanity check** — a simple confirmation task proving the environment is healthy.
Explicitly excluded: any Ruflo, claude-flow, RuVector, or multi-agent installation steps.

**`HISTORY.md` specification (master append-only audit log).** A single chronological, append-only record — the canonical answer to "what changed, when, and why." It consolidates what would otherwise be a separate decision log (there is intentionally no separate `decisions/` file; decisions are one entry type here). Each entry carries:
- **Date** · **Type** (`DECISION` / `MILESTONE` / `REVISION` / `DATA` / `TOOLING`) · **Stable ID** (e.g., `H-001`) · **Description** · **Reasoning/basis**.
Filtering to `DECISION` entries yields the decision track alone. Ledger rows may cite a `HISTORY.md` entry ID in their `source_file`/`source` field, so a number can trace to the decision that justified it. `HISTORY.md` is canonical; the per-file changelog/revision tables already embedded in the strategy documents are retained as local convenience but are **secondary** — not the source of truth — to avoid drift.

---

## 9. Model Architecture Specification

**Two layers.**
- **Data layer** (`model/data/`, CSV, source of truth): the atomic ledger plus assumption tables. Contains actuals and clearly-flagged estimates only.
- **Projection layer** (the forward model, generated into the workbook): references the data layer and labeled assumptions only; contains no hardcoded numbers.

**Atomic ledger — proposed schema** (one row per atomic financial event):

`date | type | event | category | subcategory | customer | quantity | unit_rate | amount | status | source`

- `type` ∈ {revenue, labor, overhead, materials, capital}
- `event` — for revenue, distinguishes an **invoice** (billed) from a **payment** (collected), per D5a, so partial payments represent themselves as their own rows. Billed = sum of invoice events; collected = sum of payment events. (For non-revenue types, `event` records the relevant sub-distinction or is left blank.)
- `status` ∈ {ACTUAL, ESTIMATE, BLOCKED}
- `source` — the raw export, bank statement, overhead contract, or `HISTORY.md` entry ID the row traces to.

*Design note:* the earlier draft used a binary `paid_flag`; it was replaced by separate invoice/payment events because a boolean cannot represent partial collection and would overstate cash (the ~$1,752 billed-collected gap is real). This change is logged in `HISTORY.md`.

**Derived views** (all generated by formula from the ledger): monthly P&L; plan-vs-actual; quarterly rollups; revenue by service; revenue by customer; blended labor rate (derived, never entered). A monthly total can honestly display its composition, e.g., "X total, of which Y ACTUAL and Z ESTIMATE."

**What is real vs. estimated at build time:**
- **Revenue** — traceable to CRM revenue exports as invoice/payment events. The flat transaction-line ledger now exists (`model/data/revenue-line-items.csv`, 152 rows, reconciled to the Homeworks billed anchor to the penny), so revenue can be fully populated from it — this no longer gates the build (Follow-Up #6, resolved 2026-07-06; see Section 10 step 5). The `BLOCKED — unmapped` tag is narrowed accordingly: it applies only to service classification on bundled lines ("General Maintenance," "Lawn Care," "Lawn Maintenance" — 56.6% of gross) that cannot be decomposed into catalog services, not to revenue amounts, dates, or customers, which are all ACTUAL regardless of bundling. Collected events are cross-checked against Relay bank statements (Follow-Up #9, closed 2026-07-06 — see `HISTORY.md` H-039).
- **Labor** — real, ACTUAL, sourced from Gusto payroll data per employee per pay period (D2, reframed H-049); the $25/$20 2-person 50/50-split model is now a forward-looking projection-layer assumption only, not the source of historical figures. Payroll actuals carry no job/customer attribution and cover all work combined, not recurring-only (Follow-Up #19).
- **Fixed overhead** — real once the contract figures are added to `reference/` (Section 6 item I); the equipment-maintenance line is **BLOCKED until an allocation base and method are stated** (then an ESTIMATE carrying that basis), not a silently-modeled ACTUAL.
- **Materials/fuel** — placeholder (`$0`, BLOCKED) per D1 until Job Costing is on.
- **Sales-tax / surcharge** — BLOCKED, convention unverified, per Follow-Up #10; do not book as revenue or liability until resolved.
- **Capital events** — real, subject to the corrections in Section 6 (Xavier and the owner's $3,800 truck-debt repayment both unpaid/demand-triggered, sequenced Xavier-then-owner, per item D; truck entry not a new outflow per item H; auto-insurance increase BLOCKED pending quote per item J).

**Reconciliation tab (D8):** compares generated **revenue and collected-cash** totals against CRM exports and Relay statements and flags mismatches. It does **not** verify itemized overhead or materials/fuel until those gain in-repo source data; the tab should state this scope on its face so its coverage is not overread.

**The model-update workflow (the daily operating loop):**
1. Edit a data file (e.g., add invoice/payment events).
2. Run the build script (`python model/build_model.py`) from the VS Code terminal.
3. The script regenerates `financial-model.xlsx` from the data.
4. Inspect the workbook.
5. Review the plain-text diff of the changed CSV in Source Control; write a short, descriptive commit message; commit.
*The payoff:* every change to the business's numbers becomes a labeled, reversible, human-readable entry in a permanent history. Git functions as an infinite undo — committing often in small pieces means nothing committed can be permanently broken.

**RAG-readiness (D10) — structural only, no build now.** Maintain the plain-text, clean-section, front-matter, atomic-file conventions. Trigger for adopting a vector database: the accumulated corpus (years of check-ins, job/client history) no longer fits comfortably in context, or recurring semantic search across many logs becomes necessary. Keep the eventual choice tool-agnostic.

---

## 10. Sequencing (High-Level)

The granular, checkable runbook (every command and click, from repository creation through the first successful model build) is produced separately, after this document has been reviewed. High-level order:

1. **Environment setup** — create the empty GitHub repository (named `Tupelo-Ops`, under the business's dedicated free GitHub account — registered under the business email, not the personal or work account); open it in a Codespace via VS Code Desktop. For the *first* bring-up, follow the inline environment checklist (verify `node`/`npm`/`git`; set git identity; confirm Claude Code auth; confirm Python and, once `model/requirements.txt` exists, install it; install extensions — **Python + Pylance and Rainbow CSV** essential, **GitLens** and an **Excel viewer** optional; the Claude Code editor extension is assumed present). `SETUP.md` (authored in step 3) captures this same procedure for *subsequent* new-Codespace sessions.
2. **Populate the repository (baseline)** — *this begins with an **owner action**, not an executor action:* the source files live outside the Codespace (in the owner's local environment / prior workspace), so Claude Code cannot reach them until the owner **uploads** them. The owner uploads the enumerated source files (Section 8 conversion map — the two strategy `.md` docs, the `.xlsx`, the CRM CSV exports, the P&L report, and the Relay statements) into the working tree (e.g., a temporary `incoming/` folder). Then: import all files as-is (relocated + kebab-cased) — the strategy documents arrive as Markdown and need no conversion; place raw exports, the P&L report, and bank statements in `reference/`; add this `CONTEXT.md`. **Commit this unedited baseline** before applying any content changes, so the "before" state is in history.
3. **Author repo files** — `CLAUDE.md`, `SETUP.md`, `README.md`, `HISTORY.md`, `.gitignore` (excluding `financial-model.xlsx`), and `model/requirements.txt` per Section 8 (seed `HISTORY.md` from this document's decisions/events).
4. **Complete the Phase 1 reconciliation queue** — Section 6 items **B, C, D, F, H, I, J** (items A, G, G2, G3, G4 are already done in steps 2–3): the stale-file corrections, overhead sourcing, and truck/Xavier/insurance/revenue-anchor entries.
5. **Both pre-build gates cleared** — Gate B (revenue export-format + service mapping, Follow-Up #6) cleared 2026-07-06: the flat transaction-line export exists and is reconciled; see `HISTORY.md` H-033. The residual bundled-line classification gap is tagged `BLOCKED — unmapped` per Section 9 but does not block the build. Gate A (bank-statement consolidation, Follow-Up #9) closed 2026-07-06: all four active Relay accounts reconciled to a verified $1,225.33 cash total; see `HISTORY.md` H-039. No pre-build gate remains open.
6. **Build the model** (item E) — define the data-layer CSVs with separate invoice/payment events, write `build_model.py` (appending any new dependencies to the `requirements.txt` created in step 3, not re-creating it), generate the (gitignored) workbook, add the honestly-scoped reconciliation tab, validate against `reference/` (CRM revenue + bank cash).
7. **Resume the check-in cadence** in the new environment.

---

*End of CONTEXT.md (revision 6). This document reflects decisions locked as of the sessions preceding the repository migration and is written to be intelligible without prior project context. Items explicitly deferred to post-transition sessions (tax treatment, time-tracking headcount/seat resolution, crew retention, CRM export format) are flagged as open rather than resolved, by design.*
